node_modules/
dist/
.output/
.vercel/
.nitro/
.tanstack/
.vinxi/

# Rust build directories (cli/, wasm/search/)
**/target/

# Local release products — packages are published on the Releases page, never
# committed; see cli/releases/README.md.
cli/dist/
cli/stage/
cli/stage-dmg/
*.app/

# The TLHdig archive is ~71 MiB and is downloaded on demand.
cli/fixtures/*.zip

screenshots/*.png
*.log
.DS_Store
.env
.env.*
