# Спецификация стека проекта Aruna

Редакция 28 от 04.09.2026. Заменяет редакции 1–27 целиком. Установлены Svelte 5.57.0 и pnpm 11.25.0; проверка уязвимостей npm-пакетов разведена по наборам после отказа внешнего сервиса рекомендаций.

**Правки при установке 04.09.2026.** Редакция 28 собрана от выданного файла редакции 25 – то же, что редакция 25 сделала с 24-й, – и потому слепа к работе, стоявшей в репозиторной копии. Проверено счетом: `diff` выданной 25-й с 28-й дает 123 строки и двенадцать осмысленных правок, `diff` репозиторной копии с 28-й – 401 строку. Все двенадцать правок внесены поверх копии, шестьдесят одно место отступления назад отвергнуто, и восстановлен абзац о порядке отслеживания крейтов Typst, который 28-я потеряла. Ниже – запись, оставшаяся от установки редакции 25 и по-прежнему верная.

**Правки при установке редакции 25.** Она была собрана от выданного файла редакции 24 и не видела работы 02–04.09.2026, уже стоявшей в репозиторной копии: перенос ее текста дословно вернул бы назад двадцать два места – импорты плагинов, снятое разрешение `core:window:allow-close`, каталог `src/inventory/`, порожденные типы, три переопределения, покрытие и числа тестов оболочки, контур выпуска и раздел 7.5 целиком. Эти места сохранены в том виде, в каком их оставила работа, а каждое улучшение редакции 25 внесено поверх них. Четыре внутренних расхождения самой редакции сняты там же: версия оболочки, покрытие оболочки, номер Claude Code и версия pnpm назывались в двух местах по-разному. Отклонено и одно утверждение, которое нечем подтвердить: §7.2 датировал сплошную ревизию 04.09.2026, а она в этот день не проводилась – дата возвращена на 02.09 и рядом названо, что именно сверено 04.09. Перечень с обоснованиями – в отчете `aruna-spec-v25-sync-2026-09-04.md`.

**О числах в этом документе.** Количество тестов и проценты покрытия стареют при каждом добавленном тесте, поэтому источником истины считается не документ, а вывод команд: `cargo nextest run --locked`, `cargo llvm-cov` после `cargo llvm-cov clean --workspace` (без очистки инструмент неидемпотентен), `pnpm vitest run`. Числа ниже приведены с датой замера и означают порядок величины, а не константу.

Отличие от прежних редакций принципиальное: документ описывает не фронтенд-окружение, а весь стек проекта – консольный крейт, настольное приложение и его интерфейс как три части одного целого, с общими правилами версий, безопасности и проверок.

## 1. Назначение и границы документа

1.1. Документ описывает стек программы Aruna: состав компонентов, основания выбора, правила работы и критерии приемки. Он отвечает на четыре вопроса – что стоит, почему именно это, как проверить, что все на месте, и чего еще нет (раздел 8).

1.2. Что документ не покрывает: логику самого приложения, устройство крейта cli, содержание корпуса TLHdig. Правила для агентов вынесены в CLAUDE.md в корне репозитория; расхождение между этим документом и CLAUDE.md считается дефектом документации.

1.3. Проект – это одна программа, Aruna, и контур интерфейса, который для нее строится.

Программа читает корпус TLHdig, разбирает его и формирует опись. Сейчас она существует в консольном виде – это текущая форма, выбранная сознательно, а не отдельный продукт. Интерфейс к ней разворачивается в два слоя: оболочка на Tauri дает окно, разрешения и мост, фронтенд на Svelte рисует то, что видит человек. Ни оболочка, ни фронтенд самостоятельной ценности не имеют и вне программы не существуют.

| Слой | Каталог | Пакет | Роль |
|---|---|---|---|
| Ядро программы | `cli` | `aruna` 2.5.2 | загрузка корпуса с Zenodo, нормализация, опись; консольный вход и библиотека |
| Приложение с окном | `src-tauri` | `aruna-desktop` 2.5.2 | второй вход в ту же программу: окно, разрешения, четыре команды к ядру; с 04.09.2026 именно оно распространяется |
| Интерфейс | `frontend` | `frontend` (частный) | одностраничное приложение на Svelte и Vite |

Пакет назван `aruna-desktop` только потому, что имя в Cargo обязано быть уникальным, а имя `aruna` принадлежит ядру. Это **не прослойка и не второй продукт**: ядро входит в приложение обычной зависимостью и компилируется в тот же бинарник, а приложение – вторая точка входа в ту же программу, живущая отдельным пакетом потому, что упаковщик приложений macOS этого требует. Оба крейта в одном воркспейсе с 30.08.2026 (4.9.1).

С 04.09.2026 версии ядра и приложения равны, и равенство сторожит тест.

Связь между слоями установлена 30.08.2026: оболочка объявляет зависимость от ядра по пути внутри воркспейса и вызывает его первой командой (4.9.6). Окно спрашивает у программы, где лежит собранный пакет, и показывает путь – то же самое, что программа печатает в консоли.

Корневой package.json – манифест воркспейса Node с командами верхнего уровня. Корневой Cargo.toml – манифест воркспейса Cargo: он перечисляет оба крейта и держит профили сборки (4.9.1, 4.9.3). Там же лежит единственная конфигурация политик зависимостей `deny.toml` (3.4).

1.4. Статусы компонентов, используемые далее: **установлено** (присутствует, версия подтверждена выводом инструмента), **настроено** (сконфигурировано под проект), **проверено** (работоспособность подтверждена прогоном), **ожидает** (отсутствует осознанно, подключается на следующем этапе), **исключено** (отвергнуто, причина записана).

1.5. Состояние на 04.09.2026, замерено выводом команд. Действующий выпуск – **v2.5.2**, он же третья опора; v2.5.1 снят с опор и удален целиком в тот же день. Работа, копившаяся с 02.09 незакоммиченной, отправлена четырьмя коммитами: иконки шаблона (`ab5276b`, державшийся с 03.09 намеренно), оболочка как продукт, тесты отказных ветвей и швов, документация с редакцией 28. Расхождений между облаком и рабочим деревом не осталось. Рабочее дерево несет 28 незакоммиченных файлов тремя областями: правки владельца от 02.09 (точные пины двенадцати зависимостей оболочки 4.9.4, набор 6.1, переопределения 5.2), задание на выпуск от 04.09 (3.5, 6.7) и тесты швов от 03.09 (4.9.8). Отсюда и все расхождения между облаком и деревом: в облаке манифест оболочки по-прежнему с диапазонами, `productName` и идентификатор – прежние, версия оболочки – 0.2.0.

1.6. Документы рядом с этим: `docs/ARCHITECTURE.md` – слои Rust-части и точки присоединения адаптера Tauri и будущего PDF; `docs/FRONTEND-CONTRACT.md` – договор между оболочкой и фронтендом, четыре команды и состояния окна; `docs/XML-CONTRACT.md` – что можно делать с корпусом, что обязано сохраниться и куда каждый вид данных XML должен попасть в PDF; `docs/PDF-ACCEPTANCE.md` – критерии, по которым будет приниматься будущий конвертер, написанные до его появления; `docs/FONTS.md` – стек шрифтов и основания его состава; `docs/TESTING.md` – профили тестов; `PERFORMANCE.md` – измеренные числа. Этот документ не повторяет их содержание, а ссылается на него.

## 2. Платформа и ее следствия

2.1. Целевая и единственная платформа – macOS 13.7.8 Ventura на Apple Silicon. Обновление операционной системы невозможно. Это исходное ограничение, из которого вытекает большинство решений документа.

2.2. Приложение работает в системном WKWebView, который на этой версии системы больше не обновляется. Отсюда обязательный нижний порог сборки `build.target = 'safari16'` в vite.config.ts (см. 4.3).

2.3. Xcode 15.2 – потолок для этой системы: версии 15.3 и старше требуют Sonoma. Сборочный контур привязан к нему.

2.4. Экосистема постепенно снимает поддержку Ventura. Практическое следствие: любое обновление проверяется в том числе на сохранение поддержки macOS 13, а решение, требующее более новой системы, отклоняется, а не обходится. Первый случай уже произошел – из стека выпал Playwright (см. 4.4).

2.5. Релизная сборка – universal binary, `pnpm build` в корне выполняет `tauri build --target universal-apple-darwin`. Оба таргета Rust установлены.

## 3. Состав окружения

### 3.1. Системная база

| Компонент | Версия | Статус |
|---|---|---|
| macOS | 13.7.8 (22H730), Apple Silicon | установлено |
| Xcode / clang / Git | 15.2 (15C500b) / 15.0.0 / 2.39.3 | установлено |
| Rust stable, Cargo | 1.97.1 | установлено |
| Компоненты toolchain | объявлены `rustfmt` и `clippy`; фактически установлены также `llvm-tools` и `rust-std` под три таргета – наследие прежнего тулчейна | установлено |
| Языковой сервер Rust | приходит расширением Claude Code `rust-analyzer-lsp` со своим бинарником, не через rustup | установлено |
| `rust-src` | отсутствует; нужен только для перехода в исходники стандартной библиотеки | не установлено (7.3) |
| Таргеты Rust | aarch64-apple-darwin, x86_64-apple-darwin | установлено |
| Node.js | 22.23.1, в ~/.local/bin, без Homebrew | установлено |
| pnpm / corepack | 11.25.0 / 0.35.0 | установлено 04.09.2026, см. 7.2 |
| npm | 12.0.2, часть Node-инсталляции | установлено, в проекте запрещен (5.3) |
| tauri-cli | 2.11.4 | установлено |
| typescript-language-server | 6.0.0, глобально через pnpm | установлено |

Посторонний таргет wasm32-unknown-unknown оставлен для соседних инструментов и проекта не касается. Линия Node 22 находится в поддержке до апреля 2027 – это плановая точка пересмотра Node-контура.

**Версия компилятора названа в одном месте – `rust-toolchain.toml` в корне.** Там же закреплены оба целевых таргета, и это не украшение: тулчейн, который ставит rustup, приносит только хостовый таргет, а таргеты принадлежат тулчейну. Когда канал закрепили, второй таргет молча исчез – сборочный контур не заметил, потому что называет оба сам, и сломалась только сборка на машине разработчика, с ошибкой про таргет, который документ числил установленным. Объявление в файле снимает это раз и навсегда.

Поле `rust-version` в манифестах не заводится – основание в 4.9.2.

### 3.2. Фронтенд

| Компонент | Версия | Статус |
|---|---|---|
| Svelte | 5.57.0 | проверено |
| Vite (бандлер Rolldown) | 8.2.2 | проверено |
| `@sveltejs/vite-plugin-svelte` | 7.3.0 | установлено |
| TypeScript | 6.0.3 | установлено |
| `svelte-check` | 4.7.6 | проверено, 0 ошибок |
| `@types/node` | 22.20.1 | установлено |
| `@tsconfig/svelte` | 5.0.8 | установлено |
| `@tauri-apps/api` | 2.11.1 | установлено |
| `@tauri-apps/cli` | 2.11.4 | установлено |
| `@tauri-apps/plugin-dialog`, `-opener` | 2.7.2, 2.5.4 | импортируются окном с 02.09.2026: выбор архива и открытие описи |
| `@tauri-apps/plugin-store`, `-window-state` | 2.4.4, 2.4.1 | установлено, импортов пока нет |

Архитектура – одностраничное приложение, точка входа `src/main.ts` и `src/App.svelte`. SvelteKit исключен (см. 4.2). Peer-требования плагина сборки линии 7 (Svelte не ниже 5.46.4, Vite 8.x, Node 22.12 и выше) выполняются с запасом. Линия `@types/node` обязана совпадать с рантаймом.

Второе назначение того же стека, с окном не связанное: каталог `src/inventory/` – исходники выходного документа описи. Восемь компонентов (`Document.svelte` и семь фрагментов) отрисовываются один раз при сборке через `render()` из `svelte/server`, результат ложится в `cli/src/generated/*.html`, откуда его вкомпилирует крейт `aruna`. В браузере эти компоненты не монтируются никогда: это шаблоны, которым досталась проверка типов, форматер и линтер наравне с остальным фронтендом. Перенос выполнен 01.09.2026 и закрыл третью, последнюю часть целевого состояния – до него разметку описи писал `write!` в Rust. Механизм и его основания – в `docs/FRONTEND-CONTRACT.md`, раздел «The markup».

### 3.3. Тесты

| Компонент | Версия | Статус |
|---|---|---|
| Vitest | 4.1.11 | проверено |
| `@vitest/coverage-v8` | 4.1.11 | проверено |
| `@testing-library/svelte` | 5.4.2 | проверено |
| `@testing-library/jest-dom` | 7.0.1 | проверено |
| `@testing-library/user-event` | 14.6.6 | проверено |
| `jsdom` | 30.0.1 | проверено |
| `@wdio/cli` | 9.31.2 | проверено |
| `@wdio/tauri-service` | 1.3.0 | проверено |
| `@wdio/tauri-plugin` | 1.3.0 | проверено |
| `@wdio/local-runner`, `@wdio/mocha-framework`, `@wdio/spec-reporter` | 9.31.2 | установлено |
| `@types/mocha` | 10.0.10 | установлено |
| `tauri-plugin-wdio-webdriver`, `tauri-plugin-wdio` | 1.3.0 | проверено, optional под фичей `e2e` |

Vitest настроен двумя проектами: `node` для каталога tests и `component` для src. Компонентному проекту задан `resolve.conditions: ['browser']` – без этого Svelte 5 резолвится в серверную сборку и монтирование падает. Покрытие ограничено кодом приложения, иначе в отчет попадают картинки и стили.

Состояние тестовой базы на 04.09.2026: 82 теста в девяти файлах, все зеленые. Разбивка: `App` – 23, `spec-guard` – 20 проверок соответствия этому документу, `filter` – 14, `readme-links` – 7, `inventory-artifact` – 6, `one-frontend-stack` – 6, `font-stack` – 3, `ipc-shape` – 2, `release-version` – 1. E2E – четыре сценария на движке webkit 605.1.15. На 30.08 тестов было 58 в восьми файлах при четырех у `App`: числа выросли, когда окно перестало быть смотровым стеклом над папкой загрузок и получило семь состояний, поток событий и отмену.

`tests/ipc-shape.test.ts` появился 31.08 вместе с расширением `corpus_stats` и держал вместе два ручных объявления одной формы данных – типы Rust и типы окна. С подключением specta 02.09 объявление стало одно, и файл держит уже не форму, а два обещания, которых порожденный тип не дает: что образец `src-tauri/stats-sample.json` – это действительно то, что Rust сериализует, и что пустые места приходят как `null`, а не как отсутствующие поля.

Условие пересмотра, записанное для трех пакетов Testing Library, сработало: с появлением компонентного теста настоящего экрана потребителя получили `@testing-library/svelte` и `jest-dom`. Без потребителя остался только `@testing-library/user-event` – он ждет теста, который разыгрывает ввод, а не проверяет разметку.

Четыре пакета wdio, отмеченные строкой «по факту установки», не приходят вместе с CLI и ставятся отдельно – без них прогон обрывается на поиске исполнителя.

### 3.4. Качество кода

| Компонент | Версия | Статус |
|---|---|---|
| ESLint, `@eslint/js` | 10.9.1, 10.0.1 | проверено |
| `typescript-eslint` | 8.68.0 | проверено |
| `eslint-plugin-svelte`, `svelte-eslint-parser` | 3.23.0, 1.8.1 | проверено |
| `eslint-config-prettier` | 10.1.8 | установлено |
| Prettier, `prettier-plugin-svelte` | 3.9.6, 4.1.1 | проверено |
| Clippy, rustfmt | из toolchain | проверено |
| cargo-audit | 0.22.2 | проверено |
| cargo-nextest | 0.9.143 | проверено |
| cargo-deny | 0.20.2 | проверено |
| cargo-machete | 0.9.2 | проверено |
| cargo-llvm-cov | 0.9.0 | проверено |
| GitHub CLI (`gh`) | 2.97.0 | установлено, авторизовано |

Типизированный слой линта описан в 4.7. Окно поддержки TypeScript у typescript-eslint – не ниже 4.8.4 и ниже 6.1.0; текущая версия внутри окна, и это единственная причина, по которой TypeScript 7 в проект не берется.

Настройки Prettier: без точек с запятой, одинарные кавычки, ширина 100. Исключены dist, node_modules, pnpm-lock.yaml, coverage.

Конфигурация политик одна на весь проект – `deny.toml` в корне; прежние копии в крейтах удалены при объединении в воркспейс. Обе проверки, по ядру и по оболочке, читают ее.

Состояние на 30.08.2026: `cargo deny` по обоим крейтам отвечает «advisories ok, bans ok, licenses ok, sources ok». Замечание `license-not-encountered` на строку `Apache-2.0 WITH LLVM-exception` снято 02.09.2026 – строка удалена при правке файла, как и было записано условием. Тогда же в разрешенный список добавлена `BSD-2-Clause`.

Платформенная фильтрация `targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]` задана там же и отсекает от проверок Linux-ветку дерева. `cargo machete` неиспользуемых зависимостей не находит. Покрытие оболочки на 04.09.2026 – 74,88 % по регионам при восемнадцати тестах.

Замеряя покрытие, обязательно сначала выполнять `cargo llvm-cov clean --workspace`: без очистки инструмент неидемпотентен и на одном и том же дереве дает разные числа.

Инструменты, отвергнутые окончательно, из текста убраны и перечислены одной строкой в 7.4.

Необязательные инструменты разработчика, в критерии приемки не входящие и в манифесты не попадающие: bacon (фоновая проверка), cargo-expand (раскрытие макросов, полезно при разборе макросов Tauri), samply (профилирование).

### 3.5. Оболочка интерфейса – крейт `aruna-desktop`

| Компонент | Версия | Статус |
|---|---|---|
| Tauri | 2.11.5 | проверено |
| tauri-runtime, wry, tao | 2.11.3, 0.55.1, 0.35.3 | проверено |
| `tauri-plugin-dialog` | 2.7.2 | установлено, разрешение выдано |
| `tauri-plugin-opener` | 2.5.4 | установлено, разрешение выдано |
| `tauri-plugin-window-state` | 2.4.1 | установлено, разрешение выдано |
| `tauri-plugin-store` | 2.4.4 | установлено, разрешение выдано |
| Разрешения ядра | `core:default`; `core:window:allow-close` снято 02.09.2026 вместе с кнопкой, которая закрывала окно из страницы – парадная дверь себя не закрывает | настроено |
| Разрешение `opener:allow-open-path` | выдано 02.09.2026 отдельной строкой: набор `opener:default` открывает URL и показывает в Finder, но не открывает файл, а окно открывает собранную опись | настроено |
| `tauri-plugin-log` | 2.9.0 | установлено, только отладка и только без фичи `e2e` |
| tauri-runtime-wry | 2.11.4 | проверено |
| Покрытие | 74,88 % по регионам, восемнадцать тестов (04.09.2026) | проверено |
| Версия | 2.5.2, равна версии ядра, равенство сторожит тест | настроено |
| Профили сборки | выпуск и отладка объявлены (4.9.3) | настроено |
| Версия компилятора | названа в `rust-toolchain.toml`, в манифестах не дублируется (4.9.2) | настроено |
| `log` | 0.4 | установлено |
| `aruna` (ядро) | зависимость по пути внутри воркспейса | проверено |
| `serde` | 1 с фичей derive | проверено |
| `thiserror` | 2 | проверено |
| `serde_json` | 1 | проверено |
| `tempfile` (для тестов) | 3 | проверено |
| `specta`, `tauri-specta`, `specta-typescript` | 2.0.0-rc.25 и 0.0.12 | проверено |
| `tracing`, `tracing-subscriber` | – | ожидает, требует решения по логированию (4.6) |

Манифест содержит только то, что используется. `serde` с фичей `derive` сериализует ответы и ошибки команд, `serde_json` читает счетчики из манифеста пакета, `tempfile` в зависимостях для тестов строит каталог-пакет на каждую ветку проверки. У `tauri` и `tauri-build` набор фич пуст. Правила работы с манифестом – в 4.9.

Прежние редакции утверждали, что `serde` и `serde_json` удалены как неиспользуемые. Это было верно ровно до появления команд и снято.

Конфигурация приложения: идентификатор `com.sergeyssimonov.aruna`, productName `Aruna`, заголовок окна «Aruna», `bundle.targets: ["app", "dmg"]`, `app.withGlobalTauri: true`, `bundle.macOS.signingIdentity: "-"` и `bundle.macOS.minimumSystemVersion: "13.0"`. Заголовок документа в index.html обязан совпадать с заголовком окна – по нему E2E-сервис находит окно.

Имя и идентификатор – продуктовые с 04.09.2026, и это следствие 6.7, а не решение об именах. Прежняя редакция требовала обратного – `aruna-desktop` и `com.sergeyssimonov.aruna.shell`, – потому что распространялся образ из ядра, а два бандла с именем `Aruna.app` на регистронезависимой файловой системе есть один файл: перетаскивание оболочки в `/Applications` заменило бы программу окном без ядра. Второго бандла больше нет, разводить нечего, и суффикс `.shell` снят вместе с ним.

Имя бинарника Cargo при этом остается `aruna-desktop`: на него завязан путь запуска в `frontend/wdio.conf.ts`, и внутри бандла он лежит как `Contents/MacOS/aruna-desktop`. Продуктовое имя носит бандл, а не исполняемый файл.

Подпись ad hoc задается в самой конфигурации – `bundle.macOS.signingIdentity: "-"`, а не вызовом `codesign` после сборки: второй механизм пришлось бы держать в согласии с первым, и молчал бы он ровно тогда, когда разошелся. Минимальная версия системы объявлена там же: умолчание Tauri – macOS 10.13, программа же живет только на macOS 13 (2.1), и собранный 03.09.2026 бандл объявлял 10.13, потому что этого никто не сказал.

Пиктограмма – стилизованная глиняная табличка; мастер-файл `cli/icon.png`, 1024 на 1024 с альфа-каналом, набор порождается штатной командой Tauri, и `icon.icns` несет все десять представлений. У той же команды есть побочное действие, о котором она не предупреждает: на каждый вызов она выкладывает обратно иконки Windows и Microsoft Store и каталоги `android/` и `ios/`, ненужные проекту только под macOS. Они удаляются явными путями после каждого вызова – десять таких файлов сняты коммитом `ab5276b` 03.09.2026.

Окно: метка `main`, заголовок `Aruna`, 800 на 600, `resizable: false`, `center: true`, `fullscreen: false`. Метка внесена в список исключений плагина window-state – размер и положение сознательно не восстанавливаются (6.4).

Версия приложения равна версии ядра – 2.5.2 на 04.09.2026. Прежняя редакция давала оболочке собственную нумерацию и называла условие, при котором это кончится: «имело бы смысл только тогда, когда пользователь получает именно оболочку». Условие выполнено 04.09.2026, и два номера у одной загрузки – на один больше, чем нужно.

Источник истины остается один – `version` в `cli/Cargo.toml`: его читает сборочный контур при сверке тега. `src-tauri/Cargo.toml` обязан повторять это число, и равенство проверяется тестом `spec-guard`, а не глазами при выпуске; цена – выпуск правит два манифеста вместо одного. Номер 0.1.0 из шаблона был поднят до 0.2.0 30.08.2026, когда слой перестал быть каркасом; линия 0.x закончилась вместе с ролью, которую описывала.

Поле `version` из конфигурации убрано 30.08.2026: Tauri берет версию из манифеста крейта, и это остается единственным местом, где она объявлена. Дублирование давало число, ничего не значащее и способное разойтись с манифестом молча. Сценарий E2E соответственно читает ожидаемую версию из манифеста, а не сверяет одну записанную константу с другой.

`tauri-plugin-shell` в проект не берется: право запускать дочерние процессы для задачи «открыть ссылку или файл» избыточно, эту задачу решает opener. Shell добавляется только под доказанную функцию запуска внешних программ.

`specta` с `tauri-specta` подключены 02.09.2026 и закрывают нетипизированную дыру между Rust и TypeScript: без них вызовы `invoke` не проверялись ни компилятором, ни svelte-check, и переименование поля в Rust ломало фронтенд молча. Версии – кандидаты на выпуск (`2.0.0-rc.25` у ядра, `0.0.12` у вспомогательных крейтов), поэтому пин точный и обязательный: у линии кандидатов нет обещаний совместимости даже между соседними номерами.

Побочное следствие подключения – заброшенный крейт `Inflector` в дереве, см. раздел о качестве кода.

Порожденные типы лежат в `frontend/src/bindings.ts` и закоммичены как продукт – та же дисциплина, что у артефактов описи в `cli/src/generated/`: тест оболочки `the_bindings_are_what_these_commands_produce` пересобирает их во временный файл и падает, если байт разошелся. Порождение при старте приложения отвергнуто: отладочный запуск и прогон E2E писали бы в рабочее дерево. Обновление – `cargo test -p aruna-desktop -- --ignored regenerate`. Файл исключен из prettier и eslint: его форма принадлежит экспортеру, и правка нашими инструментами развела бы продукт с источником.

Из ожидающих остается `tracing`: подключение требует решения по единственному контуру логирования (4.6).

### 3.6. Ядро программы – крейт `aruna`

Крейт `cli` версии 2.5.2 – собственно программа и образец, по которому выстроены правила 4.9. Он существовал до появления интерфейса и содержит всю предметную логику; оболочка вызывает ее и не заводит своей.

**Что ядро делает с XML на самом деле, и почему это важно для PDF.** Разборщика XML в ядре нет. Из каждого документа читаются семь полей метаданных из первых 16 КиБ, остальное копируется байт в байт. Дерева элементов не существует. Пять зависимостей – `dirs`, `memchr`, `thiserror`, `ureq`, `zip` – и ни одна из них не разбирает XML. Именно поэтому пакет воспроизводится побайтово и именно поэтому конвертация в PDF начинается не с выбора движка верстки, а с выбора разборщика (3.8). Прежние редакции этого документа исходили из того, что модель документа уже есть; это было неверно.

Из 23 936 документов 210 не являются корректным XML – 0,88 % корпуса. Любой разборщик, который умеет только отказывать, превратит их в дыру.

Манифест ядра объявляет зависимости нижними границами с комментариями о причине, а не точными версиями, и это его собственное осознанное правило: точность держит общий lock-файл, а комментарий у границы объясняет, почему она такая. Требование точных пинов из 5.1 относится к оболочке и фронтенду; к ядру применяется его правило границ.

| Свойство | Значение |
|---|---|
| Зависимости | `dirs` 5, `memchr` 2, `thiserror` 2, `ureq` 2.12 с фичей tls, `zip` 2.3 без фич по умолчанию, кроме deflate |
| Зависимости для тестов | `tempfile` 3 |
| Фичи | `bench` – открывает извлечение полей для замеров, по умолчанию выключена и бинарником не включается |
| Профиль выпуска | `lto = true`, `codegen-units = 1`, `strip = true`, `opt-level = 3`; `panic = "abort"` сознательно не ставится |
| Профиль отладки | `opt-level = 1` |
| Тесты | 17 файлов в каталоге tests плюс модульные тесты в исходниках; 424 теста, покрытие 95,97 % по регионам (04.09.2026). Число всегда записывалось измеренным, а не желаемым, и ходило в обе стороны: 95,73 % на 30.08, 95,69 % при выпуске 2.5.1, 95,63 % на 02.09 – ветки, пришедшие с новыми тестами и с двумя событиями прогресса, были покрыты не целиком; к 04.09 их закрыли тесты отказных ветвей `compare` и швов. Один тест помечен пропускаемым намеренно – он пишет пакет целиком и читает его обратно, что дорого для обычного прогона |
| Аргументы командной строки | `--help` и `--version` печатают имя, версию из манифеста и строку про переменную с путем к архиву, выходя с кодом 0 до всякой работы |
| Воспроизводимость | после полной очистки заново собранный релизный бинарник дает побайтово тот же пакет – 23 938 файлов с той же контрольной суммой |

Три приема из этого манифеста приняты как обязательные для всего проекта. Первый: нижняя граница версии ставится с комментарием о причине – у `zip` это устраненная уязвимость, и в комментарии сказано, что lock-файл, собранный с более низкой границы, может вернуть уязвимую версию при первом же обновлении. Второй: необязательная функциональность выносится в фичу, а в комментарии объясняется, зачем она нужна и почему выключена. Третий: отказ от прерывания процесса при панике обоснован практикой – без разворачивания стека ошибка разбора превращалась в молчаливое падение без сообщения.

Состав исходников: разбор и извлечение полей, работа с архивом и кешем, каталог и загрузка с Zenodo, экспорт и генерация HTML, стили представления, задания и прогресс. Эта часть спецификацией не регулируется по существу – здесь фиксируется только то, что важно для стека в целом: версия, профили, политика зависимостей и наличие собственного набора тестов.

### 3.7. Инструменты агента

Claude Code – версия 2.1.260 на дату редакции. Линия выпускается почти ежедневно, поэтому фиксируется правило, а не номер: держать последнюю опубликованную версию и сверять состав расширений после каждого обновления. Единственное требование инструмента к окружению – Node 22 и старше для установки через npm-пакет; наш Node 22.23.1 удовлетворяет ему с запасом, а сам исполняемый файл Claude Code Node не использует.

Включенные расширения: `rust-analyzer-lsp`, `typescript-lsp`, `commit-commands`, `context7`, `frontend-design`, `svelte`.

Плагин `svelte` 1.0.9 установлен 31.08.2026 из маркетплейса `sveltejs/ai-tools` – это единственное стороннее расширение в составе, и оно допущено осознанно. Основание: маркетплейс принадлежит самому проекту Svelte, hooks плагин не ставит, а его языковой сервер и MCP работают вне процесса модели.

Состав и цена по `claude plugin details`: два навыка, один агент, MCP-сервер и языковой сервер. Постоянный расход – около 354 токенов на сессию; при вызове дороже всего навык лучших практик, около 2,5 тысяч токенов. Это плата за то, что правки Svelte выполняются с актуальной документацией и прогоном автофиксера, а не по памяти модели.

Тип MCP-сервера – удаленный или локальный – требует проверки: у варианта для другой среды по умолчанию удаленный, то есть обращающийся в сеть.

Отключенные, с причинами и условиями возврата:

| Расширение | Причина отключения | Условие возврата |
|---|---|---|
| `github` | MCP-сервер падает при каждом старте без переменной GITHUB_PERSONAL_ACCESS_TOKEN | осознанная настройка токена с минимальными правами |
| `code-review` | несет навык и рассчитан на сценарий пул-реквеста с записью комментария в GitHub; при работе коммитами в main невыполним | переход на процесс с пул-реквестами |
| `security-guidance` | четыре хука, срабатывание после каждого вызова инструмента, отдельное окружение Python | этап кода, обрабатывающего внешние данные |

Основание ревизии – вывод `claude plugin details`: языковые серверы работают вне процесса модели и контекста не расходуют, поэтому их держать выгодно; навыки и MCP-серверы стоят контекста и требуют оправдания.

Сторонние расширения с hooks на каждое сохранение исключены: неаудированный код с доступом к репозиторию противоречит выбранной политике цепочки поставок. Плагин `svelte` – единственное исключение, обоснованное выше.

**Совместная работа расширений – где они пересекаются и кто главнее.** Шесть расширений не независимы, и без явных границ два из них берутся за одно и то же. Разбор по факту состава:

| Пересечение | Кто главнее | Почему |
|---|---|---|
| `frontend-design` и `/design` против `svelte-file-editor` – кто пишет `.svelte` | `/design` рисует макеты и подбирает решение; **написание и правка файлов Svelte – всегда субагент `svelte-file-editor`** (4.12) | `frontend-design` дает вкус и структуру, субагент – корректный код Svelte 5 с автофиксером; смешение дает красивую разметку с ошибками рун |
| `context7` против MCP плагина `svelte` – кто отвечает за документацию | по Svelte и SvelteKit-вопросам – MCP плагина `svelte`; по всему остальному – `context7` | у MCP Svelte документация версионная и с автофиксером, у `context7` – общая |
| `typescript-lsp` против языкового сервера плагина `svelte` – кто разбирает `.svelte.ts` | оба работают вне процесса модели и контекста не расходуют; конфликт возможен только в дублирующейся диагностике | если диагностика двоится, языковой сервер Svelte главнее для файлов с `.svelte` в имени |
| `commit-commands` против правила 5.5 о коммитах по областям | правило 5.5 главнее: команда коммита не вправе брать все изменения разом | `git add -A` без ограничения путями запрещен; команда должна получать пути явно |
| `rust-analyzer-lsp` против `typescript-lsp` | не пересекаются | разные языки |

Правило для всех пересечений одно: расширение – способ выполнить работу, а не источник правил проекта. При конфликте между тем, что предлагает расширение, и тем, что записано здесь и в CLAUDE.md, действует документ.

### 3.8. Контур конвертации в PDF

Слой спроектирован, но **держится в резерве**, и на это есть два предусловия, записанные в `docs/PDF-ACCEPTANCE.md` и проверенные 25.08.2026 с ответом «пока нет».

Первое: у результата должен быть читатель. PDF – продукт только тогда, когда есть просмотрщик, печать или человек, который его ждет. Без этого пакет XML – 663 каталога с описью над ними – сам является конечным состоянием. **Выполнено 02.09.2026 решением владельца:** PDF объявлен направлением, которое делается, а не закрывается; пакет остается опорным результатом, а не концом конвейера.

Второе: разборщик XML не должен стоить сборке больше, чем он дает. У ядра нет разборщика вовсе (3.6), и разборщик стал бы самой крупной зависимостью проекта. Порядок один – добавить кандидата, прогнать весь набор проверок, включая `deny`, `machete` и аудит, и прочитать, что сломалось, до того как написана первая строка конвертации.

**Требования к разборщику, по порядку значимости:** раскрытие сущностей, загрузка DTD и XInclude выключены по умолчанию, доступа к файлам и сети из разбора нет – это уже проверяет `cli/tests/xml_hostile.rs`; потоковый разбор, чтобы документ не держался в памяти дважды (корпус 339,5 МБ текста, самый большой документ 897 КБ); восстановление после ошибок, потому что 210 документов не являются корректным XML; сообщение позиции ошибки строкой и столбцом.

**Решение по разборщику – 4.13.** Кандидат – `quick-xml`; порядок приемки и правило для некорректных документов записаны там.

Движок верстки выбирается отдельно от разборщика и не должен связывать с собой модель документа. Граница, которую держать: байты XML → безопасный разборщик → модель документа → модель верстки → PDF → валидатор. Модель документа не знает о движке, ни один тип PDF не проникает в предметную область; модель верстки читают визуальные проверки, модель документа – семантические, и обе проверяемы без порождения PDF.

Таблица ниже – состав слоя на случай, когда предусловия выполнены. Ни один компонент в манифестах не объявлен.

| Компонент | Версия | Роль | Лицензия | Системные зависимости | Статус |
|---|---|---|---|---|---|
| `typst` | 0.15.x | компилятор и модель документа | Apache-2.0 | нет | ожидает |
| `typst-layout` | 0.15.x | верстка, разбиение на страницы, сноски, таблицы | Apache-2.0 | нет | ожидает |
| `typst-pdf` | 0.15.x | экспорт в PDF; умеет PDF/A и PDF/UA, но PDF/A требованием не является (6.8) | Apache-2.0 | нет | ожидает |
| `krilla` | 0.8.x | запись PDF, приходит транзитивно через `typst-pdf` | MIT или Apache-2.0 | нет | ожидает |
| `krilla-svg` | 0.8.x | векторные вставки, транзитивно | MIT или Apache-2.0 | нет | ожидает |
| `rustybuzz` | транзитивно | shaping клинописи и диакритики | MIT | нет | ожидает |
| `hypher` | транзитивно | переносы по образцам TeX, включая русские | Apache-2.0, требует проверки | нет | ожидает |

Версии на 02.09.2026 по реестру: `typst` 0.15.1, `krilla` 0.8.2; выпуск Typst 0.15.0 – 15.06.2026, значит выдержка набрана с запасом.

**Ни один компонент не тянет системных C-зависимостей и не требует внешнего бинарника.** Это и есть причина, по которой контур вообще проходит в основной стек при ограничениях раздела 2: цепочка от разметки до записи PDF целиком на Rust и собирается под universal binary штатно.

Крейты берутся из одной линии: `typst`, `typst-layout` и `typst-pdf` выпускаются вместе и обязаны стоять одной версией – расхождение внутри линии дает несовместимые внутренние типы на границе верстки и записи. `krilla` приходит транзитивно и своей версией не управляется: ее задает `typst-pdf`.

Совместимость с окружением проверена по трем осям. Минимальная версия компилятора у `krilla` 0.8.2 – 1.92.0, тулчейн 1.97.1. Лицензии Apache-2.0 и MIT входят в разрешенный список `deny.toml`; лицензия `hypher` требует проверки при первой установке. Сборка под universal binary штатная, потому что цепочка целиком на Rust.

**Инструменты проверки результата.** Они не входят в основной стек и допускаются только в тестовом контуре, но ограничение раздела 2 действует и там: Homebrew нет, значит растеризаторы, живущие в нем, недоступны. Решение, не требующее установки ничего: растеризация первой страницы штатным `sips` macOS (`sips -s format png страница.pdf`), извлечение текста крейтом `pdf-extract` в тестах. Оба пути помечены как требующие проверки до первого золотого файла – `sips` с многостраничными PDF работает не для всех страниц, и если этого окажется мало, следующий кандидат – крейт `pdfium-render`, который тянет готовый бинарник PDFium без сборки из исходников. Валидация PDF/A внешним инструментом (veraPDF, Java) – только перед выпуском и только вручную.

Правила отслеживания для этого контура те же, что для остального стека (5.1), с одной особенностью: крейты Typst не дают гарантий стабильности API между минорными версиями, поэтому версия пиннится точно, а обновление сопровождается прогоном golden files, а не только сборкой.

### 3.9. Шрифты

Шрифты перестают быть вопросом оформления, как только появляется PDF: в HTML недостающий знак подставляет система читателя, в PDF подставлять нечего – либо шрифт встроен, либо на его месте пустой прямоугольник навсегда.

| Шрифт | Роль | Лицензия | Системный на macOS 13 |
|---|---|---|---|
| **Noto Serif** | основная гарнитура: кириллица и латиница с диакритикой транслитерации | OFL 1.1 | нет |
| **Noto Sans Cuneiform** | 376 знаков стандартной клинописи корпуса | OFL 1.1 | да |
| **UllikummiA** | один знак, U+100000, встречается 927 раз | требует проверки | нет |
| **Noto Serif Hebrew** | один знак, U+05C3 | OFL 1.1 | нет |
| **STIX Two Math** | шесть знаков: U+24F5–24F8 и U+27E8/27E9 | OFL 1.1 | требует проверки |

Решение от 30.08.2026: **`Arial` из стека убран полностью.** Он коммерческий, встраивание в распространяемый PDF требует лицензии, а нужен был ради единственного знака U+05C3. Его место занимает `Noto Serif Hebrew` – та же семья, что и основная гарнитура, свободная лицензия, встраивание разрешено.

Основная гарнитура – `Noto Serif`. Выбрана не по вкусу: она родственна `Noto Sans Cuneiform`, уже стоящему в стеке, поэтому переход между обычным текстом и клинописью не дает разнобоя в весе и высоте строчных, а покрытие кириллицы и расширенной латинской диакритики у нее полное.

**Основная гарнитура относится только к PDF.** В HTML основной текст по-прежнему берется системным (`system-ui`), веб-шрифт в пакет не встраивается – решение владельца от 30.08.2026. Причина: пакет содержит около 24 000 файлов, и встроенная гарнитура утяжелила бы каждый из них ради вида, который на большинстве машин и так приемлем. Следствие принято сознательно: **два вывода выглядят по-разному**, и это не расхождение, требующее исправления.

Что при этом обязано совпадать – **знаковый состав, а не начертание**. Стек редких знаков (клинопись, U+100000, U+05C3, шесть знаков STIX) един для HTML и PDF, объявлен в общем файле стилей ядра и сторожится тестом. Разойтись вправе только основная гарнитура: в HTML системная, в PDF встроенная.

**Где живут шрифты для PDF и как встраиваются.** В HTML шрифт – имя, которое разрешает система читателя. В PDF шрифт – файл, который читает Typst и подмножество которого krilla встраивает в документ. Отсюда четыре требования:

1. Файлы шрифтов лежат в репозитории, в каталоге ресурсов ядра, а не берутся из системы: системный `Noto Sans Cuneiform` на macOS 13 существует, но его версия и охват на другой машине не гарантированы, а воспроизводимость требует одного и того же файла всегда.
2. Рядом с каждым файлом лежит текст его лицензии. Для OFL это обязательное условие распространения; для `UllikummiA` условия еще не установлены (7.3).
3. Встраивается подмножество, а не файл целиком – krilla делает это по умолчанию и детерминированно; это же держит размер 663 документов в разумных пределах.
4. Порядок обращения к шрифтам в PDF повторяет порядок стека из общего файла стилей: сначала основная гарнитура, затем клинопись, затем редкие знаки. Порядок значим – `Noto Sans Cuneiform` и `UllikummiA` оба рисуют клинопись, и первый должен стоять раньше.

Шесть знаков из 648 не покрыты ни одним шрифтом стека. В HTML это было незаметно, в PDF станет видно сразу – см. открытые позиции. Пока они не закрыты, проверка на отсутствие `.notdef` в выводе (6.8) будет находить ровно их, и это правильно: проверка должна показывать известную дыру, а не молчать о ней.

## 4. Архитектурные решения и их основания

### 4.1. Пакетный менеджер – pnpm

Выбран не по привычке, а по трем механизмам защиты, действующим по умолчанию: карантин новых версий (1440 минут, распространяется на транзитивные зависимости), запрет экзотических источников для транзитивных зависимостей, контроль install-скриптов. У npm 12 из этого есть только третий. Дополнительно pnpm дает жесткую изоляцию node_modules, исключающую обращение к незадекларированным пакетам, и удержание версии самого менеджера через corepack и поле `packageManager`.

### 4.2. Без SvelteKit

Приложение живет внутри Tauri, серверный слой не нужен. Kit навязал бы маршрутизацию, серверные соглашения и собственную сборку поверх десктопного окна – сложность без выигрыша. Запрет проверяемый: в репозитории не должно быть каталога src/routes, файлов +page, +layout, +server, hooks.server, импортов из `$app/*` и вызовов `svelte-kit sync`.

### 4.3. Нижний порог сборки safari16

Дефолт Vite 8 – baseline-widely-available, то есть Safari 16.4 и выше, что для нашей системы слишком высоко. Явный `build.target = 'safari16'` опускает порог до безопасного. CSS понижает Lightning CSS, `build.cssTarget` наследует то же значение.

Важное следствие: в режиме разработки транспиляция минимальна, поэтому фраза «работает в pnpm tauri dev» соответствия порогу не доказывает. Доказательством служат только сборка и прогон E2E собранного приложения.

Проверять порог следует по артефакту, а не по конфигурации: в собранном файле стилей не должно оставаться вложенности, правила `@media` вынесены наружу, символ `&` отсутствует, цвета сведены к шестнадцатеричной записи с альфой. Наличие строки в vite.config.ts доказывает намерение, а не результат.

### 4.4. E2E внутри реального приложения

Единственный способ проверить тот движок, в котором работает пользователь. Альтернативы непригодны: у macOS нет WebDriver для WKWebView; внешний tauri-driver поддерживает только Windows и Linux; Playwright с версии 1.58 (январь 2026) не поддерживает WebKit на macOS 13, а его собственные сборки движка в любом случае не совпадают с системным.

Механика прогона – все пункты обязательны:

1. WebdriverIO с сервисом `@wdio/tauri-service`, в конфигурации указан `driverProvider: 'embedded'`: WebDriver-сервер работает внутри приложения.
2. Rust-плагины wdio регистрируются под `#[cfg(feature = "e2e")]`; для сборки без фичи объявлены пустые заглушки, чтобы цепочка Builder оставалась единой.
3. Разрешения `wdio-webdriver:default` и `wdio:default` лежат в `src-tauri/capabilities-e2e/e2e.json` – вне каталога, который сканирует build.rs. Иначе обычная сборка падает на неизвестных разрешениях. Файл встраивается через `include_str!` и регистрируется в setup вызовом `add_capability` под тем же cfg; трейт `tauri::Manager` импортируется тоже под cfg.
4. Фронтенд-половина `@wdio/tauri-plugin` импортируется в `src/main.ts` под условием `import.meta.env.VITE_E2E`. Без нее недоступен `core.invoke`; в обычной сборке переменная не задана и импорт выбрасывается при сборке.
5. В конфигурации приложения включен `app.withGlobalTauri: true`.
6. Заголовок документа совпадает с заголовком окна.
7. При сборке с фичей `e2e` не регистрируется tauri-plugin-log (см. 4.6).
8. Приложение собирается командой `cargo tauri build --debug --no-bundle --features e2e` с переменной `VITE_E2E=1`. Обычный `cargo build` не годится: он выставляет `cfg(dev)`, и окно грузит devUrl вместо собранного фронтенда, отчего DOM оказывается пустым.

Сценарии проверяют запуск приложения, видимость элементов, взаимодействие и достижимость бекенда. Обращение идет через `browser.tauri.execute`; в колбэк передается контекст с `core`, объекта `app` в нем нет.

Кнопка подтверждения и ее счетчик `data-clicks` сняты 02.09.2026 вместе с закрытием окна из страницы: парадная дверь себя не закрывает, это дело оконной рамы. Что она измеряла – доходит ли изменение состояния до DOM в замороженном WKWebView – теперь измеряется прямее и на большем: сценарий «carries a typed refusal back from the core» зовет `build_corpus` с несуществующим архивом и читает обратно структурный отказ, то есть проверяет и регистрацию команды, и написание двухсловного аргумента, и переход тегированной ошибки через границу. Главная кнопка экрана помечена `data-testid="primary"`; сценарий по-прежнему меняется осознанно, а не мимоходом.

Известный шум сервиса версии 1.3.0, на результат не влияющий и настройками не устранимый: диагностика об отсутствии tauri-driver (при embedded он не нужен) и сообщение об очистке моков после закрытия сессии. Оба – дефекты сервиса.

### 4.5. Отладочные механизмы не попадают в релиз

Гарантия механическая, а не дисциплинарная: оба E2E-плагина объявлены `optional = true` и включаются только фичей `e2e`, фронтенд-половина отсекается отсутствием переменной VITE_E2E. Релизная сборка идет без фичи и без переменной, поэтому этого кода в ней физически нет. Проверяется по составу зависимостей release-профиля.

### 4.6. Логирование

Глобальный логгер в процессе может быть только один. Сейчас его занимает tauri-plugin-log, поэтому при сборке с фичей `e2e` он не регистрируется – крейты wdio ставят свой логгер первыми, и второй вызывает панику при старте. По той же причине подключение tracing требует предварительного решения, каким остается единственный контур логирования.

### 4.7. Контроль корректности и безопасности кода

Три слоя, каждый со своей задачей.

**Компилятор.** `#![forbid(unsafe_code)]` в src/main.rs и src/lib.rs – жесткая граница, нарушение блокирует сборку. Конфликта с макросами Tauri нет.

**Типизированный линт.** Набор `recommendedTypeChecked` применяется двумя отдельными блоками: к `src/**/*.ts` и к `**/*.svelte`. В блоке для компонентов заново объявляется `svelteParser` с вложенным `parser: ts.parser` и `extraFileExtensions: ['.svelte']` – иначе типизированный набор подменяет парсер и ломает разбор компонентов. Тестовая оснастка и конфигурационные файлы остаются на базовом наборе: они не входят в tsconfig.app.json, а проектная служба типов требует принадлежности файла проекту.

Смысл слоя в том, что он ловит невидимое синтаксическому линту: потерянные промисы (критично для вызовов Tauri – невыжданный `invoke` теряет и результат, и исключение), промисы в обработчиках, ожидающих синхронную функцию, `await` над не-thenable, небезопасные обращения к `any`.

**Аудит зависимостей.** `cargo audit` по уязвимостям Rust, `cargo deny` по политикам, `pnpm audit` по уязвимостям npm-пакетов.

Уязвимость, для которой исправления не существует, не замалчивается и не выдается за закрытую, а принимается с записанным основанием. На 02.09.2026 таких во фронтенде нет: `pnpm audit` не находит известных уязвимостей – три, что были открыты до вечера того дня, закрыты переопределениями (5.2).

В Rust-контуре в отчете аудита восемнадцать предупреждений класса unmaintained и unsound. Семнадцать относятся к Linux-ветке дерева (gtk-rs, glib) и к build-time крейтам (proc-macro-error, unic-*); на macOS этот код не компилируется и в рантайм не попадает. Восемнадцатое появилось 02.09.2026 вместе с `specta`: заброшенный крейт `Inflector` приходит через `specta-macros`, работает на этапе раскрытия макросов и в готовое приложение не попадает – тот же класс, что и остальные. Подавлять их запрещено: они остаются индикатором на случай появления настоящей уязвимости.

### 4.8. Контур проектирования интерфейса

Разрешенный путь – команда `/design` в Claude Code (исследовательский предпросмотр, доступна с 17.08.2026). Она рисует несколько редактируемых макетов, из них выбирается один, при необходимости правится на холсте и в той же сессии переписывается в компоненты проекта. Стек значения не имеет: холст рисуется средствами артефактов, код пишется существующими компонентами Svelte.

`/design-sync` и любая загрузка библиотеки компонентов в среду Claude Design запрещены: та среда собирает прототипы из React-функций, подставить туда компоненты Svelte нечем, а обхода через SvelteKit нет, поскольку Kit исключен. Запрет действует до появления отрисовщика, не привязанного к React. Оговорка о достоверности: назначение обеих команд подтверждено публикациями июня и августа 2026 года, а утверждение о невозможности загрузить библиотеку Svelte взято из внешнего разбора и не проверялось по первоисточнику. На решение это не влияет – запрет принят по принципу минимального риска.

Ограничение, которое принимается как данность: макет на холсте – визуальный контракт, а не готовый компонент, поэтому сложный экран всегда делается в два шага. Реализация идет в существующие компоненты; заводить параллельные Button, Input и подобные запрещено.

Контрольная проверка контура: один прогон `/design` с последующей реализацией на существующем экране не создает ни одного файла Kit и не вызывает `/design-sync`.

### 4.9. Правила Rust-части

Раздел описывает требования к коду и манифесту src-tauri. Крейт cli живет по своим правилам и здесь не регулируется, но служит образцом: в нем нижние границы версий проставлены с комментарием о причине, включая ссылку на устраненную уязвимость, а необязательная функциональность вынесена в фичу с пояснением, зачем она нужна.

**4.9.1. Воркспейс и имена пакетов.** Крейты объединены в воркспейс 30.08.2026: корневой манифест перечисляет участников `cli` и `src-tauri`, lock-файл один на оба, каталог сборки общий и лежит в корне репозитория.

Имя `aruna` принадлежит ядру, оболочка называется `aruna-desktop`, ее библиотека – `aruna_desktop_lib`. Имя пакета определяет имя исполняемого файла, а на него завязан путь к приложению в конфигурации wdio, поэтому любое переименование делается одним согласованным шагом – манифест, вызов библиотеки в main.rs, путь в wdio.conf.ts – и подтверждается прогоном E2E.

Объединение подтянуло двадцать три транзитивные зависимости ядра к версиям, которые уже стояли у оболочки. Проверено по существу, а не по рассуждению: релизный бинарник ядра пересобран и прогнан на настоящем корпусе, полученный пакет из 23 938 файлов побайтово совпал с прежним. Это и есть критерий для подобных изменений – совпадение результата работы программы, а не совпадение номеров версий.

Следствие для команд: `cargo` без указания пакета работает по всему воркспейсу, поэтому `cargo nextest run --locked` прогоняет оба крейта разом – 442 теста на 04.09.2026. Для работы с одним крейтом используется ключ `-p aruna` или `-p aruna-desktop`. В сборочном контуре команды идут через `--manifest-path` и остаются рабочими без правок.

**4.9.2. Версия компилятора – одно место.** Версия названа один раз, в `rust-toolchain.toml`. Поле `rust-version` в манифестах не заводится ни в одном крейте – решение владельца от 28.08.2026, зафиксированное здесь и в CLAUDE.md.

Основание: два механизма решают разные задачи, и дублирование создает второй источник истины. `rust-toolchain.toml` определяет, каким компилятором проект собирается на этой машине, и это ровно то, что нужно приложению, которое собирается в одном месте и не публикуется как библиотека. Поле `rust-version` объявляет минимальную поддерживаемую версию для чужих сборок – обещание, которого проект не дает и не проверяет.

Плата за это решение названа прямо: резолвер Cargo ничем не ограничен и вправе выбирать зависимости, требующие более нового компилятора. Практических отказов это пока не вызывало; если вызовет – сборка упадет на конкретном крейте, и тогда вопрос вернется с измеренным основанием, а не с предположением.

Предыдущая редакция утверждала обратное – что поле объявлено и что оно сдерживает рост списка обновляемых крейтов. Первое устарело, второе было выводом без измерения; оба утверждения сняты.

**4.9.3. Профили сборки.** Профили объявлены в корневом манифесте воркспейса и только там: выпуск – `lto = true`, `codegen-units = 1`, `strip = true`, `opt-level = 3`; отладка – `opt-level = 1`. В воркспейсе секции профилей из манифестов участников игнорируются, поэтому дублировать их в крейтах нельзя – значения молча перестанут действовать.

Выпуск дает меньший и быстрее работающий бинарник ценой более долгой сборки – размен, приемлемый для артефакта, который собирается редко. Отладочный профиль с первым уровнем оптимизации ускоряет работу приложения при разработке, почти не удлиняя сборку.

`panic = "abort"` не ставится сознательно, и причина записана комментарием прямо в манифесте: паника обязана печатать место, где она произошла. В консольном крейте отказ от разворачивания стека однажды превратил ошибку разбора в молчаливое падение без сообщения для пользователя.

**4.9.4. Политика зависимостей.** Каждый новый крейт добавляется с обоснованием: что именно он решает и почему это не решается средствами стандартной библиотеки или Tauri. Фичи включаются минимально необходимые, `default-features = false` предпочтителен там, где это не ломает сборку. Нижняя граница версии ставится осознанно: если версия выбрана из-за устраненной уязвимости или нужного поведения, причина записывается комментарием в манифесте. Неиспользуемые зависимости удаляются – это проверяет cargo-machete. Дубли версий одного крейта в дереве отслеживаются командой `cargo tree --duplicates`: единичные дубли из транзитивных веток допустимы, рост их числа – повод разбираться.

**4.9.5. Обработка ошибок.** Ошибки типизированные, через `thiserror`; строковые ошибки и `Box<dyn Error>` в коде приложения не используются. `unwrap` и `expect` запрещены везде, кроме точки входа, где падение при невозможности запустить приложение осмысленно. Ошибка, возвращаемая командой во фронтенд, обязана быть сериализуемой и не должна раскрывать внутренние пути и детали окружения.

**4.9.6. Команды и границы с фронтендом.** Команда – тонкая обертка над функцией предметной области из крейта `aruna`, а не место для логики. Оболочка не повторяет то, что уже написано в ядре, и не заводит собственных путей к данным корпуса: одно поведение – одна реализация, вызываемая и консольной формой, и окном.

Команд четыре, и все написаны по этому правилу. `corpus_location` спрашивает у `aruna::paths`, где пакет. `corpus_stats` считает, что в нем, по манифесту пакета. `build_corpus`, добавленная 02.09.2026, вызывает тот же `app::build_corpus`, что и консольный вход, – вне главного потока, с событиями `build-progress` о стадии и доле выполненного. `cancel_build` останавливает запущенную сборку через флаг отмены, который ядро проверяет между документами. Типы всех четырех порождаются из Rust в `frontend/src/bindings.ts` средствами `specta`.

Окно, которое их вызывает, имеет семь состояний: что есть на диске, предложение собрать с Zenodo или из выбранного архива, стадия и доля во время сборки, отчет о прогоне со ссылкой на опись. Состояния и команды – договор, записанный в `docs/FRONTEND-CONTRACT.md`; замена экрана – решение, а не плановая перерисовка.

Первая команда, `corpus_location`, служит образцом: каталог загрузок отдает ядро, имя пакета и имя описи объявлены там же константами, оболочка только складывает путь и проверяет наличие. Ошибка типизирована через `thiserror` и сериализуется строкой – по правилу 4.9.5.

`corpus_stats` следует тому же правилу: путь приходит параметром, счетчики читаются из манифеста пакета, обход каталога остается запасным путем на случай испорченного манифеста, а поле источника в ответе честно говорит, откуда взяты числа.

Отдельно о разрешениях. Команда ядра Tauri требует своего разрешения, даже когда вызывается из собственного кода приложения: закрытие окна по кнопке потребовало добавить `core:window:allow-close` в capability, до этого вызов молча отклонялся. Отсюда правило для интерфейса: результат обращения к бекенду обрабатывается явно, отказ показывается на экране, а не проглатывается – иначе несуществующее разрешение выглядит как неработающая кнопка. Аргументы и результат сериализуемые. Общее состояние передается через механизм состояния Tauri, а не через глобальные переменные. Длительная работа – разбор архива, обход корпуса – не выполняется в потоке событий: она уходит в отдельный поток или в блокирующую задачу исполнителя, а о ходе выполнения фронтенд узнает событиями. С подключением `specta` и `tauri-specta` соответствие типов между Rust и TypeScript проверяется машинно: типы команд порождаются из объявлений Rust, и переименование поля перестает быть тихой поломкой фронтенда. Ручная сверка сигнатур, действовавшая до 02.09.2026, отменяется.

**4.9.7. Пути и данные.** Пути к данным приложения берутся у Tauri через его каталоги, жестко прописанные пути и обращения к домашнему каталогу пользователя запрещены. Файлы корпуса и кеш располагаются в каталогах приложения, а не рядом с исполняемым файлом.

**4.9.8. Тесты Rust.** В `aruna-desktop` восемнадцать тестов на 02.09.2026 (было пять на 30.08). Один закрепляет требование 4.5 – сборка без фичи `e2e` не регистрирует WebDriver; восемь разыгрывают ветки команды, читающей счетчики: манифест пакета на месте, манифест испорчен, пакета нет вовсе. Остальные пришли с командой сборки: свежесть порожденных типов, отказ второй сборки поверх первой, доход отмены до флага идущего прогона, отсутствие путей файловой системы в событиях прогресса. Это правильный образец: требование, записанное словами, проверяется кодом, а каждая ветка отказа имеет свой тест.

Требования к дальнейшим тестам: модульные рядом с кодом, интеграционные в каталоге tests, команды проверяются на макете приложения без открытия окна средствами тестового модуля Tauri. Запуск – через nextest, покрытие – через llvm-cov. Флаг `--no-tests=pass` в наборе 6.1 остается как страховка на случай временного отсутствия тестов в одном из крейтов, но необходимостью быть перестал.

Ядро имеет собственный набор из семнадцати файлов интеграционных тестов; при работе над ним действует тот же порядок запуска. Строка про пятнадцать файлов возвращалась в редакциях 12, 14, 15 и 24 и противоречила §3.6 того же документа; редакция 25 исправила ее в источнике.

**4.9.9. Воспроизводимость сборки.** Релизная сборка выполняется с флагом `--locked`: он запрещает молчаливое изменение Cargo.lock во время сборки. Расхождение lock-файла и манифеста в этот момент – ошибка, а не повод обновиться.

### 4.10. Движок верстки для PDF – Typst

Решение владельца от 30.08.2026: верстка PDF выполняется Typst, встроенным в ядро крейтами, а не внешним процессом. Альтернативы отвергнуты окончательно и в документе больше не обсуждаются (перечень – 7.4).

Что дало решение: детерминированный вывод по построению, отключается только дата документа; вход строится значениями через API, а не генерацией разметки с экранированием; PDF/A и PDF/UA-1 с тегами из коробки; ни одной системной зависимости и ни одного внешнего бинарника; типизированные ошибки вместо разбора текстового лога.

Что решение стоит, и документ этого не скрывает: **микротипографика Typst беднее эталонной** – растяжения шрифта нет, протрузия ограничена, микроюстировка работает кернингом после разбиения строк и по умолчанию выключена. На корпусе транслитераций с короткими строками, таблицами и списками это почти не проявляется; на длинной выключенной прозе разница была бы видна. Компенсация – включить микроюстировку явно и настроить ее по одному развороту настоящего текста. Если однажды появится длинная проза, вопрос вернется с измеренным основанием: два разворота рядом, а не рассуждение.

### 4.11. Точка ветвления на HTML и PDF

Прежние редакции утверждали, что ветвление идет от существующей модели документа. Модели документа нет: ядро читает семь полей и копирует остальное байт в байт (3.6). Ветвление в его первом виде невозможно, и это исправлено.

Что переиспользуется на самом деле: разбор метаданных, каталог CTH, порядок документов, кеш, загрузка, механика пакета – все, что работает над файлами как над байтами. Что предстоит построить: модель документа поверх безопасного разборщика (3.8), и только от нее пойдет модель верстки и PDF. HTML-опись при этом остается прежней: она строится над байтами и в модели документа не нуждается.

Единым остается **стек редких знаков** – клинопись, приватная область, еврейская пунктуация, издательские пометы: он объявлен в общем файле стилей ядра и сторожится тестом. Основная гарнитура едина не обязана быть и не является: в HTML системная, в PDF встроенная `Noto Serif` (3.9).

### 4.12. Правки Svelte делегируются субагенту

Файлы `.svelte`, `.svelte.ts` и `.svelte.js` правит субагент `svelte-file-editor` из плагина `svelte`, а не основной агент. Решение владельца от 31.08.2026.

Основание не в удобстве. У субагента собственное окно контекста: он подтягивает документацию по нужному разделу Svelte 5 и прогоняет автофиксер, не расходуя контекст основного разговора. Правка компонента перестает зависеть от того, что модель помнит о рунах, и начинает зависеть от того, что написано в документации на сегодня.

Делегирование должно происходить само по описанию агента; если не происходит, субагент называется в запросе явно.

Что это **не** отменяет: результат правки проходит обычный набор проверок (6.1). Субагент меняет способ написания кода, а не приемку – `svelte-check`, линт с типизированным слоем и тесты остаются единственным основанием считать правку хорошей.

### 4.13. Разборщик XML для ядра

Без разборщика содержимое документа для ядра – байты, и любая конвертация, кроме побайтовой, невозможна. Разборщик становится самой крупной зависимостью проекта, поэтому выбор записан с требованиями, кандидатом, планом приемки и запасным вариантом.

**Требования, унаследованные от `docs/PDF-ACCEPTANCE.md`:** безопасность по умолчанию (никакого раскрытия внешних сущностей, загрузки DTD, XInclude, доступа к файлам и сети из разбора); потоковый разбор; переживание некорректного входа – 210 документов из 23 936 не являются корректным XML; позиция ошибки строкой и столбцом. Добавленные этим документом: чистый Rust без системных зависимостей (раздел 2), лицензия из разрешенного списка `deny.toml`, минимальный набор фич, прохождение всего набора проверок до первой строки конвертации.

**Кандидат – `quick-xml`.** Потоковый разборщик на чистом Rust, лицензия MIT, версия 0.41.0 на дату редакции, фичи по умолчанию отсутствуют – то есть в минимальной конфигурации он приносит ровно разбор и ничего сверх. Измерено пробой: цена вхождения в дерево нулевая – ноль новых крейтов, единственная зависимость уже стоит в ядре. Внешние сущности и DTD он не загружает по устройству: раскрытие сущностей происходит только тогда и так, как попросит вызывающий код, а значит правило «ничего не раскрывать» реализуется отсутствием вызова, а не настройкой, которую можно забыть. Позиция ошибки отдается смещением в байтах; строка и столбец вычисляются из него одним проходом – это дешевле, чем кажется, потому что самый большой документ 897 КБ.

Проверено по дереву 02.09.2026: `quick-xml` 0.41.0 уже стоит в общем lock-файле – его тянет `plist` со стороны оболочки. Это подтверждает и версию, и измеренную цену: крейт в дереве уже есть, объявление в манифесте ядра добавит ребро, а не пакет.

Что у кандидата требует проверки прогоном, а не чтением документации: точный перечень настроек мягкости (несовпадающие закрывающие теги, незакрытые элементы), поведение на голом амперсанде и одиночном знаке «меньше» в тексте, и то, какие именно из 210 некорректных документов он переживает.

Проба первого шага измерила цену вхождения, но не пригодность: разборщик может ничего не стоить дереву и все равно не подойти, если 210 дефектов окажутся разнородными и не будут чиниться детерминированно. Решение принимается по итогу второго шага, а не первого.

**Правило для некорректных документов – чинить в нормализации, а не прощать в разборщике.** Ядро уже нормализует документы и записывает результат побайтово. Правильное место для 210 случаев – там же: каждый класс дефекта классифицируется, для него пишется детерминированное исправление на уровне байтов, факт исправления записывается в манифест пакета. Тогда разборщик остается строгим, воспроизводимость сохраняется, а список того, что было починено, становится частью данных, а не тайной разборщика. Мягкий режим разборщика допускается только для дефектов, у которых нет однозначного исправления, и каждый такой случай записывается отдельно.

**Порядок приемки, в этой последовательности:**

1. Пробная ветка: добавить `quick-xml` без фич в манифест ядра и прогнать весь набор – `deny`, `machete`, аудит, clippy. Что сломается, читается до строки кода. Ветка не сливается. **Выполнено 02.09.2026, результат – ноль новых крейтов в дереве:** единственная зависимость `quick-xml` это `memchr`, который в ядре уже стоит. Лицензии, запреты и источники прошли, аудит не изменился, сборка ядра чистая. Проба снята, ветка удалена.
2. Классификация: пример в ядре, который прогоняет разборщик по всему корпусу и группирует отказы по видам. Ожидаемый результат – 210 документов в нескольких классах; если классов много и они разнородны, кандидат пересматривается.
3. Исправления в нормализации по классам, с записью в манифест и тестами на каждый класс.
4. Только после этого – модель документа и все, что выше нее.

**Запасной вариант – `xml5ever`.** Разборщик по правилам XML5 с восстановлением после ошибок, чистый Rust, из проекта Servo. Берется, если классификация покажет, что дефекты не чинятся детерминированно на уровне байтов. Цена: дерево строится целиком, позиции ошибок беднее, сопровождение менее активное – все три требуют проверки на момент выбора.

**Что отвергнуто:** привязки к libxml2 – системная C-зависимость; `roxmltree` – строгий, некорректный вход не переживает вовсе, хотя как модель документа поверх токенов пригоден и может вернуться на следующем шаге.

## 5. Правила работы

### 5.1. Версии и обновления

Все версии зафиксированы точно. Значение `latest` в package.json запрещено.

Обновление допускается при трех условиях одновременно: релиз-ноты прочитаны; версии не меньше семи дней; есть причина – затрагивающий проект дефект или безопасность – либо идет плановая ревизия перед новым этапом. Обновления «на всякий случай» запрещены. После обновления обязателен набор проверок 6.1, а при затрагивании контура E2E – и 6.2.

Транзитивные зависимости вручную не поднимаются: выбор сделан резолвером по объявленным диапазонам и зафиксирован lock-файлом, а ручная правка обходит и то, и другое. У Cargo к этому добавляется ограничение по полю rust-version: ручное вмешательство делает заявленный минимальный компилятор ложным.

Порядок ревизии: `pnpm outdated` в frontend, в корне и с ключом -g; `cargo update --dry-run --verbose` в src-tauri; `cargo search` по установленным cargo-инструментам; `claude --version`. Текущий список наблюдения и план – в 7.2.

### 5.2. Lock-файлы и цепочка поставок

В Git фиксируются `frontend/pnpm-lock.yaml`, корневой `pnpm-lock.yaml` и корневой `Cargo.lock`. Последний один на воркспейс: прежние `cli/Cargo.lock` и `src-tauri/Cargo.lock` удалены при объединении. Удалять lock-файлы без необходимости нельзя.

Защита pnpm (4.1) не отключается. Решения по install-скриптам записаны явно в `frontend/pnpm-workspace.yaml`: edgedriver, esbuild и geckodriver – false.

**Переопределения версий транзитивных зависимостей** живут там же, в секции `overrides`, и заводятся только по одной причине – уязвимость, которую прямая зависимость не может исправить сама. Каждое переопределение имеет условие снятия и подтверждается прогоном E2E, а не фактом успешной установки: подмена версии внутри чужой библиотеки проверяется тем путем, который эту библиотеку использует.

На 02.09.2026 действуют три. Два из них – `serialize-javascript` и `deepmerge-ts` – были записаны здесь раньше, чем заведены: до вечера 02.09.2026 в `frontend/pnpm-workspace.yaml` стоял только `@puppeteer/browsers`, и `pnpm audit` сообщал о трех уязвимостях по ветке wdio. Заведены решением владельца тем же вечером, точными версиями по правилу 5.1, а не каретками:

| Пакет | Переопределено на | Причина | Условие снятия |
|---|---|---|---|
| `serialize-javascript` | `7.0.5` | mocha 10 и 11 объявляют `^6.0.2`, а 6.0.2 задета двумя уязвимостями: внедрение кода через флаги регулярного выражения (исправлено в 7.0.3) и исчерпание процессора (исправлено в 7.0.5). Обновление wdio не лечит – ограничение идет от mocha | mocha поднимет зависимость сама; в ее трекере просьба открыта |
| `deepmerge-ts` | `8.0.2` | исчерпание стека при слиянии рекурсивных структур, исправлено в восьмой линии; `@wdio/config` и `@wdio/utils` даже в 9.31.5 объявляют седьмую | wdio перейдет на восьмую линию |
| `@puppeteer/browsers` | `3.2.1` | обход пути по символической ссылке в `extract-zip` 2.0.1, исправленной версии которого не существует – автор ее не выпустил. Третья линия загрузчика от `extract-zip` отказалась вовсе и распаковывает `modern-tar`, поэтому переопределение убирает уязвимый пакет из дерева целиком, а не подменяет его версию | wdio перейдет на третью линию загрузчика |

`@puppeteer/browsers` подтвержден прогоном 01.09.2026 по записи владельца: уязвимый пакет ушел из дерева целиком. Два других подтверждены прогоном 02.09.2026 по правилу выше – не фактом установки, а тем путем, который эти библиотеки использует: `pnpm install` (семь пакетов пришло, три ушло), `pnpm check` на 271 файле, линт, форматер, 80 модульных тестов и E2E – четыре сценария на webkit 605.1.15, все зеленые. После них `pnpm audit` сообщает об отсутствии известных уязвимостей. Разрешенные версии: `serialize-javascript` 7.0.5 от 25.03.2026 – низшая, закрывающая обе записи, чтобы отклонение от объявленного mocha диапазона было наименьшим; `deepmerge-ts` 8.0.2 от 21.08.2026 – она же старшая опубликованная. Обе старше семи дней, как требует 5.1.

Третий случай стоит запомнить как прием: **когда у сломанного пакета нет исправления, стоит смотреть на его потребителя.** Замены не было ни у `extract-zip`, ни в его линии, но библиотека уровнем выше от него отказалась – и переопределение потребителя решило то, что не решалось переопределением самого пакета. Незаполненные решения дают ошибку установки. esbuild работает без своего postinstall, поскольку платформенный бинарник приходит отдельным пакетом через optionalDependencies; удалению esbuild не подлежит – его тянут сервис и плагин wdio, загрузчик конфигураций и сам Vite.

### 5.3. Граница запрета npm

npm запрещен как менеджер этого проекта: никаких `npm install`, `npm ci`, `npm update`, никаких package-lock.json и npm-shrinkwrap.json, разовые запуски – `pnpm dlx`, а не npx.

Запрет про управление зависимостями проекта, а не про машину: npm остается частью Node-инсталляции и никуда не девается. Инструмент, нужный на машине и не входящий ни в один манифест – языковой сервер, форматер редактора, – ставится глобально через `pnpm add -g`; это тот же pnpm и никакого отношения к запрету не имеет.

### 5.4. Границы безопасности приложения

Каждый зарегистрированный плагин Tauri требует разрешений в capabilities. Типовой сценарий отказа: плагин подключен, а команды падают из-за невыданного разрешения. Часть команд уже покрыта разрешениями ядра (`core:default`).

Правки в tauri.conf.json, capabilities и манифестах не вносятся молча – они меняют границы того, что приложение вправе делать.

Политика содержимого задана: `default-src 'self'` и `connect-src` с каналом IPC приложения. Это запрещает загрузку постороннего кода и обращения наружу, оставляя работать только мост к бекенду. Ослабление политики – отдельное решение с записанной причиной.

До первого релиза остается решить вопрос подписи приложения.

### 5.5. Коммиты

Коммиты разделяются по областям: фронтенд вместе с src-tauri, крейт cli, документация. Смешивать области в одном коммите нельзя. `git add -A` без ограничения путями не применяется.

## 6. Проверки и приемка

### 6.1. Перед коммитом

Из каталога frontend: `pnpm check`, `pnpm lint`, `pnpm format:check`, `pnpm test:unit`, `pnpm run audit`.

Последняя команда – не сам `pnpm audit`, а скрипт `frontend/scripts/audit.sh` с ограничением в тридцать секунд. Он различает три исхода по коду возврата: уязвимостей нет – проходит молча; сервис рекомендаций не ответил – печатает предупреждение и возвращает ноль, потому что причина не в проекте; уязвимости найдены или команда отказала по существу – возвращает код как есть и останавливает набор.

**Это осознанное ослабление, и оно записано именно так.** Молчание внешнего сервиса не отменяет уязвимость, найденную вчера. Но проверка, зависящая от чужой конечной точки, не вправе блокировать работу бесконечным ожиданием – 04.09.2026 именно это и произошло. В предрелизном наборе действует `pnpm run audit:strict` без ограничения и без обхода: там задержка уместна, а пропустить проверку нельзя.

`pnpm audit` добавлен 01.09.2026 и закрывает дыру, существовавшую с самого начала: для Rust уязвимости проверял `cargo audit`, для фронтенда не проверял никто, и четыре уязвимости жили в дереве незамеченными, пока сигнал не пришел со стороны. Симметрия восстановлена – у каждого контура своя проверка.

Из каталога src-tauri: `cargo fmt --check`, `cargo clippy --locked --all-targets --all-features -- -D warnings`, `cargo nextest run --locked --no-tests=pass`, `cargo deny check licenses bans sources`.

Последняя команда добавлена 02.09.2026 по итогам разбора. `cargo deny` делает четыре проверки, и в сеть за базой рекомендаций ходит только одна – `advisories`. Три остальные читают lock-файл локально: измерено, 0,72 секунды на нашем дереве. Прежде весь `deny` стоял только в предрелизном наборе из-за медленной четверти, и это была ошибка разделения: лицензия новой зависимости обязана ловиться до пуша.

Как это проявилось: `specta` притащила крейт `Inflector` под BSD-2-Clause, лицензии не было в разрешенном списке, и отказ пришел из конвейера уже после отправки изменений. Лицензия добавлена с обоснованием – двухпунктная BSD это трехпунктная без оговорки о неодобрении, с MIT совместима. Флаг `--locked` обязателен во всех командах Cargo, включая локальные: он запрещает молчаливое изменение lock-файла и делает результат проверки тем же, что получит выпуск (4.9.9).

При правках консольного крейта – тот же набор из каталога cli. Крейты объединены в воркспейс (4.9.1), но проверяется затронутый; если затронуты оба, набор прогоняется дважды.

Флаг `--no-tests=pass` перестал быть необходимостью – тест в `aruna-desktop` есть (4.9.8) – и остается страховкой на случай, когда в затронутом крейте тестов временно нет.

Здесь есть ловушка, стоящая внимания. Флаг `--all-features` у Clippy включает фичу `e2e`, и это правильно: ветка должна проверяться. Но тот же флаг, добавленный к nextest, включил бы `e2e` и тем самым выключил единственный тест – он проверяет поведение сборки без фичи и под фичей не компилируется. Поэтому nextest запускается без `--all-features`, и это не оплошность, а условие того, что тест вообще выполняется.

E2E в этот набор не входит: цикл со сборкой приложения длинный.

### 6.2. Перед релизом

Набор 6.1 полностью, плюс `pnpm test:e2e`.

Дополнительно по Rust: `cargo audit`, полный `cargo deny check` со всеми четырьмя разделами, `cargo machete`, `cargo llvm-cov nextest`. По фронтенду – `pnpm run audit:strict`.

Сборка: `pnpm build` в корне.

Контроль артефакта: релиз собран без фичи `e2e` и без переменной VITE_E2E; отсутствие обоих E2E-плагинов подтверждено по составу зависимостей release-профиля.

### 6.3. Команды

| Команда | Назначение | Признак успеха |
|---|---|---|
| `pnpm dev` | фронтенд в режиме разработки | открывается без ошибок |
| `pnpm tauri dev` | окно приложения в режиме разработки | окно открывается и работает |
| `pnpm build` (frontend) | сборка фронтенда | сборка без ошибок |
| `pnpm build` (корень) | universal-сборка приложения | артефакт создан |
| `pnpm check` | svelte-check плюс tsc по конфигурациям node и e2e | нет ошибок типов |
| `pnpm lint` | ESLint с типизированным слоем | нет ошибок |
| `pnpm format:check` | Prettier без записи | нет замечаний |
| `pnpm test:unit` | Vitest, оба проекта | все тесты прошли |
| `pnpm test:coverage` | Vitest с покрытием | отчет сформирован |
| `pnpm test:e2e` | сборка с фичей e2e и прогон wdio | сценарии прошли, код 0, движок webkit на macos |
| `cargo fmt --check` | форматирование Rust | нет замечаний |
| `cargo clippy --locked --all-targets --all-features -- -D warnings` | линт Rust | нет предупреждений |
| `cargo audit` | уязвимости зависимостей Rust | уязвимостей нет |
| `pnpm run audit` | уязвимости npm-пакетов, с ограничением по времени | уязвимостей нет либо внешний отказ, названный явно |
| `pnpm run audit:strict` | то же, без ограничения; только перед выпуском | уязвимостей нет |
| `gh run list`, `gh release view` | состояние сборок и выпусков | сборки зеленые, выпуск на месте |
| `cargo deny check` | политики и лицензии | проверка пройдена |
| `cargo machete` | неиспользуемые зависимости | лишних нет |
| `cargo nextest run --locked --no-tests=pass` | тесты Rust | прошли либо тестов нет |
| `cargo llvm-cov nextest` | покрытие Rust | отчет сформирован |

### 6.4. Ручная проверка

Приложение запускается; окно открывается с заголовком «Aruna» в центре экрана и в размере из конфигурации – в том числе после запуска, в котором окно двигали; размер мышью не меняется; интерфейс отображается, белого экрана и зависаний нет; смена оформления системы доходит до окна без перезапуска; сохраненные данные переживают перезапуск; приложение не падает при обычных действиях. Диалоги и внешние ссылки проверяются, когда появится команда, которая их вызывает (7.3).

Требование о восстановлении размера и положения окна снято 30.08.2026 и заменено обратным. Причина: окно показывает короткое сообщение фиксированного объема, и восстановленный крошечный размер прятал кнопку за нижним краем – случай, который уже происходил. Метка окна внесена в список исключений плагина window-state, поэтому плагин остается подключенным, но на это окно не действует.

### 6.5. Что считается нарушением

Версия компонента из раздела 3 отличается от зафиксированной без записанной причины.

Отсутствует `build.target = 'safari16'`; отсутствует `withGlobalTauri`; заголовок окна не совпадает с заголовком документа.

Не используется svelte-check; не настроены ESLint, Prettier, Vitest, покрытие; типизированный слой снят с кода приложения.

E2E-контур не настроен; E2E-плагины подключены не как optional под фичей `e2e`; их разрешения лежат в сканируемом каталоге capabilities; импорт `@wdio/tauri-plugin` не привязан к VITE_E2E; любой E2E-плагин обнаружен в релизной сборке.

Не проходят Clippy или rustfmt; не настроены cargo-audit, cargo-deny, cargo-machete, cargo-nextest, cargo-llvm-cov. Отсутствие cargo-geiger нарушением не является.

В собственных крейтах отсутствует `#![forbid(unsafe_code)]`; плагины Tauri подключены без разрешений.

Lock-файлы не зафиксированы; в package.json встречается `latest`; в репозитории обнаружены package-lock.json, npm-shrinkwrap.json или следы установки через npm.

В зависимостях обнаружены Playwright или cargo-wizard.

Появились артефакты SvelteKit (перечень в 4.2); использован `/design-sync`.

Ошибки проверок игнорируются без причины.

### 6.6. Если проверка не проходит

Прочитать текст ошибки; определить, какой инструмент ее выдал; найти файл и строку; устранить причину, а не сообщение; повторить проверку. Если проблема осталась – проверить версию зависимости и lock-файл, в том числе на предмет потери поддержки macOS 13. Если решение не найдено – зафиксировать проблему отдельно и найти согласованный обходной путь.

Отключать линтер, тест или проверку безопасности без записанной причины запрещено.

### 6.7. Контур выпуска

Выпуск не делается вручную и не описывается устно – он выполняется сборочным контуром в репозитории, и этот контур такая же часть стека, как локальные проверки.

**Сборочный процесс.** Файл `.github/workflows/release-dmg.yml`. На каждую отправку – четыре задания: три на Linux (тесты и clippy ядра; svelte-check, vitest и сборка фронтенда; полный разбор всех 23 936 рукописей по настоящему архиву) и одно на macOS – форматирование, clippy и тесты оболочки, потому что крейт Tauri на Linux требует GTK и WebKitGTK, и проверка там ничего не сказала бы о единственной платформе выпуска. С 31.08.2026 задание на macOS удерживает выпуск. Основание изменилось 04.09.2026 и стало прямым: раньше образ собирался из ядра и кода оболочки не содержал, а красная оболочка все равно резала выпуск как единственный потребитель публичного контракта ядра снаружи; теперь оболочка и есть то, что распространяется.

**Образ собирается Tauri, а не скриптом.** Команда – `pnpm build` в корне, то есть `tauri build --target universal-apple-darwin`; фронтенд собирается до упаковки через `beforeBuildCommand`, поэтому заданию выпуска нужны Node и pnpm наравне с Rust. Прежний путь – `cli/scripts/make_release.sh` поверх `cli/build_app.sh` – из контура выпуска убран.

Почему три задания из четырех идут на Linux: ни одно из них ничего не выигрывает от macOS, а минуты там стоят вдесятеро. Ядро – чистый Rust на пяти зависимостях, ни одной системной; полный разбор корпуса работает над байтами; фронтенд – Node и Vite. Компилятор во всех случаях тот же, названный в `rust-toolchain.toml`.

Единственное место, где разница платформ могла бы соврать, закрыто в коде. APFS по умолчанию нечувствителен к регистру, ext4 чувствителен: `KBo 1.xml` и `KBo 1.XML` на Маке один файл, а на Линуксе два. Если бы пакет раскладывался по точным именам, Linux-задание было бы зеленым, а на Маке два документа заняли бы одно место. Раскладка идет по свернутому ключу – `collision_key` в `cli/src/export/mod.rs`, – и пакет, собранный на Линуксе, побайтово тот же, что собранный здесь. После этого проверка на Linux не приближение, а законный представитель единственной выпускаемой платформы.

Universal-образ и DMG собираются только при выпуске или при ручном запуске – минуты macOS стоят в десять раз дороже, а артефакт нужен только выпуску. Перед тегом сборку запускают вручную: поломка в упаковке больше не всплывает на отправке, которая ее внесла.

**Опорные выпуски.** Файл `.github/reference-release.json` хранит записи о выпусках с коммитом и контрольной суммой артефакта; сборочный контур сверяет их отдельным шагом. Смысл механизма: опубликованный выпуск обязан оставаться тем же, каким его получили читатели. Соответствующие теги защищены правилом репозитория от удаления, перемещения и переписывания истории – проверено попыткой удаления, отказ получен.

**Обновление зависимостей.** В репозитории есть конфигурация автоматических предложений об обновлении. Ее предложения проходят через правило 5.1 наравне с ручными: наличие предложения причиной для обновления не является.

**Инструмент выпуска.** GitHub CLI (`gh`) используется при выпуске – чтение состояния сборок, публикация и перевыпуск, загрузка опубликованного артефакта для сверки. Без него выпуск не делается.

**Опубликованных выпусков три, и только три.** Два – опорные: v1.0.5, самый старый заведомо хороший, и v1.0.9, закрывающий линию 1.x. Третий – действующий, **v2.5.2**, единственный, который проект просит устанавливать. Выпуски v2.1.0–v2.4.0 отозваны 30.08.2026 вместе с тегами и образами; v2.5.0 отозван 01.09.2026, когда его место занял v2.5.1; сам v2.5.1 отозван 04.09.2026, когда его место занял v2.5.2 – первый выпуск, в котором распространяется приложение с окном. Правило: опорный выпуск не перерезается, работа после него получает свой номер, а список в три позиции держится отзывом заменяемого. Все три записаны в `.github/reference-release.json` с коммитом и контрольной суммой; контур падает, если тег исчезает или переезжает.

Действующий выпуск подписан ad hoc, без Developer ID, и не нотаризован – у проекта нет платного членства Apple. Защита системы откажет читателю при первом открытии; README описывает проверку контрольной суммы до того, как обходить отказ. Условие из 5.4 не выполнено – см. 7.3.

### 6.8. Проверки конвертации в PDF

Раздел вступает в силу вместе с первым сгенерированным PDF; до тех пор он описывает требования, а не действующий набор.

**Обязательные в пре-коммитном наборе, когда контур появится:**

| Проверка | Что ловит |
|---|---|
| Две сборки одного документа, сравнение байтов | недетерминированный вывод |
| Golden files по структуре: число страниц, размеры, метаданные, список встроенных шрифтов | регрессии верстки и потерю шрифта |
| Отсутствие `.notdef` в выводе | молчаливую подмену шрифта и непокрытые знаки |
| Сверка извлеченного текста с исходным на выборке | потерю или искажение содержимого – главное требование бережности |
| Malformed вход: обрезанный, с битой кодировкой | падения на входе |

**Семантическая полнота проверяется не извлеченным текстом, а манифестом.** Для каждого документа из модели документа порождается ожидаемое содержание в порядке следования – разделы, идентификатор CTH, сиглум фрагмента, серия, заголовки по уровням, тело без усечений и повторов, примечания у своих якорей, метаданные, – и извлечение сверяется с ним, а не с текстом как с пятном. Специальные знаки без подмен: ни знаков вопроса, ни пустых квадратов, ни потерянной диакритики.

**Визуальный опорный набор** выбирается по тому, что ломает верстку, а не по типичности: самый короткий документ (807 байт), типичный (5,6 КБ), самый большой (897 КБ), самая глубокая вложенность (80 уровней), самое тяжелое смешанное содержание, больше всего примечаний, редкие знаки и приватная область, самые длинные идентификаторы, документ с таблицей, документ, у которого одна строка уходит на новую страницу. Сравнение никогда не побайтовое между окружениями: версии движка, шрифтов и системы двигают пиксели, не двигая смысла – либо окружение закреплено, либо сравнение с допуском поверх структурных проверок.

**Перед выпуском, вне пре-коммитного набора:** полный корпус с проверкой текста и глифов; растеризация опорного набора; ручной осмотр десятка документов с клинописью.

**PDF/A требованием не является** и не вводится без отдельного решения – так записано в `docs/PDF-ACCEPTANCE.md`, и этот документ следует ему. Что Typst умеет PDF/A – возможность, а не обязанность.

**Структурная валидность** проверяется внешними инструментами (`qpdf`, `pdfinfo`, `pdffonts`, второй независимый читатель вроде MuPDF), **ни один из которых не установлен**, и их отсутствие никогда не читается как пройденная проверка. Установка – решение того, кто запускает; при ограничении раздела 2 это отдельная задача (8.1).

**Пакетное поведение** – атомарная запись, ни одного поврежденного файла под окончательным именем, продолжение после отказа одного документа, стабильный порядок, ограниченная память и параллельность – переиспользует механику пакета из ядра: `Staging` для недостроенного и `Replaced` для замененного.

Полный корпус в пре-коммитном наборе не гоняется: 24 000 документов – это часы, и место такому прогону там же, где проверке воспроизводимости пакета.

**Чего эти проверки не докажут, и это записано намеренно.** Растеризация ловит сдвиги, но не уродливую верстку. Извлечение текста подтверждает наличие знаков, но не то, что клинопись отрисована правильным начертанием. Оба вопроса решает только глаз.

**Ограничение по памяти.** Весь корпус в один PDF – это гигабайты в памяти. Документ собирается на группу CTH: 663 файла вместо одного.

### 6.9. Критерий приемки окружения

Разделы 2 и 3 соответствуют заявленным статусам; правила разделов 4 и 5 выполняются; наборы 6.1 и 6.2 проходят; E2E зеленый. На дату редакции выполнено. Позиции со статусом «ожидает» относятся к этапу разработки кода и приемке окружения не препятствуют.

## 7. Приложения

### 7.1. Журнал отклонений реальной сборки

Записан, чтобы при повторном развертывании не проходить те же ловушки заново.

**Каркас и зависимости.** `pnpm init` вместо шаблона create-vite порождает конфликт полей packageManager и devEngines и не дает структуры проекта. `pnpm add -E` не переписывает диапазоны у пакетов, уже присутствующих в манифесте – семь версий пришлось проставлять вручную. В pnpm 11 поле «pnpm» в package.json не читается, настройки живут в pnpm-workspace.yaml, а автогенерированная заготовка этого файла содержит плейсхолдеры и требует заполнения.

**Тесты.** В jsdom-окружении Svelte 5 резолвится в серверную сборку – лечится `resolve.conditions: ['browser']`. Опция `hot` у vite-plugin-svelte 7 отсутствует. В покрытие попадают бинарные ассеты и стили, если не сузить маску до кода. Пустой набор Rust-тестов дает у nextest код возврата 4.

**Tauri.** Имя крейта из шаблона – app; переименование затрагивает три места (имя пакета, имя lib, вызов в main.rs), а BSD sed на macOS не поддерживает границы слова. Файл capability, ссылающийся на плагины под фичей, ломает сборку без фичи, поскольку build.rs проверяет разрешения на этапе компиляции. Два глобальных логгера в одном процессе несовместимы. `cargo build` дает `cfg(dev)` и загрузку devUrl – для E2E нужна сборка через Tauri CLI. Мост wdio требует одновременно импорта фронтенд-плагина, `withGlobalTauri` и корректного контекста в тесте. Сервис ищет окно по заголовку документа, а не по конфигурации.

**Линт.** Набор `recommendedTypeChecked` подменяет парсер компонентов, поэтому для `.svelte` парсер объявляется заново; файлы вне tsconfig.app.json в типизированный слой не берутся.

**Внешние отказы.** 04.09.2026 конечная точка быстрой проверки уязвимостей у сервиса рекомендаций перестала отвечать – ноль ответа за двадцать секунд при полностью работоспособной площадке и при живом реестре пакетов, отвечающем за треть секунды. Массовая конечная точка того же сервиса работала. `pnpm audit` вис без ограничения времени, и с ним вставал весь пре-коммитный набор.

Проверено на четырех версиях менеджера – 11.22, 11.23, 11.24, 11.25 – и на чужом дереве из одной зависимости: виснет везде. Это сняло подозрение с версии 11.25.0, которую я обвинил первой и напрасно. Урок: прежде чем откатывать обновление, надо доказать, что виновато именно оно, а не совпавшая по времени внешняя причина.

**Ловушка `timeout`.** В macOS этой программы нет. Первая попытка ограничить время дала команду, которая печатала «сервис не ответил», хотя не выполнялась вовсе – хуже, чем висящая, потому что выглядела работающей. Ограничение по времени на macOS делается через `perl -e 'alarm N; exec @ARGV'`, а исход обязан различаться по коду возврата, иначе внешний отказ и найденная уязвимость выглядят одинаково.

**Процедурные ловушки.** Команда отмены, действующая на дерево целиком, не годится для снятия одной пробной правки: `git stash -u` с последующим `drop` 02.09.2026 забрал и выбросил все незакоммиченные изменения – десять файлов, включая правки документов и утренние пины манифеста. Восстановлено из недостижимого коммита, который stash оставляет до сборки мусора. Правило: отменять пробу только явными путями через `git checkout -- <пути>`; команды, действующие на дерево целиком, в блоки не попадают.

Резервная копия с именем без метки времени затирается при повторном прогоне блока – так были потеряны исходные Cargo.toml и eslint.config.js. Терминальные вставки визуально съедают пустые строки, поэтому судить о структуре файла по выводу нельзя, нужна нумерация строк.

### 7.2. План обновления и список наблюдения

Ревизия от 02.09.2026 – сплошная, по шести контурам, плюс проверка совместимости делом: парность версий Tauri между крейтами и npm-пакетами сошлась до патча по всем четырем плагинам; три крейта `specta` согласованы и запинены точным равенством; peer-расхождение одно и прежнее; дубли в дереве Rust объяснимы и через границу нашего кода не проходят. Все cargo-инструменты, tauri-cli, `typst` 0.15.1, `krilla` 0.8.2, `specta` и `tauri-specta` rc.25 – последние опубликованные; Claude Code 2.1.258.

Сплошная ревизия с тех пор не повторялась, и редакция 25 ее не проводила: 04.09.2026 сверены точечно установленный номер Claude Code (2.1.260, 3.7) и версии из плана установки 8.1 – по реестрам npm и crates.io. Про остальные пять контуров на 04.09.2026 не известно ничего нового, и датировать их этим днем было бы неправдой.

Тогда же двенадцать зависимостей оболочки, объявленных диапазонами со времен шаблона, приведены к точному равенству без единого изменения lock-файла – наследие, которое правило запрещало, а проверка не сторожила; сторожить будет `spec-guard` (8.2).

**Запрещены постоянно, независимо от выхода новых версий.** `@types/node` – линия обязана совпадать с рантаймом Node 22; двадцать шестая линия (на дату ревизии 26.4.0) описывает API, которых в 22 нет, отчего проверка типов пройдет, а исполнение упадет. TypeScript 7 – у него нет программного API, на котором держится typescript-eslint; разблокируется не выходом версии, а появлением API в 7.1 и его поддержкой в линт-контуре. То же касается глобального typescript, приходящего с языковым сервером.

**Пачка установлена 30.08.2026.** Четыре пакета подняты одной командой, набор 6.1 прогнан целиком и зеленый: svelte-check 0 ошибок, ESLint без замечаний, Prettier без замечаний, 53 теста в семи файлах на тот час – к вечеру их 58 в восьми, – rustfmt и Clippy чисты, тест настольного крейта проходит.

| Пакет | Переход | Публикация | Основание |
|---|---|---|---|
| ESLint | 10.9.0 → 10.9.1 | 24.08.2026 | патч чинит ложные срабатывания правила no-loss-of-precision, внесенные в 10.9.0, то есть регрессия присутствовала в нашей версии |
| `typescript-eslint` | 8.67.0 → 8.68.0 | 24.08.2026 | исправления no-floating-promises и no-empty-object-type; новых правил нет |
| Svelte | 5.56.9 → 5.56.10 | 20.08.2026 | шесть исправлений без новых возможностей; ключевое для настольного приложения – освобождение ссылки на последнее событие после всплытия, которое иначе удерживает поддерево целевого элемента; плюс два перехода от квадратичного времени к линейному |
| Svelte | 5.56.10 → 5.57.0 | 04.09.2026 | минорная, опубликована 28.08.2026; установлена отдельным шагом после семидневной выдержки, с прогоном набора 6.1 и E2E |
| pnpm | 11.22.0 → 11.25.0 | 04.09.2026 | последняя в линии 11, опубликована 29.08.2026; поле `packageManager` и corepack, отдельным шагом. Выдержка правила 5.1 не была выдержана – взято на сутки раньше срока решением владельца, отступление записано в 7.2 |
| `@testing-library/user-event` | 14.6.5 → 14.6.6 | 22.08.2026 | pointerType синтетических событий по умолчанию становится пустой строкой вместо строки «undefined» |

Обновление Svelte дополнительно подтверждено прогоном E2E: собранное приложение открылось в системном WKWebView, оба сценария прошли, код возврата 0. Проверки типов и модульные тесты работают с исходниками, поэтому для изменений компилятора шаблонов подтверждением служит только прогон собранного приложения.

**Svelte 5.57.0 установлен 04.09.2026.** Минорная версия, поэтому порядок был строгим: релиз-ноты прочитаны целиком, обновление отдельным шагом, после него полный набор и E2E на собранном приложении – все зеленое, четыре сценария прошли.

Ценность обновления для проекта названа прямо, чтобы не приписывать ему несуществующей пользы: **ни одно из четырех нововведений версии нас не касается**. Методы `getOrInsert` у `SvelteMap` – реактивных коллекций такого рода в окне нет; третья функция `has` у `createContext` – контексты не применяются; атрибут `defaultValue` у `select` – форм нет; типы серверного рендеринга в `svelte/server` – SSR нет по решению об отказе от SvelteKit. Взято ради накопленных между 5.56.10 и 5.57.0 исправлений серверного рендеринга, стилей и реактивности и ради того, чтобы не копить отставание: через месяц пришлось бы разбирать минор вместе с патчами разом.

**Контур конвертации в PDF – особый порядок отслеживания.** Крейты Typst не дают гарантий стабильности API между минорными версиями, поэтому версия пиннится точно, а обновление сопровождается прогоном golden files по структуре PDF и сравнением растров на выборке, а не только сборкой. Отслеживаются `typst`, `typst-layout`, `typst-pdf` одной линией – они выпускаются вместе, – и отдельно `krilla`, потому что он живет своим циклом и приходит транзитивно. На дату редакции: Typst 0.15.0 от 15.06.2026, krilla 0.8.2.

**pnpm 11.25.0 установлен 04.09.2026** – на сутки раньше семидневного срока, решением владельца. Отступление записано здесь, а не умолчано: правило требовало ждать до 05.09 около 14:20.

Обстоятельство, оправдывающее отступление задним числом: версия была проверена всеми проверками, кроме зависящей от внешнего сервиса, и прошла их – установка не тронула дерево, три переопределения версий уцелели, запрет сборочных скриптов действует, 82 модульных теста и четыре сценария E2E зеленые. Единственная проверка, которая не прошла, оказалась сломанной не версией, а внешней причиной (7.1).

Причины обновления, из заметок трех выпусков линии:

Из 11.25.0 – **проверка `allowBuilds` теперь применяется и когда подготовленная зависимость из git берется из общего хранилища**; до этого запрет на выполнение сборочных скриптов обходился переиспользованием готового результата. Оттуда же настройка `audit.ignorePrune` для `pnpm audit --fix`.

Из 11.24.0 – `pnpm install --frozen-lockfile` перестал падать, когда в lock-файле записана закрепленная версия менеджера. Флаг используется в трех заданиях сборочного контура; в нашем lock-файле такой записи на 04.09.2026 не было, но она появится при смене версии менеджера, то есть при этом самом обновлении.

Из 11.23.0 – `pnpm config get` и `pnpm config list` показывают действующие настройки под документированными именами, включая реестры, слитые из всех источников. Этой диагностикой мы в тот же день и воспользовались, разбирая отказ аудита.

После установки сторож версии менеджера в `spec-guard` обновлен на 11.25.0 – тест поймал смену, как и должен был.

**Мажорная линия 12.x** – 12.3.1 опубликована 03.09.2026. Не берется: к менеджеру привязаны три переопределения, политика сборочных скриптов и поле в манифесте, и мажорная версия вправе изменить поведение любого из трех. Рассматривается отдельной задачей не раньше 10.09.2026, с чтением заметок о ломающих изменениях.

**Отложено сознательно.**

Контур wdio (все пакеты до 9.31.5, кроме `spec-reporter`, оставшегося на 9.31.2) – самая хрупкая часть окружения, она работает, а подъем версий не устраняет известное расхождение peer-зависимости: сервис Tauri тянет собственную, более старую линию `@wdio/globals`. Трогать имеет смысл при появлении конкретной поломки или при обновлении самого сервиса.

Проверено 01.09.2026 по конкретному поводу – уязвимостям в дереве: обновление до 9.31.5 не лечит ни одну из них. Свежая линия по-прежнему объявляет `deepmerge-ts` седьмой линии и по-прежнему тянет `@puppeteer/browsers`. Безопасность как основание для обновления здесь не работает, и это измерено, а не предположено.

Транзитивные крейты обоих Rust-крейтов не поднимаются: текущие lock-файлы описывают ровно тот набор, на котором прошли сборка, clippy, аудит, тесты и E2E, а причины у обновления нет – `cargo audit` уязвимостей не находит. В консольном крейте таких крейтов около двадцати, в настольном около десяти; длина списка сама по себе основанием не является.

Отдельно – три прямые зависимости консольного крейта, где доступен переход через мажорную границу: `dirs` 5 → 6, `ureq` 2.12 → 3, `zip` 2.4 → 8. Это не обновления, а миграции: меняется интерфейс библиотеки, и каждая требует чтения руководства по переходу, правки кода и полного прогона тестов крейта. Рассматриваются по одной и только при наличии причины – например, если в текущей линии перестанут выпускать исправления безопасности.

Отдельное наблюдение: число крейтов, обновляемых внутри semver, растет от ревизии к ревизии. Причина не измерялась – связывать этот рост с отсутствием поля `rust-version` было бы догадкой, а не выводом. На решения он не влияет: список отложенных обновлений ведется по существу каждого крейта, а не по его длине.

Ближайшие точки: 07.09.2026 – плагины Tauri парно и `typescript-eslint` одной пачкой; 09.09.2026 – `@testing-library/user-event` 14.6.7; переход на мажорную линию pnpm 12 – не раньше 10.09.2026 и с отдельным разбором. Svelte 5.57.0 и pnpm 11.25.0 установлены 04.09.2026, каждый отдельным шагом.

### 7.3. Открытые позиции

| Позиция | Что требуется | Раздел |
|---|---|---|
| ~~Что распространяется пользователю~~ | **закрыто 04.09.2026:** распространяется приложение с окном. `productName` `Aruna`, идентификатор `com.sergeyssimonov.aruna`, версия равна версии ядра, образ собирает Tauri. Открытым из этой позиции остался один хвост – судьба `cli/build_app.sh` и номера сборки из числа коммитов, вынесенная владельцу | 3.5, 6.7 |
| Подпись приложения | действующий v2.5.2 подписан ad hoc и не нотаризован. Условие достижимое: подпись Developer ID обязательна для первого выпуска, который предлагается устанавливать читателю без обхода защиты системы; до нее README описывает проверку контрольной суммы | 5.4 |
| Второе предусловие PDF | разборщик XML, прошедший весь набор проверок до первой строки конвертации. Кандидат выбран (4.13), приемка не начата. Первое предусловие выполнено 02.09.2026 решением владельца | 3.8, 4.13 |
| Классификация некорректных документов | 210 из 23 936 не являются корректным XML; до классификации по видам дефектов неизвестно, чинятся ли они детерминированно в нормализации | 4.13 |
| Проверка покрытия нового шрифтового стека | прогнать пример `font_coverage` с Noto Serif, Noto Serif Hebrew и остальным стеком: 642 знака из 648 должны остаться покрытыми, а шесть непокрытых – теми же самыми. Пока не сделано, стек считается предложенным, а не принятым | 3.9 |
| Источник и лицензия `UllikummiA` | шрифт не системный и обязан лежать в репозитории, иначе 927 вхождений U+100000 станут пустыми прямоугольниками. Условия распространения проверить и записать | 3.9 |
| Шесть непокрытых знаков | в HTML их скрывала системная подстановка, в PDF они станут пустыми прямоугольниками. Решить: шрифт, изображение или явная пометка | 3.9 |
| Решение по единственному контуру логирования | выбрать tracing либо tauri-plugin-log | 4.6 |
| Проверка плагинов Tauri в работе | `dialog` и `opener` получили потребителей 02.09.2026 – выбор архива и «Открыть опись», – и проверяются в работе. Остается `store`: команды, которая обращалась бы к нему, по-прежнему нет | 6.4 |
| Компонент `rust-src` | не установлен. Нужен только для перехода в исходники стандартной библиотеки из языкового сервера. Либо поставить одной командой, либо оставить как есть – решение за владельцем | 3.1 |
| `@testing-library/user-event` | без потребителя; два других пакета Testing Library его получили с появлением компонентного теста экрана. Условие пересмотра – тест, разыгрывающий ввод | 3.3 |
| Два пакета JS-половин плагинов Tauri | `store` и `window-state` в манифесте есть, импортов нет; пересматриваются вместе с первым кодом, который вызовет соответствующий плагин. `dialog` и `opener` вышли из этой строки 02.09.2026 | 3.2 |

Закрыто в редакции 9, по сверке от 30.08.2026: фиксация изменений в Git (дерево зафиксировано пятью коммитами по областям и отправлено в origin), ручная проверка окна (выполнена 29.08; переделана 30.08 под новое требование 6.4 – центрирование и фиксированный размер вместо восстановления), обработка `--help` и `--version` (реализована), парная строка про политику содержимого и подпись (политика задана, подпись выделена в отдельную позицию), отсутствие описания контура выпуска (раздел 6.7), отсутствие `cli/Cargo.lock` в перечне lock-файлов, отсутствие флага `--locked` в командах Rust, неназванные пакеты JS-половин плагинов и `gh`.

Закрыто в редакции 7: совпадение имен пакетов, объявление версии компилятора одним местом, профили сборки, стратегия паники, политика содержимого, отсутствие тестов Rust.

### 7.4. Отключенное, неиспользуемое и отвергнутое

**Отключенное и неиспользуемое** – с условием возврата или удаления у каждой строки; строка без условия считается дефектом документа.

| Что | Состояние | Условие пересмотра |
|---|---|---|
| Расширение `github` | отключено | настройка токена с минимальными правами |
| Расширение `code-review` | отключено | переход на процесс с пул-реквестами |
| Расширение `security-guidance` | отключено | этап кода, обрабатывающего внешние данные |
| Сборочные скрипты `edgedriver`, `geckodriver`, `esbuild` | запрещены в pnpm-workspace.yaml | не пересматриваются: драйверы проекту не нужны, бинарник esbuild приходит отдельным пакетом |
| `@testing-library/user-event` | установлен, потребителя нет | тест, разыгрывающий ввод |
| `@tauri-apps/plugin-store`, `-window-state` | установлены, импортов нет | первый код, вызывающий соответствующий плагин. `dialog` и `opener` вышли из этой строки 02.09.2026 |
| Тест целостности пакета в ядре | помечен пропускаемым | запускается отдельно при полной проверке записи и чтения пакета |
| `rust-src` | не установлен | переход в исходники стандартной библиотеки из языкового сервера |
| `tracing` | не подключен | решение по единственному контуру логирования (4.6) |
| Три переопределения версий в pnpm-workspace.yaml | действуют | условия снятия в 5.2 |

**Отвергнутое окончательно.** Перечислено одной строкой на каждое, чтобы вопрос не поднимался заново; из остального текста документа эти имена убраны.

| Что | Причина, одним предложением |
|---|---|
| Playwright | с 1.58 его WebKit не поддерживает macOS 13, а собственные сборки движка не совпадают с системным |
| SvelteKit | приложение живет внутри Tauri, серверный слой не нужен; проверяемый запрет в 4.2 |
| `tauri-plugin-shell` | право запускать процессы избыточно для открытия ссылок; заменен `opener` |
| `/design-sync` и загрузка библиотеки в Claude Design | среда собирает прототипы из React, компоненты Svelte не встают |
| `Arial` | коммерческий шрифт, встраивание в PDF требует лицензии; заменен `Noto Serif Hebrew` |
| LuaLaTeX, XSL-FO, WeasyPrint, браузерные движки для PDF | внешний процесс, системные зависимости или недетерминированность; решение в 4.10 |
| Собственный layout engine | профессиональная верстка пишется годами, а не подключается крейтом |
| cargo-geiger | не собирается без OpenSSL dev, которого в окружении нет; контроль unsafe держит компилятор (4.7) |
| cargo-wizard, cargo-outdated, sccache | запрещен; дублирует имеющееся; лишний слой кеширования сборки |
| Сторонние расширения Claude Code с hooks | неаудированный код с правом исполнять команды при каждом сохранении |
| Веб-шрифт в HTML-пакете | утяжелил бы каждый из 24 000 файлов; два вида приняты сознательно (3.9) |

### 7.5. Слабые места, названные прямо

Аудит 30.08.2026. Список не ставит задач и не требует действий – он существует, чтобы никто не принял зеленые проверки за доказательство того, чего они не проверяют.

**Оболочка покрыта настолько, насколько это возможно без окна.** Восемнадцать тестов и 74,88 % регионов на 04.09.2026 (было пять и 70,83 % на 30.08): разыграны ветки команды, читающей счетчики, правила состояния сборки и требование о чистоте релизной сборки. Непокрытым остается то, что требует настоящего окна, – регистрация плагинов, точка входа и тело самой команды сборки, которое без Tauri не позвать.

**E2E проверяет четыре сценария** (02.09.2026): окно открывается и показывает интерфейс, бекенд достижим, отказ ядра доезжает до страницы типизированным, остановка несуществующей сборки принимается без падения. Отказная ветка, которой прежде не было ни одной, разыграна дважды, но это по-прежнему не проверка интерфейса: отсутствие пакета, выбор архива и полный ход сборки с событиями прогресса на сценариях не разыграны.

**Из трех плагинов без потребителя остался один.** `dialog` и `opener` получили вызовы 02.09.2026 – выбор архива и открытие описи – и проверены в работе. Хранилище `store` подключено, разрешение выдано, а команды, которая к нему обращается, по-прежнему нет: соответствующий пункт приемки остается невыполнимым, и это записано честно, а не закрыто галочкой.

**Типы между Rust и TypeScript связаны с 02.09.2026.** Слабое место снято: `specta` с `tauri-specta` порождают `frontend/src/bindings.ts` из объявлений команд, и переименование поля в Rust ловится `pnpm check`. Прежняя формулировка этого раздела – «держится на внимательности» – относилась к состоянию до подключения и здесь сохранена только как след: раздел называет слабые места прямо, и снятое место называется снятым.

**Приложение подписано только ad hoc.** Действующий v2.5.2 без Developer ID и без нотаризации; защита системы откажет читателю при первом открытии, и README честно описывает, как это обойти после проверки контрольной суммы.

**У ядра нет разборщика XML.** Семь полей из первых 16 КиБ и побайтовое копирование – это и сила пакета, и предел. Кандидат выбран, приемка не пройдена; до ее итога все, что записано о PDF, остается проектом.

**Пользователь получает окно с 04.09.2026.** Прежняя формулировка этого раздела – «пользователь пока получает не окно, образ собирается из ядра, двойной клик отрабатывает молча» – снята: образ собирает Tauri, приложение открывается из Finder и показывает окно.

Слабое место при этом не исчезло, а переехало: **отказ защиты системы стал дороже.** Пока в бандле лежала консольная программа, заблокированный запуск был одним молчанием среди прочих – из Finder она молчала и так. Теперь у приложения есть окно, и человек, открывший его двойным кликом, ждет окна: отказ – это все, что он получит. README поэтому предупреждает об этом до первого запуска, а не сноской.

**Часть проверок держится на одной машине.** Воспроизводимость пакета, ручной осмотр окна, прогон E2E – все это выполняется здесь. Сборочный контур повторяет набор проверок, но не воспроизводимость и не осмотр.

### 7.6. Соответствие прежней нумерации

Редакции 2–4 ссылались на разделы девятой части. Соответствие для внешних ссылок, включая CLAUDE.md:

| Прежний номер | Текущий раздел |
|---|---|
| 9.4.3 (правило обновления) | 5.1 |
| 9.6 (механика E2E) | 4.4 |
| 9.6.5 (запрет для релиза) | 4.5 |
| 9.8.0 (разрешения плагинов) | 5.4 |
| 9.12 (набор перед коммитом) | 6.1 |
| 9.13 (набор перед релизом) | 6.2 |
| 9.16 (нарушения) | 6.5 |
| 9.20 (журнал отклонений) | 7.1 |
| 9.22 (план обновления) | 7.2 |
| 9.23 (контур проектирования) | 4.8 |

## 8. Компоненты к установке и настройке

Единый перечень того, чего в системе или в манифестах еще нет, но что предусмотрено документом. Строка уходит из этого раздела в раздел 3, когда компонент установлен, настроен и проверен прогоном – не раньше. Состояние на 02.09.2026.

### 8.1. Требуют установки

| Компонент | Что именно ставится | Куда | Версия к установке | Условие и порядок |
|---|---|---|---|---|
| `quick-xml` | разборщик XML, без фич | `cli/Cargo.toml` | `=0.42.0` – опубликована 22.08.2026 (crates.io, замер 04.09.2026), классификацию делать на ней | пробная ветка пройдена 02.09.2026 на 0.41.0 – ноль новых крейтов; остается классификация 210 некорректных документов (4.13), и только после нее крейт входит в основную ветку |
| `typst`, `typst-layout`, `typst-pdf` | три крейта одной линии, зависимости ядра | `cli/Cargo.toml` | `=0.15.1` для всех трех | после разборщика и модели документа; сначала три эксперимента из 8.3 |
| Внешние проверки PDF | `qpdf`, `pdfinfo`, `pdffonts`, второй читатель – ни один не установлен, и при запрете Homebrew путь установки требует отдельного решения | тестовое окружение | требует проверки | вместе с первым PDF; их отсутствие никогда не читается как пройденная проверка |
| `Noto Serif` | файлы начертаний Regular, Italic, Bold, Bold Italic | каталог ресурсов ядра, с текстом OFL рядом | из официального репозитория Noto, требует проверки версии | до первого PDF; после – прогон `font_coverage` |
| `Noto Serif Hebrew` | файл Regular | там же, с текстом OFL | то же | вместе с предыдущим; заменяет `Arial` в общем файле стилей ядра |
| `UllikummiA` | файл шрифта | там же, с условиями распространения | как есть | сначала выяснить условия распространения (7.3); без файла 927 вхождений U+100000 станут пустыми прямоугольниками |
| `STIX Two Math` | файл Regular | там же, с текстом OFL | последняя стабильная | на macOS 13 может быть системным – проверить и все равно положить в репозиторий ради воспроизводимости |
| `pdf-extract` | крейт для извлечения текста в тестах | `cli/Cargo.toml`, dev-dependencies | требует проверки: версия, лицензия, отсутствие C-зависимостей | вместе с первым golden file (6.8) |
| Обновление плагинов Tauri | `dialog` 2.7.3 и `opener` 2.5.5 с обеих сторон, транзитивно `fs` 2.5.2 и `log` 2.9.1 | `src-tauri/Cargo.toml` и `frontend/package.json` парно | точные | не раньше 07.09.2026; после – E2E |
| `typescript-eslint` 8.69.0 | минорная версия линт-контура | `frontend/package.json` | `8.69.0` | опубликована 31.08.2026, право с 07.09.2026, в одной пачке с плагинами |
| `@testing-library/user-event` 14.6.7 | патч | `frontend/package.json` | `14.6.7` | опубликован 02.09.2026, право с 09.09.2026 |

### 8.2. Требуют настройки, установка не нужна

| Что | Что настроить | Условие |
|---|---|---|
| Микроюстировка Typst | включить явно и подобрать величину по одному развороту настоящего текста | после первого PDF с настоящим корпусом |
| Дата документа в PDF | отключить (`set document(date: none)`), иначе детерминированность недостижима | с первым PDF, до первого golden file |
| Общий файл стилей ядра | заменить `Arial` на `Noto Serif Hebrew` в стеке редких знаков; тест шрифтового стека обязан остаться зеленым | вместе с положенными файлами шрифтов |
| Тест шрифтового стека | сузить до знакового состава: основная гарнитура вправе различаться между HTML и PDF, редкие знаки – нет (3.9) | вместе с предыдущим |
| `spec-guard` | добавить проверку, что ни одна зависимость в `src-tauri/Cargo.toml` не объявлена диапазоном | ближайшая правка теста; последняя диапазонная строка `log = "0.4"` приводится к факту тогда же |
| Проверки конвертации в PDF | завести golden files, детерминированность, `.notdef`, сверку текста (6.8) | с первым PDF |
| MCP плагина `svelte` | выяснить, удаленный он или локальный; при удаленном решить, приемлемы ли сетевые обращения | ближайшая сессия в Claude Code |
| Комментарий в сборочном контуре | строка 177 ссылается на `src-tauri/deny.toml`, удаленный 30.08 | ближайшая правка workflow |

### 8.3. Эксперименты до начала работы над PDF

Выполняются только после двух предусловий 3.8 и после выбора разборщика. Три проверки, каждая на день, и они решают вопрос по существу, а не по описанию. До их результата контур PDF считается спроектированным, но не подтвержденным.

1. **Построение документа через API.** Убедиться, что документ Typst собирается из значений без генерации разметки. Если API версии 0.15 этого не дает в нужном объеме, генерация разметки допустима – но с единственным местом экранирования и тестом на каждый специальный знак корпуса.
2. **Детерминированность.** Собрать один документ дважды на чистом дереве и сравнить байты; отдельно проверить `/ID` и теги подмножеств шрифтов.
3. **Плотная клинопись.** Взять документ с наибольшим числом знаков клинописи и посмотреть результат глазами: shaping, размер подмножества, время сборки.

### 8.4. Чего в этом разделе нет намеренно

`tracing` – ждет решения по логированию, а не установки. `rust-src` – по желанию владельца. Три отключенных расширения Claude Code – возвращаются по условиям из 7.4, а не устанавливаются заново. Ничего из 7.4 «Отвергнутое окончательно» сюда не попадает по определению.
