# Спецификация стека проекта Aruna

Редакция 15 от 30.08.2026. Заменяет редакции 1–14 целиком. Выровнена по сплошной сверке документа с деревом: шесть расхождений закрыты, снято внутреннее противоречие, перенесены правки, внесенные в репозиторную копию по ходу дня.

**О числах в этом документе.** Количество тестов и проценты покрытия стареют при каждом добавленном тесте, поэтому источником истины считается не документ, а вывод команд: `cargo nextest run --locked`, `cargo llvm-cov` после `cargo llvm-cov clean --workspace` (без очистки инструмент неидемпотентен), `pnpm vitest run`. Числа ниже приведены с датой замера и означают порядок величины, а не константу.

Отличие от прежних редакций принципиальное: документ описывает не фронтенд-окружение, а весь стек проекта – консольный крейт, настольное приложение и его интерфейс как три части одного целого, с общими правилами версий, безопасности и проверок.

## 1. Назначение и границы документа

1.1. Документ описывает окружение настольного приложения Aruna: состав компонентов, основания выбора, правила работы и критерии приемки. Он отвечает на три вопроса – что стоит, почему именно это, и как проверить, что все на месте.

1.2. Что документ не покрывает: логику самого приложения, устройство крейта cli, содержание корпуса TLHdig. Правила для агентов вынесены в CLAUDE.md в корне репозитория; расхождение между этим документом и CLAUDE.md считается дефектом документации.

1.3. Проект – это одна программа, Aruna, и контур интерфейса, который для нее строится.

Программа читает корпус TLHdig, разбирает его и формирует опись. Сейчас она существует в консольном виде – это текущая форма, выбранная сознательно, а не отдельный продукт. Интерфейс к ней разворачивается в два слоя: оболочка на Tauri дает окно, разрешения и мост, фронтенд на Svelte рисует то, что видит человек. Ни оболочка, ни фронтенд самостоятельной ценности не имеют и вне программы не существуют.

| Слой | Каталог | Пакет | Роль |
|---|---|---|---|
| Ядро программы | `cli` | `aruna` 2.5.0 | загрузка корпуса с Zenodo, разбор, опись; консольная точка входа и библиотека |
| Оболочка интерфейса | `src-tauri` | `aruna-desktop` 0.2.0 | окно, разрешения, мост между ядром и интерфейсом |
| Интерфейс | `frontend` | `frontend` (частный) | одностраничное приложение на Svelte и Vite |

Слой оболочки назван `aruna-desktop` только потому, что имя пакета в Cargo обязано быть уникальным; словом «настольное приложение» в этом документе обозначается форма, которую программа примет, а не второй продукт. С 30.08.2026 оба крейта живут в одном воркспейсе (4.9.1).

Связь между слоями установлена 30.08.2026: оболочка объявляет зависимость от ядра по пути внутри воркспейса и вызывает его первой командой (4.9.6). Окно спрашивает у программы, где лежит собранный пакет, и показывает путь – то же самое, что программа печатает в консоли.

Корневой package.json – манифест воркспейса Node с командами верхнего уровня. Корневой Cargo.toml – манифест воркспейса Cargo: он перечисляет оба крейта и держит профили сборки (4.9.1, 4.9.3). Там же лежит единственная конфигурация политик зависимостей `deny.toml` (3.4).

1.4. Статусы компонентов, используемые далее: **установлено** (присутствует, версия подтверждена выводом инструмента), **настроено** (сконфигурировано под проект), **проверено** (работоспособность подтверждена прогоном), **ожидает** (отсутствует осознанно, подключается на следующем этапе), **исключено** (отвергнуто, причина записана).

1.5. Состояние на дату редакции: окружение развернуто и проверено прогонами. Изменения, копившиеся с 25.08.2026, зафиксированы 29–30.08 пятью коммитами по областям и выпущены как 2.5.0 (6.7). Работа 30.08 – воркспейс, связь оболочки с ядром, первая команда, живое окно, версия из одного источника – на момент выпуска редакции не зафиксирована.

## 2. Платформа и ее следствия

2.1. Целевая и единственная платформа – macOS 13.7.8 Ventura на Apple Silicon. Обновление операционной системы невозможно. Это исходное ограничение, из которого вытекает большинство решений документа.

2.2. Приложение работает в системном WKWebView, который на этой версии системы больше не обновляется. Отсюда обязательный нижний порог сборки `build.target = 'safari16'` в vite.config.ts (см. 4.3).

2.3. Xcode 15.2 – потолок для этой системы: версии 15.3 и старше требуют Sonoma. Сборочный контур привязан к нему.

2.4. Экосистема постепенно снимает поддержку Ventura. Практическое следствие: любое обновление проверяется в том числе на сохранение поддержки macOS 13, а решение, требующее более новой системы, отклоняется, а не обходится. Первый случай уже произошел – из стека выпал Playwright (см. 4.4).

2.5. Релизная сборка – universal binary, `pnpm build` в корне выполняет `tauri build --target universal-apple-darwin`. Оба таргета Rust установлены.

## 3. Состав окружения

### 3.1. Системная база

| Компонент | Версия | Статус |
|---|---|---|
| macOS | 13.7.8 (22H730), Apple Silicon | установлено |
| Xcode / clang / Git | 15.2 (15C500b) / 15.0.0 / 2.39.3 | установлено |
| Rust stable, Cargo | 1.97.1 | установлено |
| Компоненты toolchain | объявлены `rustfmt` и `clippy`; фактически установлены также `llvm-tools` и `rust-std` под три таргета – наследие прежнего тулчейна | установлено |
| Языковой сервер Rust | приходит расширением Claude Code `rust-analyzer-lsp` со своим бинарником, не через rustup | установлено |
| `rust-src` | отсутствует; нужен только для перехода в исходники стандартной библиотеки | не установлено (7.3) |
| Таргеты Rust | aarch64-apple-darwin, x86_64-apple-darwin | установлено |
| Node.js | 22.23.1, в ~/.local/bin, без Homebrew | установлено |
| pnpm / corepack | 11.22.0 / 0.35.0 | установлено; доступна 11.24.0, см. 7.2 |
| npm | 12.0.2, часть Node-инсталляции | установлено, в проекте запрещен (5.3) |
| tauri-cli | 2.11.4 | установлено |
| typescript-language-server | 6.0.0, глобально через pnpm | установлено |

Посторонний таргет wasm32-unknown-unknown оставлен для соседних инструментов и проекта не касается. Линия Node 22 находится в поддержке до апреля 2027 – это плановая точка пересмотра Node-контура.

**Версия компилятора названа в одном месте – `rust-toolchain.toml` в корне.** Там же закреплены оба целевых таргета, и это не украшение: тулчейн, который ставит rustup, приносит только хостовый таргет, а таргеты принадлежат тулчейну. Когда канал закрепили, второй таргет молча исчез – сборочный контур не заметил, потому что называет оба сам, и сломалась только сборка на машине разработчика, с ошибкой про таргет, который документ числил установленным. Объявление в файле снимает это раз и навсегда.

Поле `rust-version` в манифестах не заводится – основание в 4.9.2.

### 3.2. Фронтенд

| Компонент | Версия | Статус |
|---|---|---|
| Svelte | 5.56.10 | проверено |
| Vite (бандлер Rolldown) | 8.2.2 | проверено |
| `@sveltejs/vite-plugin-svelte` | 7.3.0 | установлено |
| TypeScript | 6.0.3 | установлено |
| `svelte-check` | 4.7.6 | проверено, 0 ошибок |
| `@types/node` | 22.20.1 | установлено |
| `@tsconfig/svelte` | 5.0.8 | установлено |
| `@tauri-apps/api` | 2.11.1 | установлено |
| `@tauri-apps/cli` | 2.11.4 | установлено |
| `@tauri-apps/plugin-dialog`, `-opener`, `-store`, `-window-state` | 2.7.2, 2.5.4, 2.4.4, 2.4.1 | установлено, импортов пока нет |

Архитектура – одностраничное приложение, точка входа `src/main.ts` и `src/App.svelte`. SvelteKit исключен (см. 4.2). Peer-требования плагина сборки линии 7 (Svelte не ниже 5.46.4, Vite 8.x, Node 22.12 и выше) выполняются с запасом. Линия `@types/node` обязана совпадать с рантаймом.

Второе назначение того же стека, с окном не связанное: каталог `src/inventory/` – исходники выходного документа описи. Восемь компонентов (`Document.svelte` и семь фрагментов) отрисовываются один раз при сборке через `render()` из `svelte/server`, результат ложится в `cli/src/generated/*.html`, откуда его вкомпилирует крейт `aruna`. В браузере эти компоненты не монтируются никогда: это шаблоны, которым досталась проверка типов, форматер и линтер наравне с остальным фронтендом. Механизм и его основания – в `docs/FRONTEND-CONTRACT.md`, раздел «The markup».

### 3.3. Тесты

| Компонент | Версия | Статус |
|---|---|---|
| Vitest | 4.1.11 | проверено |
| `@vitest/coverage-v8` | 4.1.11 | проверено |
| `@testing-library/svelte` | 5.4.2 | проверено |
| `@testing-library/jest-dom` | 7.0.1 | проверено |
| `@testing-library/user-event` | 14.6.6 | проверено |
| `jsdom` | 30.0.1 | проверено |
| `@wdio/cli` | 9.31.2 | проверено |
| `@wdio/tauri-service` | 1.3.0 | проверено |
| `@wdio/tauri-plugin` | 1.3.0 | проверено |
| `@wdio/local-runner`, `@wdio/mocha-framework`, `@wdio/spec-reporter` | 9.31.2 | установлено |
| `@types/mocha` | 10.0.10 | установлено |
| `tauri-plugin-wdio-webdriver`, `tauri-plugin-wdio` | 1.3.0 | проверено, optional под фичей `e2e` |

Vitest настроен двумя проектами: `node` для каталога tests и `component` для src. Компонентному проекту задан `resolve.conditions: ['browser']` – без этого Svelte 5 резолвится в серверную сборку и монтирование падает. Покрытие ограничено кодом приложения, иначе в отчет попадают картинки и стили.

Состояние тестовой базы на 02.09.2026: 80 тестов в девяти файлах, все зеленые. Из них App – 23, spec-guard – 18 проверок соответствия этому документу, filter – 14, readme-links – 7, one-frontend-stack – 6, inventory-artifact – 6, font-stack – 3, ipc-shape – 2, release-version – 1. Числа выросли на семнадцать в один день: окно перестало быть смотровым стеклом над папкой загрузок и получило семь состояний, поток событий и отмену, а проверок у него было семь.

`tests/ipc-shape.test.ts` появился 31.08 вместе с расширением `corpus_stats` и держал вместе два ручных объявления одной формы данных – типы Rust и типы окна. С подключением specta 02.09 объявление стало одно, и файл держит уже не форму, а два обещания, которых порожденный тип не дает: что образец `src-tauri/stats-sample.json` – это действительно то, что Rust сериализует, и что пустые места приходят как `null`, а не как отсутствующие поля.

Условие пересмотра, записанное для трех пакетов Testing Library, сработало полностью. `@testing-library/svelte` и `jest-dom` получили потребителя с появлением компонентного теста настоящего экрана; `@testing-library/user-event` – 31.08.2026, когда экран получил проверку, разыгрывающую ввод, а не разметку: табуляция обязана привести фокус на единственную кнопку, а `Enter` – нажать ее. `button.click()`, которым пользуется соседняя проверка, зовет обработчик мимо фокуса и клавиатуры и об их работе не говорит ничего, тогда как `app.css` объявляет кнопке `:focus-visible`.

Четыре пакета wdio, отмеченные строкой «по факту установки», не приходят вместе с CLI и ставятся отдельно – без них прогон обрывается на поиске исполнителя.

### 3.4. Качество кода

| Компонент | Версия | Статус |
|---|---|---|
| ESLint, `@eslint/js` | 10.9.1, 10.0.1 | проверено |
| `typescript-eslint` | 8.68.0 | проверено |
| `eslint-plugin-svelte`, `svelte-eslint-parser` | 3.23.0, 1.8.1 | проверено |
| `eslint-config-prettier` | 10.1.8 | установлено |
| Prettier, `prettier-plugin-svelte` | 3.9.6, 4.1.1 | проверено |
| Clippy, rustfmt | из toolchain | проверено |
| cargo-audit | 0.22.2 | проверено |
| cargo-nextest | 0.9.143 | проверено |
| cargo-deny | 0.20.2 | проверено |
| cargo-machete | 0.9.2 | проверено |
| cargo-llvm-cov | 0.9.0 | проверено |
| GitHub CLI (`gh`) | 2.97.0 | установлено, авторизовано |

Типизированный слой линта описан в 4.7. Окно поддержки TypeScript у typescript-eslint – не ниже 4.8.4 и ниже 6.1.0; текущая версия внутри окна, и это единственная причина, по которой TypeScript 7 в проект не берется.

Настройки Prettier: без точек с запятой, одинарные кавычки, ширина 100. Исключены dist, node_modules, pnpm-lock.yaml, coverage.

Два правила ESLint выключены точечно – только для `src/inventory/**/*.svelte`, причина записана в самом `eslint.config.js`. `svelte/no-at-html-tags`: в этих компонентах `{@html}` – способ принять разметку, а не текст; при сборке в него подставляется метка вида `@@ROWS@@`, в рантайме – разметка, которую крейт собрал из тех же компонентов по тексту, уже прошедшему `escape_html`. `svelte/no-raw-special-elements`: `<head>` и `<body>` здесь буквальные, потому что компонент – это весь документ целиком, а `<svelte:head>` возвращает содержимое в отдельное поле `head`, и стили из артефакта пропали бы. Обе выключены в одном месте, а не обойдены в компонентах.

Конфигурация политик одна на весь проект – `deny.toml` в корне; прежние копии в крейтах удалены при объединении в воркспейс. Обе проверки, по ядру и по оболочке, читают ее.

Состояние на 02.09.2026: `cargo deny` по обоим крейтам отвечает «advisories ok, bans ok, licenses ok, sources ok». Строка `Apache-2.0 WITH LLVM-exception`, годами висевшая замечанием `license-not-encountered`, убрана в ту же правку, что и обещано: файл правился, лицензия в дереве не встречалась.

Правка была вынужденной и стоит того, чтобы ее запомнить. С подключением specta в дерево пришел `Inflector` под BSD-2-Clause, которой в списке не было, и `cargo deny` отказал – **уже в CI, а не до коммита**: набор 6.1 его не содержит, а спецификация называет `cargo deny` только среди инструментов. Двухпунктная BSD – это трехпунктная без оговорки о неодобрении: разрешительная, одобрена OSI, признана FSF, совместима с MIT, под которой выходит Aruna. Добавлена с этим обоснованием. Урок на будущее: новая зависимость тянет за собой лицензии, и `cargo deny check` стоит прогнать до коммита, а не узнавать об отказе от конвейера.

Платформенная фильтрация `targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]` задана там же и отсекает от проверок Linux-ветку дерева. `cargo machete` неиспользуемых зависимостей не находит. Покрытие оболочки на 31.08.2026 – 81,31 % по регионам при девяти тестах; 30.08 было 70,83 % при пяти.

Замеряя покрытие, обязательно сначала выполнять `cargo llvm-cov clean --workspace`: без очистки инструмент неидемпотентен и на одном и том же дереве дает разные числа.

Исключены: cargo-geiger (установка падает на сборке openssl-sys, в окружении нет pkg-config и OpenSSL dev, Homebrew не используется; контроль unsafe обеспечен иначе – см. 4.7), cargo-wizard (запрещен к использованию), cargo-outdated (дублирует `pnpm outdated` и `cargo update --dry-run`), sccache (лишний слой кеширования сборки и новый источник расхождений между машинами).

Необязательные инструменты разработчика, в критерии приемки не входящие и в манифесты не попадающие: bacon (фоновая проверка), cargo-expand (раскрытие макросов, полезно при разборе макросов Tauri), samply (профилирование).

### 3.5. Оболочка интерфейса – крейт `aruna-desktop`

| Компонент | Версия | Статус |
|---|---|---|
| Tauri | 2.11.5 | проверено |
| tauri-runtime, wry, tao | 2.11.3, 0.55.1, 0.35.3 | проверено |
| `tauri-plugin-dialog` | 2.7.2 | установлено, разрешение выдано |
| `tauri-plugin-opener` | 2.5.4 | установлено, разрешение выдано |
| `tauri-plugin-window-state` | 2.4.1 | установлено, разрешение выдано |
| `tauri-plugin-store` | 2.4.4 | установлено, разрешение выдано |
| Разрешения ядра | `core:default`; `core:window:allow-close` снято 02.09.2026 вместе с кнопкой, которая закрывала окно | настроено |
| Разрешение `opener:allow-open-path` | выдано 02.09.2026 отдельной строкой: набор `opener:default` открывает URL и показывает в Finder, но не открывает файл | настроено |
| `tauri-plugin-log` | 2.9.0 | установлено, только отладка и только без фичи `e2e` |
| tauri-runtime-wry | 2.11.4 | проверено |
| Покрытие | 81,31 % по регионам, девять тестов (31.08.2026) | проверено |
| Версия | 0.2.0, собственная нумерация | настроено |
| Профили сборки | выпуск и отладка объявлены (4.9.3) | настроено |
| Версия компилятора | названа в `rust-toolchain.toml`, в манифестах не дублируется (4.9.2) | настроено |
| `log` | 0.4 | установлено |
| `aruna` (ядро) | зависимость по пути внутри воркспейса | проверено |
| `serde` | 1 с фичей derive | проверено |
| `thiserror` | 2 | проверено |
| `serde_json` | 1 | проверено |
| `tempfile` (для тестов) | 3 | проверено |
| `tracing`, `tracing-subscriber` | – | ожидает, требует решения по логированию (4.6) |
| `specta`, `tauri-specta`, `specta-typescript` | =2.0.0-rc.25, =2.0.0-rc.25, =0.0.12 | подключены 02.09.2026, проверено |

Манифест содержит только то, что используется. `serde` с фичей `derive` сериализует ответы и ошибки команд, `serde_json` читает счетчики из манифеста пакета, `tempfile` в зависимостях для тестов строит каталог-пакет на каждую ветку проверки. У `tauri` и `tauri-build` набор фич пуст. Правила работы с манифестом – в 4.9.

Прежние редакции утверждали, что `serde` и `serde_json` удалены как неиспользуемые. Это было верно ровно до появления команд и снято.

Конфигурация приложения: идентификатор `com.sergeyssimonov.aruna.shell`, productName `aruna-desktop`, заголовок окна «Aruna», `bundle.targets: ["app", "dmg"]`, `app.withGlobalTauri: true`. Заголовок документа в index.html обязан совпадать с заголовком окна – по нему E2E-сервис находит окно.

Имя и идентификатор оболочки намеренно отличаются от продуктовых: продукт называется Aruna и распространяется как приложение из ядра, а оболочка – часть окружения, и ее артефакт не должен занимать имя продукта в системе. Идентификатор с суффиксом `.shell` разводит их для системных служб, которые кешируют приложение по идентификатору.

Окно: метка `main`, 800 на 600, `resizable: false`, `center: true`. Метка внесена в список исключений плагина window-state – размер и положение сознательно не восстанавливаются (6.4).

Версия оболочки – 0.2.0, и она ведет собственную нумерацию, пока образ для распространения собирается из ядра (6.7): у слоя своя короткая история, и приравнивать его номер к версии программы имело бы смысл только тогда, когда пользователь получает именно оболочку. Номер 0.1.0 из шаблона поднят 30.08.2026 – в этот день слой перестал быть каркасом.

Поле `version` из конфигурации убрано 30.08.2026: Tauri берет версию из манифеста крейта, и это остается единственным местом, где она объявлена. Дублирование давало число, ничего не значащее и способное разойтись с манифестом молча. Сценарий E2E соответственно читает ожидаемую версию из манифеста, а не сверяет одну записанную константу с другой.

`tauri-plugin-shell` в проект не берется: право запускать дочерние процессы для задачи «открыть ссылку или файл» избыточно, эту задачу решает opener. Shell добавляется только под доказанную функцию запуска внешних программ.

`specta` с `tauri-specta` закрыли нетипизированную дыру между Rust и TypeScript 02.09.2026, вместе с появлением команд сборки. До них вызовы `invoke` не проверялись ни компилятором, ни svelte-check, и первый же двухсловный аргумент (`local_archive`) сломал бы вызов молча – Tauri ждет его в camelCase. Стабильного выпуска под Tauri 2 у этого семейства нет: старший стабильный `tauri-specta` – 1.0.2 от 2023 года, под Tauri 1, – поэтому версии закреплены точным равенством, а не кареткой. Запасной вариант ts-rs не понадобился: он порождает только типы, а обертки команд пришлось бы вести вручную, и это как раз та половина, которая расходилась.

Порожденные типы лежат в `frontend/src/bindings.ts` и закоммичены как продукт – та же дисциплина, что у артефактов описи в `cli/src/generated/`: тест оболочки `the_bindings_are_what_these_commands_produce` пересобирает их во временный файл и падает, если байт разошелся. Порождение при старте приложения отвергнуто: отладочный запуск и прогон E2E писали бы в рабочее дерево. Обновление – `cargo test -p aruna-desktop -- --ignored regenerate`. Файл исключен из prettier и eslint: его форма принадлежит экспортеру, и правка нашими инструментами развела бы продукт с источником.

### 3.6. Ядро программы – крейт `aruna`

Крейт `cli` версии 2.5.0 – собственно программа и образец, по которому выстроены правила 4.9. Он существовал до появления интерфейса и содержит всю предметную логику; оболочке предстоит вызывать ее, а не заводить свою.

| Свойство | Значение |
|---|---|
| Зависимости | `dirs` 5, `memchr` 2, `thiserror` 2, `ureq` 2.12 с фичей tls, `zip` 2.3 без фич по умолчанию, кроме deflate |
| Зависимости для тестов | `tempfile` 3 |
| Фичи | `bench` – открывает извлечение полей для замеров, по умолчанию выключена и бинарником не включается |
| Профиль выпуска | `lto = true`, `codegen-units = 1`, `strip = true`, `opt-level = 3`; `panic = "abort"` сознательно не ставится |
| Профиль отладки | `opt-level = 1` |
| Тесты | 17 файлов в каталоге tests плюс модульные тесты в исходниках; 410 тестов, покрытие 95,69 % по регионам (31.08.2026, замерено при выпуске 2.5.1; в редакции 15 стояло 95,73 % – число сдвинули ветки отказа, пришедшие вместе с новыми тестами, и здесь записано измеренное, а не желаемое); один тест помечен пропускаемым намеренно – он пишет пакет целиком и читает его обратно, что дорого для обычного прогона |
| Аргументы командной строки | `--help` и `--version` печатают имя, версию из манифеста и строку про переменную с путем к архиву, выходя с кодом 0 до всякой работы |
| Воспроизводимость | после полной очистки заново собранный релизный бинарник дает побайтово тот же пакет – 23 938 файлов с той же контрольной суммой |

Три приема из этого манифеста приняты как обязательные для всего проекта. Первый: нижняя граница версии ставится с комментарием о причине – у `zip` это устраненная уязвимость, и в комментарии сказано, что lock-файл, собранный с более низкой границы, может вернуть уязвимую версию при первом же обновлении. Второй: необязательная функциональность выносится в фичу, а в комментарии объясняется, зачем она нужна и почему выключена. Третий: отказ от прерывания процесса при панике обоснован практикой – без разворачивания стека ошибка разбора превращалась в молчаливое падение без сообщения.

Состав исходников: разбор и извлечение полей, работа с архивом и кешем, каталог и загрузка с Zenodo, экспорт и генерация HTML, стили представления, задания и прогресс. Эта часть спецификацией не регулируется по существу – здесь фиксируется только то, что важно для стека в целом: версия, профили, политика зависимостей и наличие собственного набора тестов.

### 3.7. Инструменты агента

Claude Code – версия 2.1.251 на дату редакции. Линия выпускается почти ежедневно, поэтому фиксируется правило, а не номер: держать последнюю опубликованную версию и сверять состав расширений после каждого обновления.

Включенные расширения: `rust-analyzer-lsp`, `typescript-lsp`, `commit-commands`, `context7`, `frontend-design`.

Отключенные, с причинами и условиями возврата:

| Расширение | Причина отключения | Условие возврата |
|---|---|---|
| `github` | MCP-сервер падает при каждом старте без переменной GITHUB_PERSONAL_ACCESS_TOKEN | осознанная настройка токена с минимальными правами |
| `code-review` | несет навык и рассчитан на сценарий пул-реквеста с записью комментария в GitHub; при работе коммитами в main невыполним | переход на процесс с пул-реквестами |
| `security-guidance` | четыре хука, срабатывание после каждого вызова инструмента, отдельное окружение Python | этап кода, обрабатывающего внешние данные |

Основание ревизии – вывод `claude plugin details`: языковые серверы работают вне процесса модели и контекста не расходуют, поэтому их держать выгодно; навыки и MCP-серверы стоят контекста и требуют оправдания.

Сторонние расширения с hooks на каждое сохранение исключены: неаудированный код с доступом к репозиторию противоречит выбранной политике цепочки поставок. Официальный MCP проекта Svelte (sveltejs/ai-tools) под этот запрет не подпадает – он от авторов Svelte, hooks не ставит, архитектуру не задает; статус необязательный.

## 4. Архитектурные решения и их основания

### 4.1. Пакетный менеджер – pnpm

Выбран не по привычке, а по трем механизмам защиты, действующим по умолчанию: карантин новых версий (1440 минут, распространяется на транзитивные зависимости), запрет экзотических источников для транзитивных зависимостей, контроль install-скриптов. У npm 12 из этого есть только третий. Дополнительно pnpm дает жесткую изоляцию node_modules, исключающую обращение к незадекларированным пакетам, и удержание версии самого менеджера через corepack и поле `packageManager`.

### 4.2. Без SvelteKit

Приложение живет внутри Tauri, серверный слой не нужен. Kit навязал бы маршрутизацию, серверные соглашения и собственную сборку поверх десктопного окна – сложность без выигрыша. Запрет проверяемый: в репозитории не должно быть каталога src/routes, файлов +page, +layout, +server, hooks.server, импортов из `$app/*` и вызовов `svelte-kit sync`.

### 4.3. Нижний порог сборки safari16

Дефолт Vite 8 – baseline-widely-available, то есть Safari 16.4 и выше, что для нашей системы слишком высоко. Явный `build.target = 'safari16'` опускает порог до безопасного. CSS понижает Lightning CSS, `build.cssTarget` наследует то же значение.

Важное следствие: в режиме разработки транспиляция минимальна, поэтому фраза «работает в pnpm tauri dev» соответствия порогу не доказывает. Доказательством служат только сборка и прогон E2E собранного приложения.

Проверять порог следует по артефакту, а не по конфигурации: в собранном файле стилей не должно оставаться вложенности, правила `@media` вынесены наружу, символ `&` отсутствует, цвета сведены к шестнадцатеричной записи с альфой. Наличие строки в vite.config.ts доказывает намерение, а не результат.

### 4.4. E2E внутри реального приложения

Единственный способ проверить тот движок, в котором работает пользователь. Альтернативы непригодны: у macOS нет WebDriver для WKWebView; внешний tauri-driver поддерживает только Windows и Linux; Playwright с версии 1.58 (январь 2026) не поддерживает WebKit на macOS 13, а его собственные сборки движка в любом случае не совпадают с системным.

Механика прогона – все пункты обязательны:

1. WebdriverIO с сервисом `@wdio/tauri-service`, в конфигурации указан `driverProvider: 'embedded'`: WebDriver-сервер работает внутри приложения.
2. Rust-плагины wdio регистрируются под `#[cfg(feature = "e2e")]`; для сборки без фичи объявлены пустые заглушки, чтобы цепочка Builder оставалась единой.
3. Разрешения `wdio-webdriver:default` и `wdio:default` лежат в `src-tauri/capabilities-e2e/e2e.json` – вне каталога, который сканирует build.rs. Иначе обычная сборка падает на неизвестных разрешениях. Файл встраивается через `include_str!` и регистрируется в setup вызовом `add_capability` под тем же cfg; трейт `tauri::Manager` импортируется тоже под cfg.
4. Фронтенд-половина `@wdio/tauri-plugin` импортируется в `src/main.ts` под условием `import.meta.env.VITE_E2E`. Без нее недоступен `core.invoke`; в обычной сборке переменная не задана и импорт выбрасывается при сборке.
5. В конфигурации приложения включен `app.withGlobalTauri: true`.
6. Заголовок документа совпадает с заголовком окна.
7. При сборке с фичей `e2e` не регистрируется tauri-plugin-log (см. 4.6).
8. Приложение собирается командой `cargo tauri build --debug --no-bundle --features e2e` с переменной `VITE_E2E=1`. Обычный `cargo build` не годится: он выставляет `cfg(dev)`, и окно грузит devUrl вместо собранного фронтенда, отчего DOM оказывается пустым.

Сценарии проверяют запуск приложения, видимость элементов, взаимодействие и достижимость бекенда. Обращение идет через `browser.tauri.execute`; в колбэк передается контекст с `core`, объекта `app` в нем нет.

Кнопка подтверждения в окне служит и органом управления, и измерительным прибором: вне тестового режима она закрывает окно, под флагом `VITE_E2E` только увеличивает счет. Счет наружу выходит атрибутом `data-clicks`, а не текстом – человек его не видит, сценарий читает. Это единственное место в проекте, где доказано, что изменение состояния доходит до DOM в замороженном WKWebView, поэтому сценарий меняется вместе с кнопкой и осознанно, а не мимоходом.

Известный шум сервиса версии 1.3.0, на результат не влияющий и настройками не устранимый: диагностика об отсутствии tauri-driver (при embedded он не нужен) и сообщение об очистке моков после закрытия сессии. Оба – дефекты сервиса.

### 4.5. Отладочные механизмы не попадают в релиз

Гарантия механическая, а не дисциплинарная: оба E2E-плагина объявлены `optional = true` и включаются только фичей `e2e`, фронтенд-половина отсекается отсутствием переменной VITE_E2E. Релизная сборка идет без фичи и без переменной, поэтому этого кода в ней физически нет. Проверяется по составу зависимостей release-профиля.

### 4.6. Логирование

Глобальный логгер в процессе может быть только один. Сейчас его занимает tauri-plugin-log, поэтому при сборке с фичей `e2e` он не регистрируется – крейты wdio ставят свой логгер первыми, и второй вызывает панику при старте. По той же причине подключение tracing требует предварительного решения, каким остается единственный контур логирования.

### 4.7. Контроль корректности и безопасности кода

Три слоя, каждый со своей задачей.

**Компилятор.** `#![forbid(unsafe_code)]` в src/main.rs и src/lib.rs – жесткая граница, нарушение блокирует сборку. Конфликта с макросами Tauri нет.

**Типизированный линт.** Набор `recommendedTypeChecked` применяется двумя отдельными блоками: к `src/**/*.ts` и к `**/*.svelte`. В блоке для компонентов заново объявляется `svelteParser` с вложенным `parser: ts.parser` и `extraFileExtensions: ['.svelte']` – иначе типизированный набор подменяет парсер и ломает разбор компонентов. Тестовая оснастка и конфигурационные файлы остаются на базовом наборе: они не входят в tsconfig.app.json, а проектная служба типов требует принадлежности файла проекту.

Смысл слоя в том, что он ловит невидимое синтаксическому линту: потерянные промисы (критично для вызовов Tauri – невыжданный `invoke` теряет и результат, и исключение), промисы в обработчиках, ожидающих синхронную функцию, `await` над не-thenable, небезопасные обращения к `any`.

**Аудит зависимостей.** cargo-audit по уязвимостям, cargo-deny по политикам. В отчете аудита семнадцать предупреждений класса unmaintained и unsound – они относятся к Linux-ветке дерева (gtk-rs, glib) и к build-time крейтам (proc-macro-error, unic-*); на macOS этот код не компилируется и в рантайм не попадает. Подавлять их запрещено: они остаются индикатором на случай появления настоящей уязвимости.

### 4.8. Контур проектирования интерфейса

Разрешенный путь – команда `/design` в Claude Code (исследовательский предпросмотр, доступна с 17.08.2026). Она рисует несколько редактируемых макетов, из них выбирается один, при необходимости правится на холсте и в той же сессии переписывается в компоненты проекта. Стек значения не имеет: холст рисуется средствами артефактов, код пишется существующими компонентами Svelte.

`/design-sync` и любая загрузка библиотеки компонентов в среду Claude Design запрещены: та среда собирает прототипы из React-функций, подставить туда компоненты Svelte нечем, а обхода через SvelteKit нет, поскольку Kit исключен. Запрет действует до появления отрисовщика, не привязанного к React. Оговорка о достоверности: назначение обеих команд подтверждено публикациями июня и августа 2026 года, а утверждение о невозможности загрузить библиотеку Svelte взято из внешнего разбора и не проверялось по первоисточнику. На решение это не влияет – запрет принят по принципу минимального риска.

Ограничение, которое принимается как данность: макет на холсте – визуальный контракт, а не готовый компонент, поэтому сложный экран всегда делается в два шага. Реализация идет в существующие компоненты; заводить параллельные Button, Input и подобные запрещено.

Контрольная проверка контура: один прогон `/design` с последующей реализацией на существующем экране не создает ни одного файла Kit и не вызывает `/design-sync`.

### 4.9. Правила Rust-части

Раздел описывает требования к коду и манифесту src-tauri. Крейт cli живет по своим правилам и здесь не регулируется, но служит образцом: в нем нижние границы версий проставлены с комментарием о причине, включая ссылку на устраненную уязвимость, а необязательная функциональность вынесена в фичу с пояснением, зачем она нужна.

**4.9.1. Воркспейс и имена пакетов.** Крейты объединены в воркспейс 30.08.2026: корневой манифест перечисляет участников `cli` и `src-tauri`, lock-файл один на оба, каталог сборки общий и лежит в корне репозитория.

Имя `aruna` принадлежит ядру, оболочка называется `aruna-desktop`, ее библиотека – `aruna_desktop_lib`. Имя пакета определяет имя исполняемого файла, а на него завязан путь к приложению в конфигурации wdio, поэтому любое переименование делается одним согласованным шагом – манифест, вызов библиотеки в main.rs, путь в wdio.conf.ts – и подтверждается прогоном E2E.

Объединение подтянуло двадцать три транзитивные зависимости ядра к версиям, которые уже стояли у оболочки. Проверено по существу, а не по рассуждению: релизный бинарник ядра пересобран и прогнан на настоящем корпусе, полученный пакет из 23 938 файлов побайтово совпал с прежним. Это и есть критерий для подобных изменений – совпадение результата работы программы, а не совпадение номеров версий.

Следствие для команд: `cargo` без указания пакета работает по всему воркспейсу, поэтому `cargo nextest run --locked` прогоняет оба крейта разом – 412 тестов на 30.08.2026. Для работы с одним крейтом используется ключ `-p aruna` или `-p aruna-desktop`. В сборочном контуре команды идут через `--manifest-path` и остаются рабочими без правок.

**4.9.2. Версия компилятора – одно место.** Версия названа один раз, в `rust-toolchain.toml`. Поле `rust-version` в манифестах не заводится ни в одном крейте – решение владельца от 28.08.2026, зафиксированное здесь и в CLAUDE.md.

Основание: два механизма решают разные задачи, и дублирование создает второй источник истины. `rust-toolchain.toml` определяет, каким компилятором проект собирается на этой машине, и это ровно то, что нужно приложению, которое собирается в одном месте и не публикуется как библиотека. Поле `rust-version` объявляет минимальную поддерживаемую версию для чужих сборок – обещание, которого проект не дает и не проверяет.

Плата за это решение названа прямо: резолвер Cargo ничем не ограничен и вправе выбирать зависимости, требующие более нового компилятора. Практических отказов это пока не вызывало; если вызовет – сборка упадет на конкретном крейте, и тогда вопрос вернется с измеренным основанием, а не с предположением.

Предыдущая редакция утверждала обратное – что поле объявлено и что оно сдерживает рост списка обновляемых крейтов. Первое устарело, второе было выводом без измерения; оба утверждения сняты.

**4.9.3. Профили сборки.** Профили объявлены в корневом манифесте воркспейса и только там: выпуск – `lto = true`, `codegen-units = 1`, `strip = true`, `opt-level = 3`; отладка – `opt-level = 1`. В воркспейсе секции профилей из манифестов участников игнорируются, поэтому дублировать их в крейтах нельзя – значения молча перестанут действовать.

Выпуск дает меньший и быстрее работающий бинарник ценой более долгой сборки – размен, приемлемый для артефакта, который собирается редко. Отладочный профиль с первым уровнем оптимизации ускоряет работу приложения при разработке, почти не удлиняя сборку.

`panic = "abort"` не ставится сознательно, и причина записана комментарием прямо в манифесте: паника обязана печатать место, где она произошла. В консольном крейте отказ от разворачивания стека однажды превратил ошибку разбора в молчаливое падение без сообщения для пользователя.

**4.9.4. Политика зависимостей.** Каждый новый крейт добавляется с обоснованием: что именно он решает и почему это не решается средствами стандартной библиотеки или Tauri. Фичи включаются минимально необходимые, `default-features = false` предпочтителен там, где это не ломает сборку. Нижняя граница версии ставится осознанно: если версия выбрана из-за устраненной уязвимости или нужного поведения, причина записывается комментарием в манифесте. Неиспользуемые зависимости удаляются – это проверяет cargo-machete. Дубли версий одного крейта в дереве отслеживаются командой `cargo tree --duplicates`: единичные дубли из транзитивных веток допустимы, рост их числа – повод разбираться.

**4.9.5. Обработка ошибок.** Ошибки типизированные, через `thiserror`; строковые ошибки и `Box<dyn Error>` в коде приложения не используются. `unwrap` и `expect` запрещены везде, кроме точки входа, где падение при невозможности запустить приложение осмысленно. Ошибка, возвращаемая командой во фронтенд, обязана быть сериализуемой и не должна раскрывать внутренние пути и детали окружения.

**4.9.6. Команды и границы с фронтендом.** Команда – тонкая обертка над функцией предметной области из крейта `aruna`, а не место для логики. Оболочка не повторяет то, что уже написано в ядре, и не заводит собственных путей к данным корпуса: одно поведение – одна реализация, вызываемая и консольной формой, и окном.

Первая команда, `corpus_location`, написана по этому правилу и служит образцом: каталог загрузок отдает `aruna::paths`, имя пакета и имя описи объявлены там же константами, оболочка только складывает путь и проверяет наличие. Ошибка типизирована через `thiserror` и сериализуется строкой – по правилу 4.9.5.

Вторая команда, `corpus_stats`, добавлена 30.08.2026 и следует тому же правилу: путь приходит параметром, счетчики читаются из манифеста пакета, обход каталога остается запасным путем на случай испорченного манифеста, а поле источника в ответе честно говорит, откуда взяты числа. Имя файла манифеста берется константой ядра, а не повторяется строкой.

Отдельно о разрешениях. Команда ядра Tauri требует своего разрешения, даже когда вызывается из собственного кода приложения: закрытие окна по кнопке потребовало добавить `core:window:allow-close` в capability, до этого вызов молча отклонялся. Отсюда правило для интерфейса: результат обращения к бекенду обрабатывается явно, отказ показывается на экране, а не проглатывается – иначе несуществующее разрешение выглядит как неработающая кнопка. Аргументы и результат сериализуемые. Общее состояние передается через механизм состояния Tauri, а не через глобальные переменные. Длительная работа – разбор архива, обход корпуса – не выполняется в потоке событий: она уходит в отдельный поток или в блокирующую задачу исполнителя, а о ходе выполнения фронтенд узнает событиями. Пока не подключены `specta` и `tauri-specta`, соответствие типов между Rust и TypeScript не выводится автоматически, поэтому каждая команда объявляется дважды. С 31.08.2026 дублирование не держится на внимательности: обе стороны сверяются с общим образцом `src-tauri/stats-sample.json` – Rust сверяет с ним свою сериализацию, окно сверяет с ним типизированный литерал, а `satisfies` привязывает литерал к типам. Правка одной стороны без остальных роняет одну из двух проверок, и обе входят в набор 6.1. Для команды, у которой такого образца нет, остается прежнее правило – ручная сверка сигнатуры с вызовом на фронтенде.

Состав экрана на 31.08.2026. Окно показывает статистику собранного пакета тремя строками: два итога – фрагменты и группы CTH, – под ними разбивка по группам (самая большая с ее размером, число групп из одного фрагмента, число фрагментов без CTH) и счетчики письма из манифеста (документы вне NFC, документы с private use и число таких кодовых точек, сумма аномалий). Разбивка берется из перечня групп, который манифест уже несет; при отсутствии манифеста она считается обходом каталога, а строка счетчиков письма не показывается вовсе – обход документы не читает, и нули на этом месте утверждали бы, что аномалий не нашлось. Корпус окно не собирает: сборка идет консольной программой, окно только показывает результат.

**4.9.7. Пути и данные.** Пути к данным приложения берутся у Tauri через его каталоги, жестко прописанные пути и обращения к домашнему каталогу пользователя запрещены. Файлы корпуса и кеш располагаются в каталогах приложения, а не рядом с исполняемым файлом.

**4.9.8. Тесты Rust.** В `aruna-desktop` девять тестов. Первый закрепляет требование 4.5 – сборка без фичи `e2e` не регистрирует WebDriver; остальные разыгрывают ветки команды, читающей счетчики: манифест пакета на месте, манифест испорчен, пакета нет вовсе, разбивка по группам взята из перечня самого манифеста, группа без CTH опознана по метке ядра, пустой пакет не называет самой большой группы, а форма ответа совпадает с образцом, по которому ее объявляет окно. Это правильный образец: требование, записанное словами, проверяется кодом, а каждая ветка отказа имеет свой тест.

Требования к дальнейшим тестам: модульные рядом с кодом, интеграционные в каталоге tests, команды проверяются на макете приложения без открытия окна средствами тестового модуля Tauri. Запуск – через nextest, покрытие – через llvm-cov. Флаг `--no-tests=pass` в наборе 6.1 остается как страховка на случай временного отсутствия тестов в одном из крейтов, но необходимостью быть перестал.

Консольный крейт имеет собственный набор из семнадцати файлов интеграционных тестов; при работе над ним действует тот же порядок запуска. Число исправлено при установке редакции: `ls cli/tests/*.rs` дает семнадцать, и таблица 3.6 в этом же документе говорит семнадцать – «пятнадцать» здесь противоречило и дереву, и соседнему разделу. Та же строка была неверна в редакциях 12 и 14 и правится третий раз.

**4.9.9. Воспроизводимость сборки.** Релизная сборка выполняется с флагом `--locked`: он запрещает молчаливое изменение Cargo.lock во время сборки. Расхождение lock-файла и манифеста в этот момент – ошибка, а не повод обновиться.

## 5. Правила работы

### 5.1. Версии и обновления

Все версии зафиксированы точно. Значение `latest` в package.json запрещено.

Обновление допускается при трех условиях одновременно: релиз-ноты прочитаны; версии не меньше семи дней; есть причина – затрагивающий проект дефект или безопасность – либо идет плановая ревизия перед новым этапом. Обновления «на всякий случай» запрещены. После обновления обязателен набор проверок 6.1, а при затрагивании контура E2E – и 6.2.

Транзитивные зависимости вручную не поднимаются: выбор сделан резолвером по объявленным диапазонам и зафиксирован lock-файлом, а ручная правка обходит и то, и другое. У Cargo к этому добавляется ограничение по полю rust-version: ручное вмешательство делает заявленный минимальный компилятор ложным.

Единственное исключение – когда обновление **родителя** убирает уязвимый пакет из дерева целиком. Подмены версии внутри чужого диапазона при этом не происходит: резолвер по-прежнему выбирает сам, а исчезновение проверяется по lock-файлу – имени пакета там не встречается ни разу. Такое переопределение пишется точной версией (каретка пустила бы дерево дрейфовать вопреки первому абзацу этого правила), сопровождается наборами 6.1 и 6.2 и записью – какой пакет и из-под какого родителя исчез. Подмена версии, оставляющая пакет в дереве, запрещена по-прежнему, в том числе когда она закрывает оповещение.

Порядок ревизии: `pnpm outdated` в frontend, в корне и с ключом -g; `cargo update --dry-run --verbose` в src-tauri; `cargo search` по установленным cargo-инструментам; `claude --version`. Текущий список наблюдения и план – в 7.2.

### 5.2. Lock-файлы и цепочка поставок

В Git фиксируются `frontend/pnpm-lock.yaml`, корневой `pnpm-lock.yaml` и корневой `Cargo.lock`. Последний один на воркспейс: прежние `cli/Cargo.lock` и `src-tauri/Cargo.lock` удалены при объединении. Удалять lock-файлы без необходимости нельзя.

Защита pnpm (4.1) не отключается. Решения по install-скриптам записаны явно в `frontend/pnpm-workspace.yaml`: edgedriver, esbuild и geckodriver – false. Незаполненные решения дают ошибку установки. esbuild работает без своего postinstall, поскольку платформенный бинарник приходит отдельным пакетом через optionalDependencies; удалению esbuild не подлежит – его тянут сервис и плагин wdio, загрузчик конфигураций и сам Vite.

**Оповещения Dependabot и почему они расходятся с местными проверками.** GitHub смотрит на граф без учета платформы и без деления на области, поэтому его список бывает краснее набора 6.1, где `cargo deny` отсекает Linux-ветку фильтром платформ, а npm-дерево не проверяется вовсе. Расхождение – не повод не смотреть: оповещение закрывается только с доказательством недостижимости, записанным в самом оповещении, а при достижимом исправлении сначала берется исправление. Поднимать транзитивную версию через `overrides` запрещено правилом 5.1 – это обход и объявленных диапазонов, и резолвера. Исключение там же и только одно: переопределение родителя, выбрасывающее пакет из дерева целиком.

Разбор 01.09.2026 закрыл четыре оповещения как `not_used`, и все четыре – по проверенной недостижимости, а не по усталости:

| Пакет | Почему недостижимо |
|---|---|
| `glib` (Rust) | `cargo tree --target aarch64-apple-darwin -i glib` не находит его: цепочка `gtk` → `muda` → `tauri` компилируется только на Linux. В DMG не попадает тем более – там крейт `aruna`, от `tauri` не зависящий. Исправления нет и быть не может: `gtk` 0.18.2 закрепляет `glib` 0.18.5, а первая неуязвимая – 0.20.0; сам Dependabot отвечает `security_update_not_possible` |
| `extract-zip` (npm) | Уязвим путь распаковки скачанного браузера у `@puppeteer/browsers`. Драйвер здесь встроенный (`driverProvider: 'embedded'`), браузеры не скачиваются, каталогов загрузки на машине нет. Исправленной версии не существует вовсе |
| `serialize-javascript` (npm, два оповещения) | У `mocha` 10 его подключают только `lib/nodejs/worker.js` и `buffered-worker-pool.js` – параллельный режим, который не включен: `mochaOpts` задает лишь `ui` и `timeout`, `maxInstances` равен единице. Обновлением не лечится: `@wdio/mocha-framework` и в 9.31.5 требует `mocha ^10.3.0`, а `mocha` 10 – `serialize-javascript ^6.0.2` |

Общее у трех последних: это инструменты E2E, они живут в `devDependencies` и в распространяемый артефакт не входят ни при какой сборке.

**`extract-zip` вечером того же дня исчез из дерева.** `@puppeteer/browsers` 3.2.1 отказался от него в пользу `modern-tar`: у 2.13.2 было семь зависимостей (`debug`, `yargs`, `semver`, `tar-fs`, `progress`, `extract-zip`, `proxy-agent`), у 3.2.1 осталось две – `yargs` и `modern-tar`. `@wdio/utils` объявляет `^2.2.0`, поэтому резолвер туда сам не пойдет; переопределение `'@puppeteer/browsers': '3.2.1'` в `frontend/pnpm-workspace.yaml` – тот самый случай исключения из 5.1, и проверяется он по lock-файлу: имени `extract-zip` в `frontend/pnpm-lock.yaml` больше нет ни разу. Совместимость проверена, а не предположена: семь имен, которые импортирует `@wdio/utils/build/node.js` (`install`, `canDownload`, `resolveBuildId`, `detectBrowserPlatform`, `Browser`, `ChromeReleaseChannel`, `computeExecutablePath`), в 3.2.1 на месте; `engines` требует Node >= 22.12.0, локально 22.23.1 и в CI `node-version: "22"`; набор 6.1 и E2E зеленые. Два переопределения, опробованных тогда же – `serialize-javascript` и `deepmerge-ts`, – сняты: они подменяют версию, оставляя пакет в дереве, и ни одного оповещения ими не закрывается.

### 5.3. Граница запрета npm

npm запрещен как менеджер этого проекта: никаких `npm install`, `npm ci`, `npm update`, никаких package-lock.json и npm-shrinkwrap.json, разовые запуски – `pnpm dlx`, а не npx.

Запрет про управление зависимостями проекта, а не про машину: npm остается частью Node-инсталляции и никуда не девается. Инструмент, нужный на машине и не входящий ни в один манифест – языковой сервер, форматер редактора, – ставится глобально через `pnpm add -g`; это тот же pnpm и никакого отношения к запрету не имеет.

### 5.4. Границы безопасности приложения

Каждый зарегистрированный плагин Tauri требует разрешений в capabilities. Типовой сценарий отказа: плагин подключен, а команды падают из-за невыданного разрешения. Часть команд уже покрыта разрешениями ядра (`core:default`).

Правки в tauri.conf.json, capabilities и манифестах не вносятся молча – они меняют границы того, что приложение вправе делать.

Политика содержимого задана: `default-src 'self'` и `connect-src` с каналом IPC приложения. Это запрещает загрузку постороннего кода и обращения наружу, оставляя работать только мост к бекенду. Ослабление политики – отдельное решение с записанной причиной.

До первого релиза остается решить вопрос подписи приложения.

### 5.5. Коммиты

Коммиты разделяются по областям: фронтенд вместе с src-tauri, крейт cli, документация. Смешивать области в одном коммите нельзя. `git add -A` без ограничения путями не применяется.

## 6. Проверки и приемка

### 6.1. Перед коммитом

Из каталога frontend: `pnpm check`, `pnpm lint`, `pnpm format:check`, `pnpm test:unit`.

Из каталога src-tauri: `cargo fmt --check`, `cargo clippy --locked --all-targets --all-features -- -D warnings`, `cargo nextest run --locked --no-tests=pass`. Флаг `--locked` обязателен во всех командах Cargo, включая локальные: он запрещает молчаливое изменение lock-файла и делает результат проверки тем же, что получит выпуск (4.9.9).

При правках консольного крейта – тот же набор из каталога cli. Крейты независимы, поэтому проверяется тот, который затронут; если затронуты оба, набор прогоняется дважды.

Флаг `--no-tests=pass` перестал быть необходимостью – тест в `aruna-desktop` есть (4.9.8) – и остается страховкой на случай, когда в затронутом крейте тестов временно нет.

Здесь есть ловушка, стоящая внимания. Флаг `--all-features` у Clippy включает фичу `e2e`, и это правильно: ветка должна проверяться. Но тот же флаг, добавленный к nextest, включил бы `e2e` и тем самым выключил единственный тест – он проверяет поведение сборки без фичи и под фичей не компилируется. Поэтому nextest запускается без `--all-features`, и это не оплошность, а условие того, что тест вообще выполняется.

E2E в этот набор не входит: цикл со сборкой приложения длинный.

### 6.2. Перед релизом

Набор 6.1 полностью, плюс `pnpm test:e2e`.

Дополнительно по Rust: `cargo audit`, `cargo deny check`, `cargo machete`, `cargo llvm-cov nextest`.

Сборка: `pnpm build` в корне.

Контроль артефакта: релиз собран без фичи `e2e` и без переменной VITE_E2E; отсутствие обоих E2E-плагинов подтверждено по составу зависимостей release-профиля.

### 6.3. Команды

| Команда | Назначение | Признак успеха |
|---|---|---|
| `pnpm dev` | фронтенд в режиме разработки | открывается без ошибок |
| `pnpm tauri dev` | окно приложения в режиме разработки | окно открывается и работает |
| `pnpm build` (frontend) | сборка фронтенда | сборка без ошибок |
| `pnpm build` (корень) | universal-сборка приложения | артефакт создан |
| `pnpm check` | svelte-check плюс tsc по конфигурациям node и e2e | нет ошибок типов |
| `pnpm lint` | ESLint с типизированным слоем | нет ошибок |
| `pnpm format:check` | Prettier без записи | нет замечаний |
| `pnpm test:unit` | Vitest, оба проекта | все тесты прошли |
| `pnpm test:coverage` | Vitest с покрытием | отчет сформирован |
| `pnpm test:e2e` | сборка с фичей e2e и прогон wdio | сценарии прошли, код 0, движок webkit на macos |
| `cargo fmt --check` | форматирование Rust | нет замечаний |
| `cargo clippy --locked --all-targets --all-features -- -D warnings` | линт Rust | нет предупреждений |
| `cargo audit` | уязвимости зависимостей | уязвимостей нет |
| `gh run list`, `gh release view` | состояние сборок и выпусков | сборки зеленые, выпуск на месте |
| `cargo deny check` | политики и лицензии | проверка пройдена |
| `cargo machete` | неиспользуемые зависимости | лишних нет |
| `cargo nextest run --locked --no-tests=pass` | тесты Rust | прошли либо тестов нет |
| `cargo llvm-cov nextest` | покрытие Rust | отчет сформирован |

### 6.4. Ручная проверка

Приложение запускается; окно открывается с заголовком «Aruna» в центре экрана и в размере из конфигурации – в том числе после запуска, в котором окно двигали; размер мышью не меняется; интерфейс отображается, белого экрана и зависаний нет; смена оформления системы доходит до окна без перезапуска; сохраненные данные переживают перезапуск; приложение не падает при обычных действиях. Диалоги и внешние ссылки проверяются, когда появится команда, которая их вызывает (7.3).

Требование о восстановлении размера и положения окна снято 30.08.2026 и заменено обратным. Причина: окно показывает короткое сообщение фиксированного объема, и восстановленный крошечный размер прятал кнопку за нижним краем – случай, который уже происходил. Метка окна внесена в список исключений плагина window-state, поэтому плагин остается подключенным, но на это окно не действует.

### 6.5. Что считается нарушением

Версия компонента из раздела 3 отличается от зафиксированной без записанной причины.

Отсутствует `build.target = 'safari16'`; отсутствует `withGlobalTauri`; заголовок окна не совпадает с заголовком документа.

Не используется svelte-check; не настроены ESLint, Prettier, Vitest, покрытие; типизированный слой снят с кода приложения.

E2E-контур не настроен; E2E-плагины подключены не как optional под фичей `e2e`; их разрешения лежат в сканируемом каталоге capabilities; импорт `@wdio/tauri-plugin` не привязан к VITE_E2E; любой E2E-плагин обнаружен в релизной сборке.

Не проходят Clippy или rustfmt; не настроены cargo-audit, cargo-deny, cargo-machete, cargo-nextest, cargo-llvm-cov. Отсутствие cargo-geiger нарушением не является.

В собственных крейтах отсутствует `#![forbid(unsafe_code)]`; плагины Tauri подключены без разрешений.

Lock-файлы не зафиксированы; в package.json встречается `latest`; в репозитории обнаружены package-lock.json, npm-shrinkwrap.json или следы установки через npm.

В зависимостях обнаружены Playwright или cargo-wizard.

Появились артефакты SvelteKit (перечень в 4.2); использован `/design-sync`.

Ошибки проверок игнорируются без причины.

### 6.6. Если проверка не проходит

Прочитать текст ошибки; определить, какой инструмент ее выдал; найти файл и строку; устранить причину, а не сообщение; повторить проверку. Если проблема осталась – проверить версию зависимости и lock-файл, в том числе на предмет потери поддержки macOS 13. Если решение не найдено – зафиксировать проблему отдельно и найти согласованный обходной путь.

Отключать линтер, тест или проверку безопасности без записанной причины запрещено.

### 6.7. Контур выпуска

Выпуск не делается вручную и не описывается устно – он выполняется сборочным контуром в репозитории, и этот контур такая же часть стека, как локальные проверки.

**Сборочный процесс.** Файл `.github/workflows/release-dmg.yml` содержит шесть заданий: `check`, `desktop`, `corpus`, `frontend`, `macos`, `publish`. Задание `desktop` прогоняет для настольного крейта ровно набор 6.1, то есть локальная батарея и сборочная не расходятся. Задания под macOS выполняются на выделенном образе, остальные на Linux.

**Опорные выпуски.** Файл `.github/reference-release.json` хранит записи о выпусках с коммитом и контрольной суммой артефакта; сборочный контур сверяет их отдельным шагом. Смысл механизма: опубликованный выпуск обязан оставаться тем же, каким его получили читатели. Соответствующие теги защищены правилом репозитория от удаления, перемещения и переписывания истории – проверено попыткой удаления, отказ получен.

**Обновление зависимостей.** В репозитории есть конфигурация автоматических предложений об обновлении. Ее предложения проходят через правило 5.1 наравне с ручными: наличие предложения причиной для обновления не является.

**Инструмент выпуска.** GitHub CLI (`gh`) используется при выпуске – чтение состояния сборок, публикация и перевыпуск, загрузка опубликованного артефакта для сверки. Без него выпуск не делается.

**Состояние на дату редакции.** Выпуск 2.5.0 опубликован 29.08.2026 без подписи приложения. Это осознанное состояние, а не упущение сборочного контура, но условие из 5.4 оно не выполняет – см. 7.3.

### 6.8. Критерий приемки окружения

Разделы 2 и 3 соответствуют заявленным статусам; правила разделов 4 и 5 выполняются; наборы 6.1 и 6.2 проходят; E2E зеленый. На дату редакции выполнено. Позиции со статусом «ожидает» относятся к этапу разработки кода и приемке окружения не препятствуют.

## 7. Приложения

### 7.1. Журнал отклонений реальной сборки

Записан, чтобы при повторном развертывании не проходить те же ловушки заново.

**Каркас и зависимости.** `pnpm init` вместо шаблона create-vite порождает конфликт полей packageManager и devEngines и не дает структуры проекта. `pnpm add -E` не переписывает диапазоны у пакетов, уже присутствующих в манифесте – семь версий пришлось проставлять вручную. В pnpm 11 поле «pnpm» в package.json не читается, настройки живут в pnpm-workspace.yaml, а автогенерированная заготовка этого файла содержит плейсхолдеры и требует заполнения.

**Тесты.** В jsdom-окружении Svelte 5 резолвится в серверную сборку – лечится `resolve.conditions: ['browser']`. Опция `hot` у vite-plugin-svelte 7 отсутствует. В покрытие попадают бинарные ассеты и стили, если не сузить маску до кода. Пустой набор Rust-тестов дает у nextest код возврата 4.

**Tauri.** Имя крейта из шаблона – app; переименование затрагивает три места (имя пакета, имя lib, вызов в main.rs), а BSD sed на macOS не поддерживает границы слова. Файл capability, ссылающийся на плагины под фичей, ломает сборку без фичи, поскольку build.rs проверяет разрешения на этапе компиляции. Два глобальных логгера в одном процессе несовместимы. `cargo build` дает `cfg(dev)` и загрузку devUrl – для E2E нужна сборка через Tauri CLI. Мост wdio требует одновременно импорта фронтенд-плагина, `withGlobalTauri` и корректного контекста в тесте. Сервис ищет окно по заголовку документа, а не по конфигурации.

**Линт.** Набор `recommendedTypeChecked` подменяет парсер компонентов, поэтому для `.svelte` парсер объявляется заново; файлы вне tsconfig.app.json в типизированный слой не берутся.

**Процедурные ловушки.** Резервная копия с именем без метки времени затирается при повторном прогоне блока – так были потеряны исходные Cargo.toml и eslint.config.js. Терминальные вставки визуально съедают пустые строки, поэтому судить о структуре файла по выводу нельзя, нужна нумерация строк.

### 7.2. План обновления и список наблюдения

Ревизия от 30.08.2026 – сплошная, по шести контурам: npm-пакеты фронтенда, корня и глобальные, крейты обоих Rust-крейтов, cargo-инструменты, базовое окружение. Все cargo-инструменты и tauri-cli подтверждены последними опубликованными версиями; Claude Code 2.1.251, GitHub CLI 2.97.0 – тоже последние.

**Запрещены постоянно, независимо от выхода новых версий.** `@types/node` – линия обязана совпадать с рантаймом Node 22; двадцать шестая линия (на дату ревизии 26.4.0) описывает API, которых в 22 нет, отчего проверка типов пройдет, а исполнение упадет. TypeScript 7 – у него нет программного API, на котором держится typescript-eslint; разблокируется не выходом версии, а появлением API в 7.1 и его поддержкой в линт-контуре. То же касается глобального typescript, приходящего с языковым сервером.

**Пачка установлена 30.08.2026.** Четыре пакета подняты одной командой, набор 6.1 прогнан целиком и зеленый: svelte-check 0 ошибок, ESLint без замечаний, Prettier без замечаний, 53 теста в семи файлах на тот час – к вечеру их 58 в восьми, – rustfmt и Clippy чисты, тест настольного крейта проходит.

| Пакет | Переход | Публикация | Основание |
|---|---|---|---|
| ESLint | 10.9.0 → 10.9.1 | 24.08.2026 | патч чинит ложные срабатывания правила no-loss-of-precision, внесенные в 10.9.0, то есть регрессия присутствовала в нашей версии |
| `typescript-eslint` | 8.67.0 → 8.68.0 | 24.08.2026 | исправления no-floating-promises и no-empty-object-type; новых правил нет |
| Svelte | 5.56.9 → 5.56.10 | 20.08.2026 | шесть исправлений без новых возможностей; ключевое для настольного приложения – освобождение ссылки на последнее событие после всплытия, которое иначе удерживает поддерево целевого элемента; плюс два перехода от квадратичного времени к линейному |
| `@testing-library/user-event` | 14.6.5 → 14.6.6 | 22.08.2026 | pointerType синтетических событий по умолчанию становится пустой строкой вместо строки «undefined» |

Обновление Svelte дополнительно подтверждено прогоном E2E: собранное приложение открылось в системном WKWebView, оба сценария прошли, код возврата 0. Проверки типов и модульные тесты работают с исходниками, поэтому для изменений компилятора шаблонов подтверждением служит только прогон собранного приложения.

**Следующая версия Svelte – 5.57.0, опубликована 28.08.2026.** Это минорная версия, то есть новое поведение, а не только починка. К минорным применяется более строгий порядок: релиз-ноты читаются целиком, обновление идет отдельно от пачки патчей, после него прогоняется не только набор 6.1, но и E2E из 6.2. Раньше 04.09.2026 к ней не возвращаемся.

**pnpm 11.24.0** – менеджер сообщает о доступном обновлении с текущей 11.22.0. Версия менеджера удерживается полем `packageManager` и меняется командой corepack, то есть это правка манифеста, затрагивающая всех участников. Правило 5.1 действует и здесь: нужны релиз-ноты и выдержка. Кандидат на следующую ревизию.

**Отложено сознательно.**

Контур wdio (`@wdio/cli` и `@wdio/local-runner` до 9.31.4, `@wdio/globals` до 9.31.3) – самая хрупкая часть окружения, она работает, а подъем версий не устраняет известное расхождение peer-зависимости: сервис Tauri тянет собственную, более старую линию `@wdio/globals`. Трогать имеет смысл при появлении конкретной поломки или при обновлении самого сервиса.

Транзитивные крейты обоих Rust-крейтов не поднимаются: текущие lock-файлы описывают ровно тот набор, на котором прошли сборка, clippy, аудит, тесты и E2E, а причины у обновления нет – `cargo audit` уязвимостей не находит. В консольном крейте таких крейтов около двадцати, в настольном около десяти; длина списка сама по себе основанием не является.

Отдельно – три прямые зависимости консольного крейта, где доступен переход через мажорную границу: `dirs` 5 → 6, `ureq` 2.12 → 3, `zip` 2.4 → 8. Это не обновления, а миграции: меняется интерфейс библиотеки, и каждая требует чтения руководства по переходу, правки кода и полного прогона тестов крейта. Рассматриваются по одной и только при наличии причины – например, если в текущей линии перестанут выпускать исправления безопасности.

Отдельное наблюдение: число крейтов, обновляемых внутри semver, растет от ревизии к ревизии. Причина не измерялась – связывать этот рост с отсутствием поля `rust-version` было бы догадкой, а не выводом. На решения он не влияет: список отложенных обновлений ведется по существу каждого крейта, а не по его длине.

Ближайшие точки: не раньше 04.09.2026 – Svelte 5.57.0 и pnpm 11.24.0, каждая отдельным шагом; далее по появлению версии с причиной.

### 7.3. Открытые позиции

| Позиция | Что требуется | Раздел |
|---|---|---|
| Что распространяется пользователю | образ для выпуска собирается из ядра: двойной клик по нему отрабатывает молча, вывод только в терминале. Окно, показывающее результат, теперь существует – решение, собирать ли образ из оболочки, затрагивает скрипт сборки, сборочный контур, механизм опорных выпусков и подпись | 3.6, 6.7 |
| Подпись приложения | выпуск 2.5.0 опубликован без подписи. Условие «решить до первого релиза» уже не достижимо – заменяется на достижимое: подпись обязательна для первого выпуска, который предлагается устанавливать читателю без обхода защиты системы | 5.4 |
| Решение по единственному контуру логирования | выбрать tracing либо tauri-plugin-log | 4.6 |
| Проверка плагинов Tauri в работе | `dialog` и `opener` получили потребителей 02.09.2026 – выбор архива и «Открыть опись», – и проверяются в работе. Остается `store`: команды, которая обращалась бы к нему, по-прежнему нет | 6.4 |
| Компонент `rust-src` | не установлен. Нужен только для перехода в исходники стандартной библиотеки из языкового сервера. Либо поставить одной командой, либо оставить как есть – решение за владельцем | 3.1 |
| `@testing-library/user-event` | потребитель есть с 31.08.2026: проверка, разыгрывающая ввод, – табуляция до единственной кнопки и нажатие ее клавишей. Условие пересмотра, записанное 29.08, сработало | 3.3 |
| Четыре пакета JS-половин плагинов Tauri | в манифесте есть, импортов нет; пересматриваются вместе с первым кодом, который вызовет соответствующий плагин | 3.2 |

Закрыто 02.09.2026: типобезопасные команды – specta и tauri-specta подключены с точным пином, типы окна порождаются из Rust (3.5).

Закрыто в редакции 9, по сверке от 30.08.2026: фиксация изменений в Git (дерево зафиксировано пятью коммитами по областям и отправлено в origin), ручная проверка окна (выполнена 29.08; переделана 30.08 под новое требование 6.4 – центрирование и фиксированный размер вместо восстановления), обработка `--help` и `--version` (реализована), парная строка про политику содержимого и подпись (политика задана, подпись выделена в отдельную позицию), отсутствие описания контура выпуска (раздел 6.7), отсутствие `cli/Cargo.lock` в перечне lock-файлов, отсутствие флага `--locked` в командах Rust, неназванные пакеты JS-половин плагинов и `gh`.

Закрыто в редакции 7: совпадение имен пакетов, объявление версии компилятора одним местом, профили сборки, стратегия паники, политика содержимого, отсутствие тестов Rust.

### 7.4. Отключенное и неиспользуемое

Отдельная таблица, чтобы отключенное не превращалось в забытое. Каждая строка имеет условие возврата или удаления; строка без условия – дефект документа.

| Что | Состояние | Условие пересмотра |
|---|---|---|
| Расширение `github` | отключено | настройка токена с минимальными правами |
| Расширение `code-review` | отключено | переход на процесс с пул-реквестами |
| Расширение `security-guidance` | отключено | этап кода, обрабатывающего внешние данные |
| Сборочные скрипты `edgedriver`, `geckodriver` | запрещены в pnpm-workspace.yaml | не пересматриваются: драйверы Edge и Firefox проекту не нужны |
| Сборочный скрипт `esbuild` | запрещен там же | не требуется: платформенный бинарник приходит отдельным пакетом |
| `@testing-library/user-event` | установлен, потребителя нет | тест, разыгрывающий ввод; `svelte` и `jest-dom` потребителя получили |
| `@tauri-apps/plugin-store`, `-window-state` | установлены, импортов нет | первый код, вызывающий соответствующий плагин. `dialog` и `opener` вышли из этой строки 02.09.2026: первый открывает выбор архива, второй – собранную опись |
| Тест целостности пакета в ядре | помечен пропускаемым | запускается отдельно, когда нужна полная проверка записи и чтения пакета |
| `rust-src` | не установлен | нужен переход в исходники стандартной библиотеки из языкового сервера |
| Строка `Apache-2.0 WITH LLVM-exception` в deny.toml | лицензия разрешена, в дереве отсутствует | убирается при следующей правке файла |
| `tracing`, `specta`, `tauri-specta` | не подключены | 4.6 и 3.5 соответственно |
| cargo-geiger, cargo-wizard, cargo-outdated, sccache, Playwright, SvelteKit, `tauri-plugin-shell`, `/design-sync` | исключены | основания в 3.4, 4.2, 4.4, 3.5, 4.8; пересмотру не подлежат без изменения обстоятельств |

### 7.5. Слабые места, названные прямо

Аудит 30.08.2026. Список не ставит задач и не требует действий – он существует, чтобы никто не принял зеленые проверки за доказательство того, чего они не проверяют.

**Оболочка покрыта настолько, насколько это возможно без окна.** Пять тестов и 70,83 % регионов: разыграны ветки команды, читающей счетчики, и требование о чистоте релизной сборки. Непокрытым остается то, что требует настоящего окна, – регистрация плагинов и точка входа.

**E2E проверяет два сценария.** Открытие окна с реакцией на клик и достижимость бекенда. Этого хватает как измерительного прибора для движка, но это не проверка интерфейса: ни одна ветка окна, кроме успешной, не разыграна – ни отсутствие пакета, ни отказ команды.

**Диалоги, внешние ссылки и хранилище не проверены в работе.** Плагины подключены, разрешения выданы, но вызвать их нечем. До первой такой команды соответствующие пункты приемки невыполнимы – и это записано честно, а не закрыто галочкой.

**Типы между Rust и TypeScript не связаны ничем.** Пока нет `specta`, соответствие структуры `CorpusLocation` и одноименного типа во фронтенде держится на внимательности: переименование поля в Rust не поймает ни компилятор, ни svelte-check.

**Приложение не подписано.** Выпуск 2.5.0 опубликован без подписи, и защита системы откажет читателю при первом открытии.

**Пользователь пока получает не окно.** Образ для распространения собирается из ядра, двойной клик по нему отрабатывает молча. Окно существует и решает эту проблему, но в выпуск не входит.

**Часть проверок держится на одной машине.** Воспроизводимость пакета, ручной осмотр окна, прогон E2E – все это выполняется здесь. Сборочный контур повторяет набор проверок, но не воспроизводимость и не осмотр.

### 7.6. Соответствие прежней нумерации

Редакции 2–4 ссылались на разделы девятой части. Соответствие для внешних ссылок, включая CLAUDE.md:

| Прежний номер | Текущий раздел |
|---|---|
| 9.4.3 (правило обновления) | 5.1 |
| 9.6 (механика E2E) | 4.4 |
| 9.6.5 (запрет для релиза) | 4.5 |
| 9.8.0 (разрешения плагинов) | 5.4 |
| 9.12 (набор перед коммитом) | 6.1 |
| 9.13 (набор перед релизом) | 6.2 |
| 9.16 (нарушения) | 6.5 |
| 9.20 (журнал отклонений) | 7.1 |
| 9.22 (план обновления) | 7.2 |
| 9.23 (контур проектирования) | 4.8 |
