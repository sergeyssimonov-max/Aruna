[package]
name = "aruna-desktop"
version = "0.1.0"
description = "Aruna — the TLHdig Hittite cuneiform corpus, as a desktop application"
authors = ["Sergey Simonov"]
license = "MIT"
repository = "https://github.com/sergeyssimonov-max/Aruna"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "aruna_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
log = "0.4"
tauri = { version = "2.11.3", features = [] }
tauri-plugin-log = "2"
tauri-plugin-dialog = "2.7.2"
tauri-plugin-opener = "2.5.4"
tauri-plugin-window-state = "2.4.1"
tauri-plugin-store = "2.4.4"
tauri-plugin-wdio-webdriver = { version = "1.3.0", optional = true }
tauri-plugin-wdio = { version = "1.3.0", optional = true }

[features]
e2e = ["dep:tauri-plugin-wdio-webdriver", "dep:tauri-plugin-wdio"]

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
# Без `panic = "abort"`: паника обязана печатать место, где она произошла.
# В крейте cli отказ от разворачивания стека однажды превратил ошибку разбора
# в молчаливое падение без сообщения для пользователя.

[profile.dev]
opt-level = 1
