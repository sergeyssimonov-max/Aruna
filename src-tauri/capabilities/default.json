{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "what the window itself calls: the core, and the folder picker",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
