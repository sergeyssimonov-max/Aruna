# Стратегия производительности

## Принципы (в порядке приоритета)

1. **Корректность и читаемость важнее скорости.**  
   Любая оптимизация, которую нельзя легко объяснить и измерить, подлежит удалению.

2. **Сначала измеряй, потом оптимизируй.**  
   Никаких изменений «потому что так быстрее» без цифр до/после.

3. **Минимальный стек.**  
   Стандартная библиотека + `memchr` там, где это реально помогает.  
   Новые зависимости ради микрооптимизаций запрещены.

4. **Параллелизм — только по делу.**  
   Rayon и подобное разрешены только при доказанном ускорении ≥ 1.5–2× на реальной нагрузке.  
   (ZIP-распаковка занимает ~73 % прогона и не параллелится через один `ZipArchive`.
   Разбор – остальные ~25 %, так что даже мгновенный разбор дал бы 1.3×. Rayon убран.)

5. **`unsafe` — только на границе.**  
   Допустим исключительно на FFI / WASM ABI и только с полными `// SAFETY:` комментариями.  
   `unsafe` «на всякий случай» или ради микрооптимизаций запрещён.

6. **Публичный API не ломаем** без явного согласования.

## Как измерять

- Полный прогон – `export_beta` на fixture ZIP под `/usr/bin/time -l`, выход
  в отдельный каталог (без второго аргумента пакет ляжет в `~/Downloads`).
  Сравнивать по строке `instructions retired`: на семи прогонах она расходится
  на 0,2 %, wall-clock – на 3–4 %, пик RSS – на 10–17 %. Правку памяти слабее
  четверти `/usr/bin/time` не различает; для нее нужен `heap(1)`.
- По стадиям (`read` / `parse` / `sort` / `render`) — `cli/examples/bench_parse.rs`,
  min-median-max по `ARUNA_BENCH_RUNS` прогонам, без сторонних зависимостей:
  `cargo run --release --example bench_parse -- fixtures/…zip`.
- Разбор отдельно — `cli/examples/bench_fields.rs`: архив читается один раз,
  дальше измеряются только нарезка окон (`Windows::of`) и чтение полей
  (`parse_windows`) на уже подготовленных окнах. `ARUNA_BENCH_WINDOW` обрезает
  окна — это и отвечает на вопрос, за чем следует цена: за байтами или за
  документами.
- Сортировка отдельно — `cli/examples/bench_order.rs`: архив читается один раз,
  дальше измеряется только `order::sort_records`, на свежей копии каждый прогон.
  Стоимость копии печатается отдельной строкой и в результат не входит;
  `ARUNA_BENCH_WARMUP` прогонов не записываются.
- Пиковая память — `/usr/bin/time -l` (macOS) или `-v` (Linux) вокруг команды.
- Поиск в описи – `frontend/scripts/bench-search.ts` на собранном в песочнице
  пакете: `node scripts/bench-search.ts <каталог пакета> [chrome|webkit|both]`
  из `frontend/`. Разбор – в разделе «Поиск в браузере».

Измерительный код в программе не исполняется: конвейер обобщён по `Probe`, и
`parse_zip` монодоморфизируется с реализацией без часов, тогда как
`parse_zip_timed` — с той, что считает. Стадия закрывается там, где работа
действительно заканчивается: если сдвинуть отметку, стадия «подешевеет», не
изменив программу. Это ровно та ошибка, на которой уже один раз получили
мнимые −110 мс.

## Текущий профиль

Замеры на `cli/fixtures/TLHbasisONLINE25_1_ZENODO_Beta_03.zip` (23 936 манускриптов),
release, 9 прогонов, медиана, Apple M1 Max. Перемерено 2026-09-26 на `69c2a9c`,
после перехода чтения архива на буфер 8 КиБ (`ea6bde1`). Сеть и запись файла сюда
не входят.

| Стадия          | Медиана   | Доля  |
|-----------------|-----------|-------|
| read (zip)      | 753 мс    | 73 %  |
| parse           | 254 мс    | 25 %  |
| sort            | 5 мс      | 0.5 % |
| render html     | 14 мс     | 1.4 % |
| **итого**       | 1027 мс   |       |

**Прежняя таблица мерила не только распаковку.** До `ea6bde1` архив читался
через `BufReader` на 256 КиБ, а zip переходит к каждой записи через
`SeekFrom::Start`, после чего буфер заливается заново целиком – около 72 тыс.
заливок и 18 ГиБ копирования за прогон при записях в среднем по 3 КиБ. Из 928 мс
стадии read около 175 мс были этими заливками. Замер 24.09.2026 на `81b9dc1`:
read 928, parse 255, итого 1202 мс; 14,9 млрд инструкций на один проход.

Разбор идёт потоком: запись разбирается и окно освобождается до чтения
следующей, поэтому «read» и «parse» замеряются внутри одного прохода, а не
вокруг двух. Пиковый RSS одного прохода – 38,0–43,2 МиБ (три прогона 26.09.2026,
footprint 27,9–29,0 МиБ); 9,24 млрд инструкций – против 14,9 млрд с буфером 256 КиБ. Прогон `bench_parse` с
девятью повторами показывает 84 МиБ, поэтому пик одного прохода меряется
запуском с `ARUNA_BENCH_RUNS=1`.

### Из чего состоит `parse`

Замерено `bench_fields` на трех размерах окна (16 / 4 / 1 КиБ). Первые столбцы –
замер до `6dedc9b`, который и привел к правке ниже; последние – 2026-09-24, после
нее, девять прогонов, медиана.

| Окно | Байт окон | Чтение полей, до | Пропускная, до | Чтение полей, 24.09 | Пропускная, 24.09 |
|------|----------:|-----------------:|---------------:|--------------------:|------------------:|
| 16 КиБ | 173,8 МиБ | 325,9 мс | 533 МиБ/с | 223,4 мс | 778 МиБ/с |
| 4 КиБ  |  80,9 МиБ | 198,8 мс | 407 МиБ/с | 144,6 мс | 559 МиБ/с |
| 1 КиБ  |  23,4 МиБ |  91,2 мс | 256 МиБ/с |  73,0 мс | 320 МиБ/с |

Линейная подгонка по крайним точкам до правки: **1,56 мс на МиБ окон** плюс
**54,7 мс** постоянных на 23 936 документов – примерно **83 % цены разбора
следовало за байтами окна** и **17 % – за числом документов** (2,3 мкс на
запись: аллокации полей и сборка записи).

Та же подгонка 2026-09-24: **1,00 мс на МиБ** плюс **49,6 мс** постоянных
(2,1 мкс на запись) – за байтами теперь **78 %** цены. Прямая идет через крайние
точки, средняя лежит выше нее на 14 мс (до правки – на 18): зависимость от
окна не строго линейна, и коэффициенты – оценка, а не закон.

Предельная скорость сканирования до правки выходила 641 МиБ/с при том, что
нарезка окон – один проход – идет на 16 207 МиБ/с. Разрыв в 25 раз означал, что
окно обходится многократно. После правки – 1 000 МиБ/с против 15 427, разрыв в
15 раз: семь извлекателей по-прежнему идут по окну каждый своим проходом.

### Кто именно обходил

`bench_fields --features bench` меряет каждый извлекатель по отдельности, в том
порядке и с теми же условиями, в каких их вызывает `parse_windows`. Оказалось,
что дело не во всех семи, а в одном:

| Извлекатель   | Было     | Стало   | 2026-09-24 |
|---------------|---------:|--------:|-----------:|
| `inv`         | 212,5 мс | 93,3 мс | 99,6 мс |
| `lang`        |  64,4 мс | 63,1 мс | 73,8 мс |
| `editor+year` |  35,2 мс | 34,5 мс | 36,9 мс |
| `sigla`       |   3,9 мс |  3,8 мс |  4,3 мс |
| year fallback |   2,2 мс |  2,2 мс |  2,3 мс |
| `corpus`      |   1,1 мс |  1,1 мс |  1,2 мс |
| `cth`         |   0,8 мс |  0,8 мс |  0,9 мс |
| **разбор**    | **325 мс** | **204 мс** | **223 мс** |

Последний столбец – перемер 2026-09-24 (`81b9dc1`, девять прогонов, окно
16 КиБ); сумма медиан извлекателей – 218,9 мс. Правка `inv` держится: он
по-прежнему вдвое дешевле, чем был.

`extract_inv` искал элемент шестью вызовами `find_open_tag` — два окна на три
написания, — а каждый такой вызов обходит *все* теги стога. Два из шести были
заведомо лишними: сравнение имён регистронезависимое, поэтому `InvNr` и `invNr`
— один и тот же вопрос. И поле есть лишь у 11,6 % записей, так что 88 %
документов прогоняли все шесть проходов до конца.

Заменено на `xml_scan::find_open_tags` — один обход стога, первое вхождение для
каждого имени сразу. Порядок предпочтений сохранён: выигрывает первый элемент
предпочтительного написания, и только если он пуст, очередь переходит
следующему. Каталог из 23 936 записей после замены байт в байт прежний.

`sort` вынесен из `parse` отдельной стадией: это разные затраты с разными
причинами — одна растёт с архивом, другая с описью, — и в сумме ни одну из них
не было видно.

Воспроизводится тремя командами:

```
cargo run --release --example bench_parse -- cli/fixtures/…zip
cargo run --release --example bench_order -- cli/fixtures/…zip
cargo run --release --features bench --example bench_fields -- cli/fixtures/…zip
```

## Поиск в браузере

Поиск по описи – `frontend/src/inventory/filter.ts`. Vite собирает его в
`cli/src/generated/inventory_filter.js`, и крейт встраивает скрипт в
`TLHdig_Beta_0.3.html`. Работает он в том браузере, которым читатель открыл
пакет, а в окне приложения не выполняется вовсе. Это обычный скрипт страницы:
на каждое событие `input` он ищет подстроку по заранее собранным текстам строк
и ставит или снимает `hidden` у всех 24 599 строк таблицы (663 заголовка групп и
23 936 манускриптов). Без воркера, без WASM, без виртуального списка.

### Замер 2026-09-24

`81b9dc1`, пакет из `export_beta` в песочнице, Apple M1 Max, macOS 13.7.8, хост
свободен. Оснастка – `frontend/scripts/bench-search.ts`: она кладет рядом с описью
копию страницы со скриптом замера, так что шрифт и ссылки разрешаются как у
оригинала. Запрос вводится в поле, и посылается событие `input` – ровно то, что
слушает фильтр; нажатий клавиш нет. Каждый повтор начинается с полной таблицы:
поле сначала очищается, и очистка меряется отдельной строкой. Три разогрева,
25 повторов, медиана и p95.

- **Chrome 153.0.8010.53** – отдельный безоконный процесс (`--headless=new`) с
  временным профилем; открытый на машине Chrome не затрагивается. Шаг часов –
  0,1 мс. Кадры рисуются.
- **WebKit** – системный WKWebView (`bench-search.swift`), тот же движок, на
  котором здесь работает Safari 18.6. Окна нет, поэтому кадров нет, а шаг часов –
  1 мс. Нижняя граница цели сборки – `safari16`, но на этой машине стоит 18.6.

Три числа от ввода: **обработчик** – сам фильтр; **+раскладка** – плюс
принудительный пересчет стилей и раскладки (`offsetHeight`); **+кадр** – до
первого кадра после этого, то есть с отрисовкой.

| Запрос | Групп | Строк | Chrome: обработчик | Chrome: +раскладка | Chrome: +кадр | WebKit: обработчик | WebKit: +раскладка |
|---|---:|---:|---:|---:|---:|---:|---:|
| `k` | 650 | 18 569 | 4,5 мс | 90 мс | 250 мс | 2 мс | 153 мс |
| `kbo` | 555 | 14 029 | 4,9 мс | 101 мс | 227 мс | 3 мс | 249 мс |
| `hit` | 602 | 21 799 | 4,4 мс | 87 мс | 278 мс | 2 мс | 96 мс |
| `schwemer` | 50 | 91 | 6,3 мс | 132 мс | 140 мс | 5 мс | 546 мс |
| `2021` | 58 | 753 | 6,5 мс | 131 мс | 142 мс | 5 мс | 532 мс |
| `cth 547` | 1 | 62 | 6,3 мс | 133 мс | 139 мс | 5 мс | 549 мс |
| `zzzznope` | 0 | 0 | 6,2 мс | 132 мс | 138 мс | 6 мс | 550 мс |
| очистка поля | 663 | 23 936 | 4,5 мс | 562 мс | **770 мс** (p95 898) | 2 мс | **2 256 мс** (p95 6 040) |

**Цена поиска – не поиск.** Сам фильтр стоит 2–7 мс в обоих движках. Все
остальное – пересчет стилей и раскладки таблицы после того, как у строк сменился
`hidden`. В Chrome ответ на запрос виден через 140–280 мс, в WebKit одна только
раскладка занимает 0,1–0,55 с. Возврат полной таблицы после очистки поля –
самое дорогое: 0,77 с до кадра в Chrome и 2,3 с раскладки в WebKit, в худшем
из 25 повторов – 6,0 с. Прогон WebKit целиком шел 11 минут против 3 у Chrome.

**Оговорки.** Chrome мерился без окна, WebKit – без окна и без кадров: в
настоящем Safari к раскладке добавится отрисовка, так что его числа – скорее
нижняя граница. Одна машина, один прогон на движок.

### Что пробовали 2026-09-25, и почему отложено

Та же оснастка, Chrome, пакет из `export_beta` на `365beb5` (сумма та же,
`83392544…3dc99`). Правки вносились в копию страницы в песочнице, в дерево не
попадали. Смотрели очистку поля – самый дорогой случай.

| Вариант | Простой хоста | +раскладка | +кадр |
|---|---:|---:|---:|
| как есть | 96 % | 562 мс | 769 мс |
| не трогать строки, чья видимость не меняется | 77 % | 568 мс | 773 мс |
| ячейки без переноса (`white-space: nowrap`) | 94 % | 565 мс | 772 мс |

Оба варианта отвергнуты: разницы нет. При очистке видимость меняется у всех
строк, так что пропускать нечего, а перенос строк в цене раскладки не участвует.
Цена – само число строк в одной таблице. `content-visibility` на строки таблицы не
действует: спецификация сдерживания исключает внутренние элементы таблицы.
Помогла бы опись из отдельной таблицы на каждую группу CTH, но это правка
разметки в крейте, и она двигает сумму пакета, задевает доступность (663 таблицы
вместо одной), печать и тесты описи. Решение владельца от 25.09.2026: отложить
до выпуска после 2.6.1.

### Историческое: поиск снятого сайта

Chrome на macOS, dev-сервер, настоящая опись (23 936 манускриптов). Это поиск
веб-сайта, удаленного 2026-08-23 в выпуске 2.1.0 (`684dd8a`): React, виртуальный
список, WASM-движок в воркере. Метрика другая – задержка ответа воркера
`postMessage` → `result`, медиана из 25 повторов, – поэтому с таблицей выше она не
сравнивается.

| Запрос     | Совпадений | Медиана |
|------------|-----------:|--------:|
| `k`        | 650 групп  | 0.7 мс  |
| `kbo`      | 555 групп  | 0.7 мс  |
| `hit`      | 591 группа | 0.9 мс  |
| `schwemer` | 50 групп   | 0.4 мс  |
| `zzzznope` | 0          | 0.4 мс  |

Сквозная задержка от нажатия до обновлённого счётчика в шапке — 18–27 мс
медианы: это отрисовка списка, а не поиск. Виртуальный список рисовал только
видимые строки, а нынешняя опись держит в документе все 24 599.

Скрипты тех замеров лежали вне репозитория и обращались к
`/src/lib/search.worker.ts` и `/src/data/inventory.bin.gz`; обоих файлов больше
нет, и числа этой таблицы не воспроизводятся.

## Что уже решено замерами

| Место              | Результат                          | Решение          |
|--------------------|------------------------------------|------------------|
| Rayon в archive.rs | parse 115 мс без него против 18 мс с ним; на полном прогоне 1495 мс против 1389 мс — 1.07×. На нынешнем профиле разбор дороже, но потолок всё равно 1.21×: столько дал бы разбор ценой в ноль | Удалён: ниже порога 1.5–2× из принципа 4 |
| ZIP-распаковка     | 753 мс из 1027 мс (73 %, замер 26.09.2026; прежде 887 из 1229 с буфером 256 КиБ), один `ZipArchive` не читается параллельно | Оставляем как есть |
| Поиск `AO:InvNr` вне заголовка | parse 120 мс → 294 мс: номер лежит в теле документа, и без просмотра широкого окна колонка была пуста у всех 24 000 записей | Оставлено: пустая колонка хуже 10 % прогона |
| `PoolHits`: маска по размеру пула вместо одного `u64` | Задержка ответа не изменилась: медианы совпали до 0.1 мс на восьми запросах (`k` 0.7, `kbo` 0.7, `hit` 0.9, `schwemer` 0.4, `zzzznope` 0.4 — одинаково до и после). Замерено переключением модуля на прежнюю версию и обратно | Оставлено: потолок пула стал 255 бесплатно |
| Один обход разметки вместо трёх (`xml_scan::Tags`) | parse 291 → 328 мс, полный прогон 1668 → 1698 мс (+1.8 %). Каталог из 23 936 записей — байт в байт прежний | Оставлено: три копии цикла успели разойтись между собой, принцип 1 ставит читаемость выше 2 % |
| Потоковый разбор вместо сбора корпуса в память | Пик памяти 231 → 48 МБ, полный прогон 1.78 → 1.71 с (три прогона подряд каждой версии). Каталог байт в байт прежний | Сделано ради надёжности: память больше не растёт вместе с архивом |
| simd_scan → xml_scan | Читаемость + честное имя         | Выполнено        |

Числа в этом файле относятся к сборке, на которой сняты, и устаревают вместе с
парсером — доля ZIP была 91 %, пока разбор не начал искать инвентарный номер в
теле документа. Правя профиль, перезамеряйте, а не пересчитывайте.

## Правило для будущих изменений

Любая новая «оптимизация» обязана иметь:
- цифры до/после,
- краткое обоснование,
- отсутствие заметного ухудшения читаемости.

Иначе — откат.

---

## Базовые показатели этапа тестирования (2026-08-20)

Сняты на TLHdig Beta 0.3 (23 936 документов, 339.5 МБ текста), macOS 13,
Apple Silicon, release-профиль. Команды — в [docs/TESTING.md](docs/TESTING.md).
Это отправная точка для будущих бюджетов, а не сами бюджеты: назначать порог
раньше, чем есть baseline, значит закреплять случайное число.

### Конвейер описи — `bench_parse`

| стадия | min | median | max |
|---|---|---|---|
| чтение ZIP | 870.3 мс | 871.0 мс | 872.6 мс |
| разбор (24k документов) | 218.0 мс | 219.0 мс | 219.2 мс |
| сортировка | 4.0 мс | 4.4 мс | 5.0 мс |
| рендер HTML | 6.2 мс | 6.3 мс | 6.4 мс |
| **итого** | **1098.5 мс** | | |

### Сборка пакета – `bench_export`

| стадия | значение |
|---|---|
| заголовки (проход 1) | 1600.5 мс |
| размещение 24k документов | 9.3 мс |
| рендер описи | 13.4 мс |
| полная сборка с записью 389 МБ | 6085.8 мс |

Пять прогонов `export_beta` целиком: **user CPU 2.70–2.79 с** (величина, пригодная
для сравнения), real 8.6–10.7 с, пик RSS ~100 МиБ. Real time за сессию менялся от
7.5 до 10.7 с при неизменном коде — это системное время файловой системы, и по нему
регрессии не отслеживаются.

### Отдельные операции

| операция | значение |
|---|---|
| MD5 71 МиБ из памяти | 194.5 мс (364 МиБ/с) |
| MD5 71 МиБ с диска | 204.4 мс (347 МиБ/с) |
| сортировка 24k записей | 3.22 мс |
| подсчёт блоков Unicode по 309 млн кодовых точек | 307 мс |
| структурная инвентаризация всего корпуса | 6.01 с user, 42 МиБ RSS |

### Ограничения этих измерений

- `bench_export` для стадии «normalise» держит 30.1 МиБ документов в памяти и
  повторяет проход по ним: данные полностью помещаются в кэш, поэтому названная
  пропускная способность (~34 ГиБ/с) относится к прогретому кэшу, а не к рабочей
  нагрузке, где каждый документ читается из архива один раз.
- Одна машина, один запуск подряд. Ни p99, ни распределение по холодному старту
  не снимались.
- Число аллокаций, дескрипторов и объём ввода-вывода не измерялись: в проекте нет
  для этого инструментов, а добавлять их без задачи преждевременно.

---

## Перемер 2026-09-24

Тот же корпус (23 936 документов, 339,5 МБ текста), `81b9dc1`, Apple M1 Max,
macOS 13.7.8, rustc 1.97.1, release-профиль. Хост свободен: простой 83–89 % по
`top -l 1` перед каждой командой. `HOME`, кеш и временный каталог – в песочнице.
Раздел выше не тронут, чтобы разница читалась.

Сравнивается не один и тот же код: с августа сборка пакета проверяет шрифты,
классифицирует каждый документ, сверяет опубликованную копию и снимает прежнюю.
Поэтому рост user CPU – это прибавившаяся работа, а не регрессия.

### Конвейер описи – `bench_parse`, 9 прогонов

| стадия | min | median | max |
|---|---|---|---|
| чтение ZIP | 924.7 мс | 927.5 мс | 935.1 мс |
| разбор (24k документов) | 253.7 мс | 255.2 мс | 258.4 мс |
| сортировка | 5.0 мс | 5.2 мс | 6.4 мс |
| рендер HTML | 13.8 мс | 14.0 мс | 14.6 мс |
| **итого** | **1197.1 мс** | | |

Рендер вырос с 6,3 до 14,0 мс вместе с описью: HTML теперь 3 643 КиБ.

### Сборка пакета – `bench_export`

| стадия | значение |
|---|---|
| заголовки (проход 1) | 1673.0 мс |
| размещение 24k документов | 11.3 мс |
| рендер описи | 24.5 мс |
| полная сборка с записью | 7003.4 мс |

### Полный прогон – `export_beta`, по 7 прогонов

«Холодный» – в пустой каталог, «повторный» – поверх готового пакета: тогда
прогон еще снимает прежнюю копию.

| | инструкций | user | sys | real | пик RSS |
|---|---|---|---|---|---|
| холодный | **66,69 млрд** (разброс 0,18 %) | 3,96 с | 2,81 с | 6,84 с (3,4 %) | 97,1 МиБ (10,5 %) |
| повторный | **78,83 млрд** (разброс 0,17 %) | 3,97 с | 3,96 с | 8,03 с (3,7 %) | 100,4 МиБ (17,2 %) |

Снятие прежней копии обходится в 12,1 млрд инструкций и 1,2 с wall, почти все –
системное время. Против 2026-08-20 (user 2,70–2,79 с, пик ~100 МиБ) память не
выросла, хотя работы прибавилось.

### Отдельные операции

| операция | 2026-08-20 | 2026-09-24 |
|---|---|---|
| MD5 71 МиБ из памяти | 194.5 мс (364 МиБ/с) | 207.8 мс (342 МиБ/с) |
| MD5 71 МиБ с диска | 204.4 мс (347 МиБ/с) | 212.7 мс (334 МиБ/с) |
| сортировка 24k записей | 3.22 мс | 4.27 мс |
| подсчет блоков Unicode по 309 млн кодовых точек | 307 мс | 278 мс |
| структурная инвентаризация всего корпуса | 6.01 с user, 42 МиБ RSS | 6.27 с user, 45 МиБ RSS |

Сортировка подорожала на треть; после 20.08 `order.rs` менял только `c7d7cca`
(одна группа CTH – одна серия, даже когда ярлыки делят номер). MD5 медленнее на
4–7 % при разбросе замера 1,7 %; после 20.08 `md5.rs` менял только `e833a40`
(сумма с дескриптора, а не с пути). Причину ни того, ни другого этот перемер не
устанавливал: оба сдвига – меньше 0,2 % полного прогона.

Скрипт серии и сырые выводы остались вне репозитория, как и в прошлые
перемеры; команды те же, что в разделе «Как измерять».
